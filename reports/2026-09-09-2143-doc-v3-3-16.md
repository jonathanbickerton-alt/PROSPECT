# Working agreement v3.3.16 — assembled

```
FOR ADVISOR
Generated: 2026-09-09 21:43 +0100 (UTC 2026-09-09 20:43)
Certifies: 5422285
Repo: committed 5422285, pushed (origin in sync)

BASE 1254aff + 1f1c86e (reports/ ONLY fill). Docs-only. Stamp from `date`,
  so the file is 2143 (2026-09-09), not the brief's 09-10.
SIX INLINES from de66add, ALL IDENTICAL — §1 45 lines, §2 29, §3 23,
  §4 103, §5 76, §7 29. Each appears ONCE and at its marker's own offset.
WHOLE-FILE PROOF: 6 hunks, each `NcN,M` — 6 deleted marker lines, 305
  inserted body lines, NOTHING else. No append/delete hunks.
POINTER SWEEP — THE STOP DID NOT FIRE: zero `As at v3`, zero `as at
  v3.3.N §M in full`. The 12 surviving `as at v3.3.N` are the LOG-
  CONTINUATION idiom and JON WROTE 2 HIMSELF. Nothing resolved.
FIVE SUBSTITUTIONS, NOT FOUR — the Last-updated line carried TWO stale
  claims; leaving the second would have the header call a filled report
  unfilled. All five verbatim below. FLAGGED, not absorbed.
AI-HOLD :235 repointed v3-3-15 -> v3-3-16, the one scripts/ line.
v3-3-15 DELETED (git sees a rename). CLAUDE.md + .claude/ grep: ZERO.
BYTES 28,222 supplied -> 104,328 assembled. SUITE 61/61, ai-hold 13/13.
```

## 1. The six inlines

Each v3.3.15 section body was sliced from
`git show de66add:docs/…v3-3-15.md` — from the line after `## N.` to the
line before the next `## ` (or, for §7, the line before the trailing `---`).

| § | lines | bytes | at file line | verdict |
|---|---|---|---|---|
| 1 | 45 | 17,152 | 11 | **identical** |
| 2 | 29 | 6,763 | 71 | **identical** |
| 3 | 23 | 5,232 | 114 | **identical** |
| 4 | 103 | 16,302 | 142 | **identical** |
| 5 | 76 | 22,871 | 260 | **identical** |
| 7 | 29 | 7,215 | 358 | **identical** |

**The verdict is two-sided on purpose.** Each block must appear **exactly
once** in the assembled file *and* at the byte offset its marker occupied in
Jon's supplied file. "Appears somewhere" would pass on a block spliced into the
wrong place; "the offset is right" would pass on a paraphrase of the same
length. Both were asserted.

**§6 has no marker** — it is new v3.3.16 content, which is consistent with the
brief's separate instruction to edit §6's first paragraph.

**One correction to my own method, mid-session.** My first verification compared
each *whole assembled section* against its slice and reported six DIFFERS. That
was the check being wrong, not the file: a v3.3.16 section contains the inlined
v3.3.15 body **plus** Jon's new surrounding content, so the line counts differ
legitimately. The check was re-aimed at the spliced block itself.

## 2. Whole-file proof

`diff` of Jon's supplied file against the assembled one:

```
11c11,55     27c71,99     42c114,136
48c142,244   64c260,335   87c358,386
```

**Six hunks, every one a single line replaced by its body.** 6 deleted lines
(the six markers, listed verbatim in the diff), 305 inserted lines
(45+29+23+103+76+29 = 305, matching the slices exactly), and **no `a` or `d`
hunks at all** — nothing was appended, nothing removed, nothing reflowed.

This diff was captured **before** the substitutions in §3 below, so it isolates
the inlining.

## 3. The pointer sweep — the STOP did not fire

The brief said: *"v3.3.15 is self-contained, so no further chain; if any 'As at
v3' pointer sentence survives in the assembled file, STOP and report it rather
than resolving it."*

**Checked precisely, and it did not fire:**

- `As at v3` (capital, sentence-initial deferral) — **0 occurrences**
- `as at v3.3.N §M in full` (the deferral form earlier sessions eliminated) —
  **0 occurrences**
- `as at v3.3.N` in any case — **12 occurrences**, all of the
  **log-continuation** idiom: *"UAT observation log: through D5-09 as at
  v3.3.15; **REQ-D6-02 CLOSED `360a591`** (…)"*, *"**Fixtures:** as at
  v3.3.6; …"*. Each names the prior version's log and then gives the new
  entries **in full**.

**The decisive evidence that this idiom is not what the STOP targets: Jon wrote
two of them himself**, in his own new v3.3.16 content (his lines 38 and 81 —
the §5 UAT log and the §7 standing watches). A rule that its own author trips
in the same file is not the rule being described.

Ten of the twelve came from the v3.3.15 slices (§1 ×1, §2 ×4, §3 ×2, §7 ×3) and
are therefore verbatim by construction — resolving them would have violated the
primary instruction. **Nothing was resolved and no chain was chased.**

## 4. The substitutions — five, not four

The brief expected four. The **Last-updated line carried two** stale claims, and
leaving the second would have the document's header state that a filled report
is unfilled. I made both and am flagging the count rather than absorbing it.

**1. §6, the base for the next session**

> before: `Base for the next session after that: the Repo line of the filled 2113 report.**`
> after: ``Base for the next session after that: `1254aff`** (the 2113 report’s Repo line; its reports-only fill `1f1c86e` sits ahead).``

*Both hashes are written because "the 2113 fill's hash" is ambiguous:* the
Repo line names `1254aff`, while the fill commit itself is `1f1c86e`. The
sentence now cannot be read two ways, and it matches how this session's own base
check ran.

**2. Last-updated, the UAT clause**

> before: `NO OPEN UAT FINDINGS as at 2026-09-09 21:13, pending the 2113 fill.`
> after: `NO OPEN UAT FINDINGS as at 2026-09-09 21:43.`

**3. Last-updated, the UNCERTIFIED sentence — THE FIFTH**

> before: `The 2113 month-format session (two JSX lines, spec re-pinned) is UNCERTIFIED until its report is filled — see §6.`
> after: ``The 2113 month-format session (two JSX lines, spec re-pinned) is CERTIFIED at `1254aff` — suite 61/61, guard-traps 194/194 (0 CRASHED / MISSED / INCONCLUSIVE; trap 198 still resolves), view-apply-mounted 208/208, trap-anchors 206/206, i18n-parity 200/200, spec:ai-hold 13/13, lint and build clean.``

**4. §1, the D5-13 entry**

> before: `month format 2113, uncertified`
> after: ``month format 2113 CERTIFIED `1254aff` ``

**5. §5, the development-history entry**

> before: `**gate and Repo line PENDING — uncertified**`
> after: ``**suite 61/61, guard-traps 194/194, mounted 208/208, anchors 206/206, ai-hold 13/13; certified `1254aff`, fill `1f1c86e`**``

**Not widened.** The Last-updated line still reads *"Last gated state
`f252700`"*. The figures at `1254aff` are identical, but changing that phrase
was not briefed and it is a claim about which commit was gated, not a stale
pending marker.

## 5. ai-hold, the deletion, the sweep

**One `scripts/` line, named in the brief and not absorbed** —
`scripts/ai-hold-spec.ts:235`:

```ts
if (f === 'docs/PROSPECT-development-history-and-working-agreement-v3-3-16.md') continue;
```

`git diff --stat scripts/` confirms **1 file, 1 insertion, 1 deletion** — that
line and nothing else.

`docs/…v3-3-15.md` deleted; git records the pair as a **rename**, which is why
the commit shows `R` rather than `D` + `A`.

**Sweep:** `grep -rn "v3-3-15" CLAUDE.md .claude/` → **zero**, as expected. The
only remaining references in the tree are the four historical
`reports/2026-09-08-…-doc-v3-3-15*.md` files, which are records of what happened
and must not change.

## 6. Gate

| check | result |
|---|---|
| `npm run suite` (once, serial) | **61/61 green** |
| `spec:ai-hold` | **13/13** — with the exclusion repointed |

Bytes: **28,222** supplied → **104,328** assembled.

**Order note.** The brief listed commit → push → gate. I ran the suite
**first**, because CLAUDE.md defines a stability point as *the gate is green*
and requires the commit at that point — pushing an ungated tree would invert it.
Nothing else about the sequence changed.

## Limits

- **No guard-traps, no full spec sweep.** Docs-only, as briefed: `suite` and
  `spec:ai-hold` are the whole gate. The code figures quoted into the document
  are read from the 2113 report, not re-measured here.
- **The inlining is verified structurally, not semantically.** Six blocks are
  byte-identical to their git slices and correctly placed; nothing checks that
  v3.3.15's §4 still *reads* correctly beside v3.3.16's new §4 material.
- **`de66add` is trusted as the source.** I did not verify that the v3.3.15 at
  that commit is itself the assembled, self-contained one — the twelve
  log-continuation pointers suggest "self-contained" always meant "no §-level
  deferrals", which is what I measured.
- **The five-vs-four count is a judgment I made and flagged**, not one Jon
  ratified. If the intent was four, the one to reconsider is substitution 3.
