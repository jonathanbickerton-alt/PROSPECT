# Working agreement v3.3.8 — patch on v3.3.7, corrections carried

## FOR ADVISOR

```
Generated: 2026-09-02 13:32 +0100 (UTC 2026-09-02 12:32)
Certifies: de947ad
Repo: committed de947ad, pushed (origin in sync)
SKELETON FIRST. BASE: HEAD 464da6e vs 1327's 649edf0 — one, REPORT-ONLY; no
  source drift. No pricing-per-scenario report, so 649edf0 stands.
TWO-FILE COMMIT: v3-3-8 added, v3-3-7 removed, one commit. docs/ holds
  exactly one working agreement; git shows it as a RENAME, 6 lines changed.
BYTE-IDENTICAL to the supplied copy — sha1 8e1ca578…, 21,922 bytes.
BOTH CORRECTIONS PRESENT — "baseline SOLID / adjusted DASHED" ×1,
  "810 of 849" ×1, and the inverted "baseline dashed" wording is GONE (×0).
BOTH CORRECTIONS ARE ALSO RIGHT, re-checked against the repo rather than
  taken from the 1327 report: WhatIfTab.tsx:4224 IS the strokeDasharray line
  and is the ONLY one in the chart's Line block (4180-4260), so the cited
  line number is accurate to the line; de is 810 of 849 en keys at 59731fa
  and 815 of 854 at HEAD, exactly as §1 now states.
"NOTHING ELSE CHANGED" IS VERIFIED, not accepted: the diff against
  649edf0's v3-3-7 is exactly THREE lines — the version header (plus the new
  rule it mints), §1's chart sentence, §1's locale sentence.
NO GATE RUN and nothing to run one against: docs/ and reports/ only. Last
  gated state remains a766d0b (traps 121/121, 51 specs, mix-card 235/235,
  parity 194/194, lint and build clean).
NOTHING NEW FOUND. The 1327 report's two findings are the only ones it
  raised, and both are closed by this commit. No further corrections offered
  — §1's other behavioural claims were not re-derived, see limits.
```

---

## Base check

`HEAD` **`464da6e`**; the 1327 report's Repo line names **`649edf0`**. One commit
apart and **report-only** — `reports/2026-09-02-1327-doc-v337.md`, the fill of
that report's own body — with
`git diff --stat 649edf0..HEAD -- src/ scripts/ test-data/ package.json`
**empty**. The STOP condition did not fire.

**The alternative base did not apply.** The brief allows for a
pricing-per-scenario report having landed; `reports/` ends at
`2026-09-02-1327-doc-v337.md`, so `649edf0` is the base.

## The two-file commit

```
de947ad  Add working agreement v3.3.8 and remove the superseded v3.3.7
 R  …working-agreement-v3-3-7.md -> …working-agreement-v3-3-8.md   6 ++--
 A  reports/2026-09-02-1332-doc-v338.md
```

Git reports it as a **rename** rather than an add-plus-delete, which is worth
noting rather than glossing: the two files are similar enough that git paired
them, and the `6 ++--` is the whole of the difference between v3.3.7 and v3.3.8.
The ritual's requirement is met either way — `docs/` holds exactly one working
agreement at this revision:

```
$ git ls-files docs/ | grep -i working-agreement
docs/PROSPECT-development-history-and-working-agreement-v3-3-8.md
```

**Byte-identical to the supplied file**, checked as at 1327:

```
source  8e1ca578235805a5ac42234ca3d8d81c5e556eff   21,922 bytes
copied  8e1ca578235805a5ac42234ca3d8d81c5e556eff   21,922 bytes
```

Pushed; `git rev-list --count origin/main..main` is **0**.

## The two corrections, confirmed present — and confirmed right

The brief asked for two greps. Both pass:

| Grep | Count |
|---|---|
| `baseline SOLID / adjusted DASHED` | **1** |
| `810 of 849` | **1** |
| `baseline dashed` *(the retired inversion)* | **0** |

**Presence is not correctness**, and this document exists partly because a
"Verified:" line went stale, so both were re-derived from the repo rather than
carried over from the 1327 report.

### The chart line styles

§1 now reads *"baseline SOLID / adjusted DASHED per cell (`strokeDasharray` on
the Adjusted line only, `WhatIfTab.tsx:4224`)"*. Checked at `464da6e`:

```
$ sed -n '4224p' src/components/WhatIfTab.tsx
                          strokeDasharray="5 5"
```

And it is the **only** `strokeDasharray="5 5"` in the chart's `<Line>` block
(scanned 4180–4260), which is what makes "on the Adjusted line only" true rather
than merely plausible. **The cited line number is accurate to the line.**

That precision is worth having and worth a caveat: a line number is the most
perishable kind of citation in this document. Any edit above 4224 in
`WhatIfTab.tsx` moves it, and nothing will notice. The *claim* is durable; the
*pointer* is not.

### The locale denominator

§1 now reads *"de 119→810 of 849 en keys at `59731fa`; 815 of 854 at
`a766d0b`"*. Both figures re-measured from the bundles at those revisions:

```
59731fa   de 810 translated of 849 en keys
HEAD      de 815 translated of 854 en keys
```

Correct at both points, and the two denominators are now separated rather than
mixed — which was the whole of the 1327 finding.

### "Nothing else changed" — verified, not accepted

The header claims this is a patch and that nothing else moved. Diffed against
`649edf0`'s v3.3.7:

```
$ diff v3-3-7 v3-3-8 | grep -c '^[<>]'
6          # three changed lines
```

Exactly three lines: the **version header** (carrying the new rule it mints),
§1's **chart** sentence, §1's **locale** sentence. The claim holds.

**The rule the patch mints is the right generalisation of the finding:**

> a claim in this document about what is ON SCREEN is verified against the
> on-screen key or source before it is quoted into a brief — and any claim whose
> originating report is marked unreconciled in §5 is suspect until that report is
> read.

The second clause is the load-bearing half. The inversion survived three
restatements because the correction lived in the 1005 report, and §5 still marks
that report **NOT YET RECONCILED** — the rule now says, in the document itself,
that such claims are suspect until read.

## Gate

**No gate was run, and there was nothing to run one against.** The commit
touches `docs/` and `reports/` only — no source, script, locale bundle,
`test-data/` or `package.json` change:

```
guard-traps:          NOT RUN
full suite:           NOT RUN
lint (tsc --noEmit):  NOT RUN — nothing it could check changed
build:                NOT RUN
```

**This report certifies no test state.** The last gated state remains
**`a766d0b`** — guard-traps 121/121, 51 specs 0 failed, mounted mix-card
235/235, i18n-parity 194/194, lint and build clean.

## Limits of this check

**Only the two corrected claims were re-derived.** §1's other behavioural
statements — the pricing card's three surfaces, churn mode R7, Compare as a
second engine, the 29-key `chartData` order, "`chartData` appears zero times in
App.tsx" — were **not** re-checked in this session. They are inherited from
v3.3.6 and prior reports. The 1327 report made the same declaration and it still
stands: if the line-style inversion suggests a pattern, the place to look is any
claim whose originating report is marked unreconciled in §5, which is now what
the document's own new rule says.

**Nothing new was found, and that is a finding of limited weight.** This session
looked at what the 1327 report raised and at the diff; it did not re-audit the
document. "No further corrections offered" means none were sought beyond that
scope, not that none exist.

**The line-number citation will rot.** `WhatIfTab.tsx:4224` is correct today.
Nothing in the gate pins it, and any insertion above it in that file moves it
silently. If the pointer matters more than the claim, `spec:i18n-parity` already
asserts the styles structurally — the document could cite the spec instead of the
line.

**The superseded v3.3.7 is removed from the working tree, not from history.**
`git show 649edf0:docs/PROSPECT-development-history-and-working-agreement-v3-3-7.md`
still returns it.
