# The six non-unique trap anchors, fixed one at a time

## FOR ADVISOR

Generated: 2026-09-04 12:04 +0100 (UTC 2026-09-04 11:04)
Certifies: 7b64ddb
Repo: committed 7b64ddb, pushed (origin in sync)
BASE: 3773ce1 - diff EMPTY. STOP did not fire. NO source file changed.
FOUR FIXED, TWO STOPPED. Intended site MEASURED by planting each candidate
  separately, not read off the trap's name - that changed the answer twice.
24: 2 occ (1606, 1653) -> 1 at 1606. FAIL 81/82 'MACHINE: the blocked branch
  is reachable'. 1653 reddens NOTHING - a blocked branch NO spec covers.
26: 2 occ (1698, 2128) -> 1 at 1697, extended UPWARD (a downward pin is what
  broke its earlier version). FAIL 'BANNER: the effect clears the error'.
51: 3 occ (774, 893, 1028) -> 1 at 774; all three redden identically, the
  spec's subject being the set. FAIL 'WIRING: no import site discards ...'.
124: 2 occ (3721, 3795 - the second INSIDE A COMMENT) -> 1 at 3721. FAIL
  'IDENTIFIER: App.tsx still carries exactly 20 occurrences [19]'.
22 STOPPED: 2 occ, byte-identical for 26 lines; the spec asserts
  occurrences === 2, so its subject is the PAIR. A unique anchor needs 18
  lines and pins one row - uniqueness is the WRONG requirement here.
102 STOPPED: at its intended site mix-card CRASHES (undefined reading
  'subscriberVolume'), it does not fail. guard-traps reads an EXIT CODE, so
  102 printed [CAUGHT] every run while nothing asserted its defect.
MY PROCEDURE FAILED TWICE: a plant hit 'Permission denied' and the spec then
  read green (= nothing planted, NOT uncovered); a spec timed out mid-plant.
BASELINE now = {22, 102}. GATE (serial): guard-traps 139/139, no MISSED and
  no INCONCLUSIVE - no extended anchor ages out on the harness LF path;
  58/58 specs; trap-anchors 150/150; traps 139 -> 139; tsc+build clean.

## Base check

`git diff --stat 3773ce1..HEAD -- src/ scripts/ test-data/ package.json` EMPTY.
HEAD at entry was `31a2f6a`, which is 0958's report and touches no source. The
STOP did not fire.

## Method

Per trap, in order, nothing batched: print every occurrence with line numbers;
identify the intended site; draft the extended anchor and **count it before
replacing**; replace; watch the baseline check go red and remove the entry;
plant by hand and confirm the catching spec reddens with a FAIL line that
matches the trap's name; restore.

**FOUR FIXED, TWO STOPPED.** The two stops are not leftovers — each was
measured, and in both cases the anchor turned out not to be the problem.

**Where possible the intended site was MEASURED, not inferred**, by planting at
each candidate occurrence separately and seeing which one the catching spec
notices. That changed the answer twice and produced two findings that a
name-based reading would have missed entirely.

**No source file changed in this session.** Only `scripts/guard-traps.ts` and
`scripts/trap-anchors-spec.ts`.

## Trap 22 — STOPPED

`t('actuals_cohort_months_compared', { n: summaryMape.monthsWithActuals })`,
**2 occurrences**, lines 3142 and 3170 of `ForecastVsActualsTab.tsx`.

The two sites are **byte-identical for 26 lines** — 17 above the anchor and 9
below — because they are the volume KPI row and the ARPU KPI row rendering the
same label. They diverge only at the enclosing map:

```
3125  {(['inflow', 'outflow', 'retention', 'base'] as const).map(kpi => {
3153  {(['inflowArpu', 'outflowArpu', 'retentionArpu', 'baseArpu'] as const).map(kpi => {
```

And the catching spec's assertion is about the **pair**:

```js
check('GRAIN: both KPI card sites are keyed', occurrences === 2, `found ${occurrences}`);
```

Planting at the first site reddens it correctly:

```
coverage-copy spec: 34 passed, 1 failed
  FAIL GRAIN: both KPI card sites are keyed  [found 1]
```

**Uniqueness is the wrong requirement for this trap, not an unmet one.** The
minimum unique anchor spans 18 lines including a comment block — far more
rot-prone than what it replaces — and it would tie the trap to the volume row
while the spec's subject is both rows.

The natural fix is a trap that mutates EVERY occurrence, so "which one does
`replace()` take" stops being a question, with `spec:trap-anchors` taught to
treat a global mutation as uniqueness-exempt while still recording the count.
That changes the harness contract, so it is reported rather than taken.

## Trap 24 — FIXED

`    if (unfittable.length > 0) {`, **2 occurrences**, lines 1606 and 1653 of
`App.tsx` — the leaf-scope reader and the book reader.

**Measured, not inferred.** Planting at each site separately:

| site | result |
|---|---|
| 1606 | `walk-fixes 81/82` — `FAIL MACHINE: the blocked branch is reachable — it is not gated on a constant` |
| 1653 | `walk-fixes 82/82` — **nothing fails** |

So 1606 is the intended site, and the trap was hitting it by ordering alone.

New anchor, three lines, **1 occurrence at line 1606** — only the leaf reader's
return lacks `keys`:

```
    if (unfittable.length > 0) {
      return { kind: 'blocked' as const, missing: 0, total: leaves.length,
               unfittable: unfittable.length };
```

Baseline went red (`5 of 6`), entry removed, 150/150. Hand-plant reddens
`MACHINE: the blocked branch is reachable` at the intended site.

**FINDING, recorded not fixed: the second blocked branch at 1653 is covered by
no spec at all.** Its `kind: 'blocked'` return can be gated off to a constant
and the whole suite stays green.

## Trap 26 — FIXED

`    setError('');\n    setNotice('');`, **2 occurrences**, lines 1698 and 2128
of `App.tsx` — the selection-keyed `useEffect` and `generateStandardForecast`.
The first is what the trap's name describes, and the trap's positioned
terminator search keeps the splice inside it, so it planted correctly.

**Extended UPWARD, deliberately.** The trap's own comment records that an
earlier version broke the moment `setNotice` was added between the anchor and
the terminator. Extending downward would re-pin exactly that span. Adding the
`useEffect(() => {` line above gives **1 occurrence at line 1697** and leaves
the span free.

Baseline went red (`4 of 5`), entry removed, 150/150. Hand-plant:

```
walk-fixes spec: 80 passed, 2 failed
  FAIL BANNER: the effect clears the error
  FAIL BANNER: and the stale result panel with it
```

## Trap 51 — FIXED

`historicalMonths:      parseStoredMonths(first.Historical_Months),`,
**3 occurrences**, lines 774, 893 and 1028 of `App.tsx` — the three import
sites.

All three redden `spec:restore-base` identically, because its checks are about
the SET of sites (`all three sites use the shared parser`). No one of them is
more intended than another, so the anchor is pinned to **774**, where it already
planted — nothing about what is caught changes.

New anchor, two lines, **1 occurrence at line 774**; the following line's double
space before `?? 0` is what the other two lack.

Baseline went red (`3 of 4`), entry removed, 150/150. Hand-plant:

```
restore-base spec: 13 passed, 2 failed
  FAIL WIRING: no import site discards the saved months
  FAIL WIRING: all three sites use the shared parser
```

## Trap 102 — STOPPED, and the reason is the important one

`    if (isChurnDraft) {`, **3 occurrences**, lines 3159, 3559 and 3782 of
`WhatIfTab.tsx`. The trap's name says "the churn **add**", which is 3159, and a
three-line anchor unique at 3159 was drafted and confirmed.

It was not applied, because planting at 3159 does this:

```
mix-card spec CRASHED — TypeError: Cannot read properties of undefined
  (reading 'subscriberVolume')
    at main (scripts/mix-card-spec.tsx:1738:47)
```

**The spec does not fail. It dies.** With the churn add gated off no event is
produced, so the spec dereferences a row that is not there.

`guard-traps` reads an exit code, so **a crash and a failed assertion are
indistinguishable to it**. Trap 102 has been reporting `[CAUGHT]` in every gate
run while nothing asserted the defect its `why` describes — "every churn Add
emits one zero-volume event with no churn fields". That is the brief's
catching-by-accident condition exactly.

The other two sites DO have real assertions, which is what makes the diagnosis
solid rather than a guess:

| site | result |
|---|---|
| 3159 (add) | **CRASH** |
| 3559 | `228/235` — `FAIL R7 group (i): ATOMIC — exactly three rows` |
| 3782 | `232/235` — `FAIL R7 edit (b): THE SIGN SURVIVES — the delta is still POSITIVE  [-5209.077604166665]` |

Its anchor is not the problem, and making it unique would only make a broken
trap unique. The assertion is the defect. Baseline entry left in place.

## Trap 124 — FIXED

`forecastType: 'Standard Forecast',`, **2 occurrences**, lines 3721 and 3795 of
`App.tsx` — and the second is **inside a comment**, the note recording why the
What-If branch was deleted.

The trap planted at the right site only because the code happens to precede the
comment about it. Adding `                scenario: scen,` gives **1 occurrence
at line 3721**, so a comment can no longer decide where a trap plants by being
moved above the code it describes.

Baseline went red (`2 of 3`), entry removed, 150/150. Hand-plant:

```
forecast-type-split spec: 52 passed, 2 failed
  FAIL IDENTIFIER: App.tsx still carries exactly 20 occurrences  [19]
  FAIL IDENTIFIER: exactly 7 are quoted literals  [6]
```

## The baseline list, after

```js
const KNOWN_NON_UNIQUE = new Set([
  '22 the cohort-months label loses its grain',
  '102 the churn add falls behind the spread gate',
]);
```

Its comment block was rewritten to match: it no longer says "six", and each
remaining entry carries the measurement that justifies it, so the next reader
does not have to re-derive why these two stayed.

## Two procedural failures of mine, worth recording

**A plant silently did not apply.** In trap 51's first probe, occurrence 0's
mutation hit `python3: Permission denied` and the spec then reported
`15 passed, 0 failed`. Read quickly that says "site 0 is not covered"; it
actually says "nothing was planted". Re-run properly, all three sites redden.
This is the INCONCLUSIVE-vs-CAUGHT distinction reappearing inside my own manual
procedure: **a green spec after a plant means nothing unless the plant is known
to have landed.**

**A spec exceeded the two-minute default and was killed mid-plant**, leaving
`App.tsx` mutated. Restored immediately and verified with `git diff` before
continuing — recorded because a timeout during a plant is exactly how a
mutation gets committed by accident.

## Limits of this check

- **Only trap 24's sites were probed exhaustively before a decision.** For 26
  and 124 the intended site was clear from the trap's name and the code/comment
  distinction, and only the intended site was planted.
- **Trap 51's anchor pins an arbitrary one of three equivalent sites.** All
  three redden identically; if the 774 block is deleted the trap ages out even
  though two working sites remain.
- **Trap 51's new anchor depends on WHITESPACE** — the double space before
  `?? 0` — which a formatter would erase. The `spec:trap-anchors` ZERO check
  would catch that at once, but it is a thinner distinction than the others.
- **Trap 102 is left as it was**, still reporting CAUGHT by crash, and trap 22
  still plants at the first of two identical sites. Both are stated, neither is
  repaired.
- **Nothing here tests that the four fixed traps still catch under guard-traps
  itself** beyond the gate run below; the by-hand plants exercised the mutation,
  and the gate exercises the harness path.

## Gate

```
guard-traps:  139/139 caught, no MISSED, no INCONCLUSIVE
full suite:   58/58 spec scripts green
trap-anchors: 150/150 (139 traps, 146 anchors)
lint:         tsc --noEmit clean
build:        clean

Run SERIALLY. The guard-traps figure is the one that matters here: the four
extended anchors were verified BY HAND, but only a full harness run proves
they still match through guard-traps own LF-normalised path. Zero
INCONCLUSIVE means none of the four ages out on line endings — the failure
that took four traps at once on 2026-08-13.

Trap count 139 -> 139: no trap was added, removed or retired.
```
