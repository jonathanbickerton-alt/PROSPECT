# REQ-D6-06 follow-up — the member edit bar; the Promotion per-row bin under the dialog

## FOR ADVISOR

```
Generated: 2026-09-15 14:48 +0100 (UTC 2026-09-15 13:48)
Certifies: 1bc094b   (REQ-D6-06 clauses 6-8: member edit bar; promo bin)
Repo: committed 1bc094b, pushed (origin in sync)
BASE 88e0adb; status --short EMPTY; diff to HEAD EMPTY. Both quoted.
1 CLAUSES 6-8 RECORDED, EXPECTED.md alone (9116140), before any code.
2 EDIT BAR WIDENED by the ONE predicate: ramp/held/churn member pencils
  aria-disabled, "Use the campaign pill" as text; Spread and single rows,
  and the campaign pill, unchanged. Mounted campaign-delete 97/97.
3 ENFORCED AT THE ROW ENTRY, not in handleEditStart: the campaign editors
  call that to seed drafts, so a bar there would close the pill's route.
4 CONFLICT, RESOLVED AND STATED: aria-disabled, NOT native disabled - a
  dead click would redden trap 107's own "a refusal is a stated reason".
5 CHURN keeps handleEditStart's arm and sentence; trap 107 anchor unaged.
6 PROMO ROW BIN under the dialog; removeMarketEvent( occurs 0 times in
  WhatIfTab. Harness remover now REAL: Confirm drops one row, Cancel none.
7 RED FIRST: survey 70/72 - campaign-delete's 4 predicted reds (clause 8
  wording, predicate count 4 -> 7) and trap 160's anchor aged. Re-aimed.
8 TRAPS 243, 244 RED by hand; 160 re-anchored, RED. None stayed green.
9 LIMIT: the Promotion pencil guards at the row (that card has no decline
  panel); trap 243 plants the Volume row entry.
SHED: nothing. Items 0, 1 and 2 done.
guard-traps 240/240 caught; full suite 72/72 green.
```

## 0. Base

BASE is `88e0adb`, the 1143 report's `Repo:` and `Certifies:` hash; HEAD carried
only that report's fill (`751ac77`) on top of it.

```
$ git status --short
(empty)

$ git diff 88e0adb HEAD --stat -- src scripts test-data
(empty)
```

Both empty. The skeleton `5ee0c6c` was the session's **first repo action**.

## 1. Item 0 — clauses 6–8 recorded

Clauses 6, 7 and 8 were appended verbatim under REQ-D6-06 in `test-data/EXPECTED.md`,
and committed **alone** at `9116140`, before any code.

## 2. Item 1 — the edit bar widened

**Measured first, and it moved where the bar goes.**
- The churn bar is inside `handleEditStart`.
- `handleEditCampaignStart` and `handleEditPromoCampaignStart` both call the row
  handlers (`handleEditStart` / `handleEditPromoStart`) to seed their drafts.
- A ramp or held member refused inside those handlers would therefore close the
  campaign pill's route too, and case (c) would break.

So the widened refusal stands at the **row entry**, `editEventFromVolumeTable`, the
only route the per-row pencil takes. The rule reads `isCampaignStepMember`, the one
predicate from 1143, with no second copy. It refuses a named ramp or held member with
`whatif_member_no_edit`, shown in the existing `edit-decline-reason` panel.

**Churn is left to `handleEditStart`'s own arm**, which is untouched. If the row
entry declined churn members first, that arm would be shadowed: trap 107 could
plant `if (false)` and nothing would notice. The trap 107 anchor did not age, so it
was not re-anchored, and the new spec pins the arm verbatim.

**A conflict in the brief, resolved and stated.**
- The brief asks for the pencil "disabled + `aria-disabled`", and for case (d),
  "trap 107's assertion still green".
- That assertion (`mix-card` R7 (c)/(g)) clicks a churn member's pencil and requires
  "a refusal is a stated REASON, not a dead click".
- A native `disabled` makes the click dead and would redden it.

So the Volume pencil is `aria-disabled`, greyed, titled and followed by the reason
text, but keeps its click, which reaches the rule and is refused with a sentence.
`mix-card` stayed green (237/237).

**The Promotion table's pencil** reads the same predicate and shows the same reason.
That card has no decline panel, so its click returns at the row: the refusal is the
reason rendered beside the pencil.

**Reason keys.**
- `whatif_row_edit_barred`: "Use the campaign pill", beside each barred pencil.
- `whatif_member_no_edit`: the decline sentence.
- `whatif_row_delete_barred`: re-worded to "Use the campaign bin" (clause 8).

These are in six locales: one re-worded and two added, so 916 → **918**.

### Mounted — `campaign-delete`, follow-up block, fill-in order

```
campaign-delete spec: 97 passed, 0 failed
view-apply-mounted spec: 208/208 passed
mix-card spec: 237/237 passed
trap-anchors spec: 254 passed, 0 failed  (240 traps, 249 anchors)
```

- **(M-a) a held member:** the pencil is `aria-disabled="true"` and the reason
  "Use the campaign pill" is in the DOM. A click opens no row editor: no "Save
  changes", and the draft is not seeded. The member sentence is stated verbatim,
  and the bin reason reads "Use the campaign bin".
- **(M-b) a Spread member and a single event:** neither pencil is barred. The
  Spread member's pencil opens the row editor, seeded from that row's campaign and
  month.
- **(M-c) the held campaign's pill:** it still opens the campaign editor ("Save
  campaign"), seeded with the campaign.
- **(M-a2) a Promotion ramp member:** the pencil is barred, the reason is shown, a
  click opens nothing, and the bin reason is the short form.
- **(d) churn:** `mix-card`'s R7 assertions are green, and the spec pins
  `handleEditStart`'s churn arm verbatim.

## 3. Item 2 — the Promotion per-row bin under the dialog

**Not shed.** The Promotion row bin now stages `setPendingChange({ kind: 'delete',
nextEvents: … })`, the same single-row kind and wording as the Volume row bin. It
commits through `confirmPendingChange` → `setMarketEvents`, the setter the campaign
kind uses. **`removeMarketEvent(` now occurs 0 times in `WhatIfTab`**, which the
spec pins as a structural zero, along with exactly two `kind: 'delete'` stagings.

**The harness gap from 1143 is closed.** `campaign-delete`'s host now passes a
**real** `removeMarketEvent`. A bin that bypassed the dialog would visibly delete,
so trap 244 is red on the missing dialog rather than hidden by a no-op.

- **(M-f) Cancel:** the dialog reads "Delete this event?". Nothing is removed while
  it is open, and Cancel leaves 3 / 3.
- **(M-e) Confirm:** the Spread promotion drops by exactly **one** (3 → 2), that
  row's id is gone, and the ramp promotion keeps 3.

## 4. Red first and traps

The whole suite on the changed `src`, before any spec was touched:

```
full suite on the changed src, specs untouched: 70/72 green
[FAILED ] spec:campaign-delete
[FAILED ] spec:trap-anchors
70/72 green
── campaign-delete
campaign-delete spec: 61 passed, 4 failed
  FAIL  1.4 PIN: ONE member predicate — defined once, read by the edit bar and both row bins (4 occurrences)  [7]
  FAIL  (d) the held member's reason is TEXT in the DOM: "Delete the whole campaign with its bin"  [Use the campaign bin]
  FAIL  (d) the ramp member's reason is TEXT in the DOM: "Delete the whole campaign with its bin"  [Use the campaign bin]
  FAIL  (c) DECISION 3 on this card too: a ramp member's per-row bin is disabled, reason as text
── trap-anchors
trap-anchors spec: 251 passed, 1 failed  (238 traps, 247 anchors)
  FAIL  trap 160 the route stops asking whether the row is a promotion: anchor 1 still matches WhatIfTab.tsx  [ZERO — the anchor has aged out; the trap plants nothing]
```

**`campaign-delete`'s four reds were the predicted ones:** clause 8's new wording in
(d) ×2 and (c), and the predicate count, now 7 (the definition, the churn arm, the
row entry, both pencils and both bins). They were re-aimed only after this red.

**Trap 160 was not predicted.** Its two-line anchor was `editEventFromVolumeTable`'s
header followed by `if (event.isPromotion)`, and the member rule now stands between
them. It is re-anchored on the promotion test's own three-line body, which occurs
once, with the same plant.

**One red was my own.** The first verification run showed a new pin at 3,
not the 2 I had written: "exactly two per-row bins stage `kind: 'delete'`". The
third occurrence was the `pendingChange` type union
(`{ kind: 'delete' | 'edit' | …`), not a third bin, so the code was right and the
count was mis-measured. The pin now counts the staging call,
`setPendingChange({ kind: 'delete'`, which is 2. This was a check written this
session, not a re-aim of an assertion that had been green. No trap was planted
before it was fixed.

Each trap was planted by hand, restored from a scratchpad backup, and md5-verified:

```
trap 243   campaign-delete spec: 93 passed, 4 failed
  FAIL  1.4 PIN: ONE member predicate — defined once, read by the churn edit arm, the row entry, both pencils and both bins (7 occurrences)  [6]
  FAIL  (M-a) a click does NOT open the row editor — no "Save changes"
  FAIL  (M-a) and the draft is not seeded from the member  [H camp]
  FAIL  (M-a) the refusal is STATED — the member sentence, verbatim  [absent]
  pre-plant md5 79ef36b8c08d13b1b6a86ff35bdcbd16, restored 79ef36b8c08d13b1b6a86ff35bdcbd16

trap 244   campaign-delete spec: 89 passed, 8 failed
  FAIL  (M) PIN clause 7: NO bin calls removeMarketEvent — zero occurrences of `removeMarketEvent(` in WhatIfTab  [1]
  FAIL  (M) PIN clause 7: exactly two per-row bins stage `setPendingChange({ kind: 'delete'` — Volume and Promotion  [1]
  FAIL  (M) PIN clause 7: the Promotion row bin stages the dialog change
  FAIL  (M-f) CLAUSE 7: the Promotion row bin asks first — "Delete this event?"  [no dialog]
  FAIL  (M-f) nothing is removed while the dialog is open  [2]
  FAIL  (M-f) Cancel closes the dialog and changes nothing — 3 / 3  [3 / 2]
  pre-plant md5 79ef36b8c08d13b1b6a86ff35bdcbd16, restored 79ef36b8c08d13b1b6a86ff35bdcbd16

trap 160   view-apply-mounted spec: 202/206 passed
  FAIL  D5-04: the pencil SWITCHES to the Promotion tab  [promo false volume true — seeding the draft behind the Volume card is an invisible edit]
  FAIL  D5-04: the promotion editor restored the % unit  [the D5-03 restore, reached through the Volume pencil]
  FAIL  D5-04: and restored the mix and the lock  [the arms the Volume form cannot represent]
  FAIL  D5-04: the PROMOTION editor did  [0]
  pre-plant md5 79ef36b8c08d13b1b6a86ff35bdcbd16, restored 79ef36b8c08d13b1b6a86ff35bdcbd16
```

**Every trap went red on the first plant; none needed a discriminator, and none was removed.**

## Gate

Run **serially** as one chain writing to files; captures in the scratchpad:
`gate-mb.out`, the suite in `gate-mb-suite.out`, guard-traps in `gate-mb-gt.out`.

| check | figure |
|---|---|
| `npm run suite` | **72/72 green** |
| guard-traps (to a file, per-trap lines) | **240/240 caught**, 0 missed / inconclusive / crashed; unfiltered, no FATAL; 107, 160, 243, 244 each CAUGHT |
| `spec:trap-anchors` | **254/254** — 240 traps, 249 anchors; next free id 245 |
| `spec:i18n-parity` | **200/200** |
| keys per locale | **918** in each of de / en / es / fr / it / pt |
| `spec:i18n-scan` | **PASS** |
| `spec:survival` | **27/27** — 104 first-row dereferences across 26 files |
| `tsc --noEmit` | **clean**, exit 0, 0 errors |
| `npm run lint` | **clean**, exit 0 |
| `npm run build` | **built in 5.56s**; the >2000 kB chunk notice is pre-existing |

**guard-traps left the tree clean:** `WhatIfTab.tsx` md5 `79ef36b8…` identical before and after the run (and `EventsSummaryTable.tsx`).

### Exact counts the brief named

| pin | required | measured |
|---|---|---|
| `runIngest` sites | 3 | **3** (ingest 53/53) |
| apply sites | 12 | **12** (event-toggle 156/156) |
| display markers | 6 | **6** |
| ramp/hold checkboxes | 4 | **4** (hold-shape 61/61; direct 4) |
| last-column, Market / Yield / Pricing | 4 / 2 / 3 trailing | **4 / 2 / 3** |
| `computeAdjustedForecast` sites | 6 | **6** (pricing-roundtrip 150/150; direct 6) |
| `handleSetEventEnabled` invocations | 5 | **5** |
| `handleDeleteCampaign` callers | 3 | **3** (campaign-delete 97/97) |
| `removeMarketEvent` direct callers from a bin | 0 (structural) | **0** in `WhatIfTab.tsx`, **0** in all of `src/components`; pinned in the spec as a structural zero |

## What was shed

**Nothing.** Item 0 was recorded and committed alone. Item 1 (never shed) is done:
the widened bar, the reason keys and (M-a)–(M-c), with (d) held by `mix-card`, plus
trap 243. Item 2 is done as well: the Promotion row bin under the dialog, the real
harness remover, (M-e)/(M-f) and trap 244.

## Limits

- **The Promotion pencil guards at the row**, because that card has no decline
  panel. Trap 243 plants the Volume row entry. A mutation of the Promotion pencil's
  own `if (rowEditBarred) return` is pinned only by (M-a2); no trap covers it.
- **A named barred pencil is `aria-disabled` but still focusable and clickable.**
  That was chosen deliberately (see §2). A keyboard user reaching it hears the title
  "Use the campaign pill", and on the Volume card a click states the full reason.
- **`removeMarketEvent` remains a prop of `WhatIfTab`**, passed by `App`, and is now
  unused there. The prop was not removed, because that would touch every harness.
- **An unnamed ramp's rows have no campaign pill,** so they are not barred (`!!campaignLabel`),
  exactly as the 1143 delete bar did.
- **Nothing was opened in a browser.** Everything is asserted in JSDOM, in English.
