# The Promotion card's preview shows the emitted shape; and the base lag

```
FOR ADVISOR
Generated: 2026-09-10 22:46 +0100 (UTC 2026-09-10 21:46)
Certifies: __ PENDING

BASE 7e95170 (code a60e3a1); HEAD 2cd330d. status --short EMPTY; diff
  a60e3a1..HEAD -- src scripts test-data EMPTY. Both quoted in the body.
1 THE GRID COMPUTED `pcts[i] / pctTotal` INLINE and never read promoHold
  — the hold-OFF split, in a card whose Add emitted the held shape. The
  chart read the EVENTS, so it was right all along.
2 DERIVE ONCE, READ THRICE: `promoShape` is now one useMemo read by the
  grid, the button AND buildPromoEvents — which no longer computes a
  shape at all; it takes `shape` as a REQUIRED param. tsc named all
  three call sites when the old params went.
3 TAIL LINE + BUTTON COUNT, the Volume card's form: "then +3,000 held
  through Jun 2028 · 21 months" and "Add Promotion · 24 events". The
  unheld label is unchanged and pinned (session 2's lesson).
4 MOUNTED 50/50, Jon's click order. Grid rows AND emitted rows each
  asserted against the SAME hand-written literal, never one against the
  other — comparing them would pass with both wrong.
5 TRAPS 219-221 CAUGHT. 219's red line is Jon's symptom verbatim:
  [+1,000 +1,000 +1,000]. 209 aged out AGAIN on this refactor and was
  re-anchored — spec:trap-anchors caught it before the gate.
6 THE LAG: PROMO AND VOLUME ARE IDENTICAL, diff 0.00 at every one of 22
  months. The re-banded pool and Contract_Length are EXONERATED. Base
  delta at Jun 2028 is 60,000 = every row strictly before it (T+1 again,
  exactly). +54,000 is APRIL 2028; 57,000 is MAY. Row count is 22, not
  24: the campaign starts at index 2 of a 24-month horizon.
SHED: nothing. Item 1 built and trapped; item 2 measured, no change.
guard-traps: __/__ PENDING
full suite:  __/__ PENDING
Repo: __ PENDING
```

## 0. The base check

```
$ git status --short
                              (empty)
$ git diff a60e3a1 HEAD --stat -- src scripts test-data
                              (empty)
```

## 1. Where the grid and the handler diverged

**The grid, as it stood** (`WhatIfTab.tsx`, the promo spread panel):

```tsx
const pcts = promoSpreadDistType === 'even'
  ? Array.from({ length: promoSpreadMonths }, () => 100 / promoSpreadMonths)
  : promoCustomDist.slice(0, promoSpreadMonths);
const pctTotal = pcts.reduce((s, p) => s + p, 0);
…
{Array.from({ length: promoSpreadMonths }, (_, i) => {
  const fraction = pcts[i] / (pctTotal || 1);
  const vol = Math.round(totalVol * fraction);
```

**The handler**, meanwhile, called `spreadShape({ …, hold: promoHold, horizonMonths })`.

**`promoHold` appears nowhere in the grid.** So the grid rendered the hold-OFF
share split — `+1,000 / +1,000 / +1,000` — while Add emitted
`1,000 / 2,000 / 3,000` and nineteen more months of 3,000. **The chart reads
the events, so the chart was right all along**, which is why the card and the
chart disagreed rather than both being wrong.

**This is the same defect I fixed on the Volume card in REQ-D6-03 session 1 and
did not fix here.** That session's own report says the Volume preview *"used to
re-implement `pct/total` inline, and a preview that recomputes what the save
computes is a preview that can disagree with it"*. The Promotion card had a
third copy and I left it.

The **Volume card's** preview is the shape to match — ramp rows from
`spreadShape`, plus one summarised tail line at `volume-hold-tail`.

## 2. Derive once, read thrice

`buildPromoEvents` **no longer computes a shape.** Its five spread parameters
(`spreadEnabled / spreadMonths / spreadDistType / customDist / horizonMonths`)
are gone, replaced by one:

```ts
  shape: SpreadShapeMonth[];
```

and the body is now `const shape = p.shape;`. **`hold` stays**, because every
emitted row carries it as a restore field.

The card derives it once:

```ts
const promoShape = useMemo(() => spreadShape({
  months: promoSpreadEnabled ? promoSpreadMonths : 1,
  dist: !promoSpreadEnabled ? [100] : …,
  hold: promoHold,
  horizonMonths: horizonMonthsFrom(newPromo.date ?? ''),
}), [promoSpreadEnabled, promoSpreadMonths, promoSpreadDistType, promoCustomDist,
     promoHold, horizonMonthsFrom, newPromo.date]);
```

**Three readers, one derivation**: the preview grid, the Add button's count,
and `buildPromoEvents` via `shape: promoShape`.

**tsc named all three call sites** the moment the old parameters went:

```
src/components/WhatIfTab.tsx(3995,7): error TS2353: Object literal may only
  specify known properties, and 'spreadEnabled' does not exist in type
  'BuildPromoEventsParams'.          (and 5412, 5454)
```

The **row-edit** path takes a named `SINGLE_ROW_SHAPE` — one month, no tail —
so its two `hold: false` decisions read as one.

## 3. The tail line and the button

**The tail is summarised, never listed** — nineteen rows of one repeated figure
is not information:

```
then +3,000 held through Jun 2028 · 21 months
```

at `data-testid="promo-hold-tail"`, the Volume card's form exactly.

**The button reads the row count**, from the same `promoShape`:
`Add Promotion · 24 events`. It said `Add Promotion` while adding twenty-two,
which is what left the three-row grid uncontradicted.

**The unheld label is unchanged byte-for-byte and pinned** — session 2's lesson,
where tidying the churn button's label broke `spec:mix-card`, which finds that
button by its text.

**Percentage drafts show rates**: `raw` is rounded to 2dp rather than to a whole
`3%` the user never typed, matching clause 7's rule that a percentage amount is
a rate.

**Two new keys in six locales**, de and it written rather than left English:
`whatif_add_promotion_n`, `whatif_hold_tail_months`. **892 keys per locale.**

## 4. Mounted — 50/50

`spec:promo-hold-mounted`, driven in **Jon's click order** (amount → ramp →
Hold), now asserts the grid **before** the Add click:

```
(C) the PREVIEW GRID reads +1,000 / +2,000 / +3,000
(C) a tail line is present under Hold ON
(C) it names the held figure and the last month
(C) the button reports 24, not "Add Promotion"
(C) the EMITTED rows are 1000 / 2000 / 3000, then 3000 held
```

**Both sides against the SAME hand-written literal, never one against the
other.** 3,000 over a 3-month even ramp is `3000 × 1/3, × 2/3, × 3/3`; the
campaign starts at `MONTHS[0]` on a 24-month forecast, so the tail is 21 months.
A check that compared the grid to the rows would pass with **both** wrong,
which is precisely the failure being fixed.

*(My first attempt put these after the Add click and read the events table
instead of the preview — 24 values where three were expected. Moved, not
loosened.)*

## 5. Traps

| trap | red line on planting |
|---|---|
| **219** the grid reverts to the hold-off split | `FAIL  (C) the PREVIEW GRID reads +1,000 / +2,000 / +3,000  [+1,000 +1,000 +1,000]` |
| **220** the button stops reporting its count | `FAIL  (C) the button reports 24, not "Add Promotion"  [Add Promotion]` |
| **221** the tail line is not rendered | `FAIL  (C) a tail line is present under Hold ON` (+1) |

**219's red line is Jon's symptom verbatim.**

Planted by hand against a scratchpad backup, restored each time to
`163143f98b974a17ad4a3d32bf65c90e`. **No `git checkout --`, no `git restore`.**

**Trap 209 aged out again** — its anchor quoted the `hold`/`horizonMonths` lines
this refactor deleted from `buildPromoEvents`:

```
FAIL  trap 209 …: anchor 1 still matches WhatIfTab.tsx
      [ZERO — the anchor has aged out; the trap plants nothing]
```

Re-anchored to `const shape = p.shape;`, which it now mutates to drop the held
months — **the same defect at the seam that replaced the old one**, not a
weaker one. Second re-anchor for 209 in two sessions; `spec:trap-anchors`
caught it both times before the gate ran.

## 6. The lag — measured, changed nothing

On the **20:33 save**, through the app's own resolve path
(`buildRestoredLeafIndex` → `resolveFromStore`), a promotion and a Volume-card
inflow event of the **same shape on the same cohort**, run side by side:

```
months 24  2026-07 -> 2028-06 | start idx 2 | horizon from start 22
emitted rows 22 | first3 1000/2000/3000 | last 3000
SUM 63,000 | SUM excluding the last row 60,000
```

| month | row | PROMO baseΔ | VOLUME baseΔ | diff |
|---|---|---|---|---|
| 2026-09 | 1,000 | **0.00** | 0.00 | **0.00** |
| 2026-10 | 2,000 | 1,000.00 | 1,000.00 | 0.00 |
| 2026-11 | 3,000 | 3,000.00 | 3,000.00 | 0.00 |
| … | 3,000 | … | … | **0.00** |
| **2028-04** | 3,000 | **54,000.00** | 54,000.00 | 0.00 |
| **2028-05** | 3,000 | **57,000.00** | 57,000.00 | 0.00 |
| **2028-06** | 3,000 | **60,000.00** | 60,000.00 | 0.00 |

**The two are identical at every one of the 22 months.** The promo re-banded
pool and `Contract_Length` — the brief's two suspects — are **exonerated**: a
promotion's inflow reaches Base exactly as a Volume-card inflow event does.

**The T+1 rule holds exactly, again.** The campaign's own first month reads
`0.00`; the base delta at month *k* is the sum of every row strictly before it;
and at the last month that is `63,000 − 3,000 = 60,000`.

**+54,000 is April 2028** and **57,000 is May 2028** — two months and one month
before Jun 2028 respectively. The row count is **22, not 24**, because the
campaign starts at index 2 of a 24-month horizon (Jul 2026 → Jun 2028).

**This is the same shape as the 1651 session's finding**: the lag is intact and
the reading is displaced by the horizon's start offset. Nothing was changed.

*Caveat:* the resolved aggregate reports `seedBaseKnown false` and
`seedBaseVolume 0`, so Base here is a pure delta over an unknown stock. That
does not affect the deltas — they are differences of two runs over the same
seed.

## Gate

| check | figure |
|---|---|
| `npm run suite` | __ PENDING |
| guard-traps | __ PENDING |
| `spec:promo-hold-mounted` | **50 passed, 0 failed** |
| `spec:trap-anchors` | **229 passed, 0 failed** (217 traps, 224 anchors; next free 222) |
| `spec:i18n-parity` | **200 passed, 0 failed** |
| `spec:i18n-scan` | **PASS** |
| `spec:survival` | **27 passed, 0 failed** — 104 dereferences across 26 files |
| `tsc --noEmit` / `lint` | clean |
| `npm run build` | built in 6.04s |

**i18n keys per locale: 892 each** — two new, de/it written not English.

### Exact counts the brief named

| pin | required | measured |
|---|---|---|
| `runIngest` sites | 3 | __ PENDING |
| last-column, Market / Yield / Pricing | 3 / 2 / 3 | __ PENDING |
| apply sites | 12 | __ PENDING |
| display markers | 6 | __ PENDING |

## What was shed

**Nothing.** Item 1 is built and trapped; item 2 was measured rather than
deferred, and changed nothing.

## Limits

- **The percentage grid rendering is not mounted-asserted.** Case 1 asserts the
  emitted percentage rates; the grid's 2dp rendering for a percentage draft is
  implemented and reasoned but only the absolute grid is pinned by a literal.
- **The tail line's month count comes from `heldRows.length`**, which is the
  emitted tail, so it cannot disagree with the rows — but it is also not
  independently derived, and a wrong shape would produce a consistently wrong
  line.
- **Nothing was run against a browser.** The grid is asserted in JSDOM.
- **§6 is one cohort on one save.** The promo-vs-volume equality is measured on
  Corporate / Mobile Voice with a 24-month horizon; it is not a proof that no
  promotion arm ever diverges — a re-banded Retention promo with a mix or
  pricing arm was not run, and `promoRebanded` was false here.
- **`spec:mix-card` was not re-run against the button-label change** beyond the
  suite, because the unheld label is byte-identical.
