# Working agreement v3.3.15 — assembling the full document

```
FOR ADVISOR
Generated: 2026-09-08 13:08 +0100 (UTC 2026-09-08 12:08)
Certifies: 78e06de (the tree the suite was measured on)
Repo: committed 78e06de, pushed (origin in sync)

BASE 9b58b48 + a4160e2 (reports/ ONLY; ZERO drift in gated paths). Proceeded.
SOURCES: v3.3.13 at 364a590 (md5 c19afc68fe), v3.3.12 at 510eb70 (md5
  726f825cd9). Each marker replaced by the v3.3.12 §N body then the v3.3.13
  §N body, both sliced by line number, neither rewritten.
SIX BLOCKS, TWELVE DIFFS, ALL "identical": §1 17+4 ln, §2 6+4, §3 5+1,
  §4 7+4, §5 3+3, §7 3+1 (v3.3.12 + v3.3.13 lines).
diff supplied-file -> assembled: exactly 6 lines removed, the six markers.
  Nothing else in Jon's text was touched. 152 lines, 38,571 bytes, LF.
NOT DOCS ONLY — ONE LINE IN scripts/: `spec:ai-hold` pins the working
  agreement's FILENAME in its exclusion list. Deleting v3-3-14 turned it RED
  (12/13, 2 identifier hits in the new file). Repointed at v3-3-15; 13/13.
  Named because the brief said docs only and this is the exception.
FILES: docs/…v3-3-15.md added, docs/…v3-3-14.md deleted, ai-hold-spec.ts.
  CLAUDE.md and .claude/ carry NO v3.3.14 reference — grep clean, no edit.
LIMIT: v3.3.12's own "as at v3.3.11 §N" onward references remain as text.
  The brief scoped resolution to v3.3.13 -> v3.3.12; v3.3.11 was not opened.
full suite: 61/61 green
```

## Base

`git log --oneline -1` read `a4160e2` — `9b58b48` plus **two reports-only
commits** (the D5-09 walk report and its 79-column rewrap).
`git diff 9b58b48 --stat -- src/ scripts/ test-data/ package.json` was empty;
the whole delta was `reports/2026-09-08-1202-d5-09-walk-closed.md`. The
permitted case, stated rather than passed over.

## Where the file was

Not in the repo. Jon's copy was in `Downloads/` (25,055 bytes, LF, 94 lines,
six `[INLINE …]` markers plus one prose mention of the notation on line 5).
It was copied into `docs/` by the assembly, never edited in place.

## Sources

```
git log --all --oneline -- 'docs/*v3-3-13*'
  86a0957  Working agreement v3.3.14 in docs/; v3.3.13 removed
  364a590  Commit working agreement v3.3.13, removing v3.3.12   <- read
git log --all --oneline -- 'docs/*v3-3-12*'
  364a590 ; 510eb70  Commit working agreement v3.3.12, removing v3.3.11  <- read
```

`git show 364a590:docs/…v3-3-13.md` — 64 lines, 6,844 bytes, md5
`c19afc68fe7e53d7291006886caf2619`.
`git show 510eb70:docs/…v3-3-12.md` — 89 lines, 15,409 bytes, md5
`726f825cd9d2409d6100e4a922a09038`.

**Every one of v3.3.13's six sections is itself a reference.** §1–§4 and §7 open
"*As at v3.3.12 §N in full*"; §5 says "*Everything through the promotion arc:
CLOSED at `e5f1e79`* — v3.3.12 §5". So each marker resolved to **two** bodies,
which is why the brief's "everything it in turn carries" is the operative
clause and not a formality: resolving only v3.3.13 would have inlined six
pointers.

## The six blocks

Bodies were sliced by line number — from the line after `## N.` to the line
before the next `## ` heading or the trailing `---`, trailing blanks trimmed —
and concatenated **v3.3.12 first, then v3.3.13**, so each section reads base
then additions and v3.3.13's own "*As at v3.3.12 §N in full. Additions:*" lead
-in still points at the text immediately above it. Headings keep their levels;
there are no sub-headings inside these sections.

Verification re-read each block **out of the assembled file** by recorded line
offset and diffed it against the slice taken from git:

| § | v3.3.12 body | v3.3.13 body |
|---|---|---|
| 1 | 17 ln — **identical** | 4 ln — **identical** |
| 2 | 6 ln — **identical**  | 4 ln — **identical** |
| 3 | 5 ln — **identical**  | 1 ln — **identical** |
| 4 | 7 ln — **identical**  | 4 ln — **identical** |
| 5 | 3 ln — **identical**  | 3 ln — **identical** |
| 7 | 3 ln — **identical**  | 1 ln — **identical** |

No marker was left unresolved, so the STOP condition never fired.

**The whole-file check is the stronger one.** `diff` of Jon's supplied file
against the assembled one reports **exactly six deleted lines — the six markers
— and nothing else**. Every character of Jon's own v3.3.15 text is untouched,
and every inlined character came from `git show`. Assembled file: **152 lines,
38,571 bytes**, LF throughout.

## The scripts/ line — the brief's "docs only" has one exception

`scripts/ai-hold-spec.ts:235` excluded the working agreement from the AI-
identifier scan **by exact filename**, because §1's AI-hold paragraph quotes
`@google/genai` and `GEMINI_API_KEY`. Deleting v3-3-14 and adding v3-3-15 moved
the file out from under that pin, and `spec:ai-hold` went **12/13** with two
hits on the new file — measured before the fix, not predicted:

```
FAIL (d) the three AI identifiers appear in ZERO tracked files
     [docs/…v3-3-15.md :: @google/genai | docs/…v3-3-15.md :: GEMINI_API_KEY]
```

The exclusion was repointed at v3-3-15 — a one-character version change on one
line — and the spec returned **13/13**. This is the same instruction the brief
gave for `CLAUDE.md` and `.claude/` ("update it where it names the current
copy"), landing on a file the brief did not expect to be involved. It is named
here rather than absorbed, because "docs only" was an explicit term.

The tracked prompt surface proper needed **no** edit: `grep -rn "v3-3-14\|
v3\.3\.14" CLAUDE.md .claude/` returned nothing. `git grep` over the whole
tracked tree outside `docs/` and `reports/` found exactly the one line above.

## Gate

```
full suite   61/61 green   (serial, one run, exit 0)
spec:ai-hold 12/13 -> 13/13 (the exclusion repointed)
guard-traps  NOT RUN — excluded by the brief
```

## Limits

- **The v3.3.11 chain was not resolved.** v3.3.12's §2, §3, §4 and §7 open with
  their own "*As at v3.3.11 §N in full*", and those sentences are now inlined
  as text. The brief scoped resolution to v3.3.13 → v3.3.12 and that is what
  was done; v3.3.15 is a full document down to v3.3.12 and a pointer below it.
- **guard-traps was not run.** The 61 specs are green on `78e06de`; the 177
  traps were last proven at `d63e67a` and one script line has changed since —
  a filename inside a comment-documented exclusion list, on no trap's anchor
  (`git grep v3-3-14` found no other site).
- **No source behaviour was read.** This session opened the two history copies,
  the supplied document, and `ai-hold-spec.ts` around its exclusion list.
