# D5-08 closed on Jon's walk

```
FOR ADVISOR
Generated: 2026-09-07 16:58 +0100 (UTC 2026-09-07 15:58)
Certifies: 7118e5b (the tree the suite and survival were measured on)
Repo: committed 7118e5b, pushed (origin in sync)

BASE 8c0cb71 + 5aa117b (reports/ only; ZERO drift in src/ scripts/ test-data/
  package.json). Proceeded — the extra commit is last session's own report.
DOCS ONLY. EXPECTED.md's D5-08 entry, appended. Nothing else in the file.
No source, no scripts, no package.json. guard-traps NOT run.
APPENDED VERBATIM, as its own bold closing paragraph at the end of the D5-08
entry, matching D5-07's form, immediately before the "D5-07 DECIDED" heading:
  "CLOSED 8c0cb71; walked by Jon 2026-09-07 on the 07 Sep 09:13 save (ten
   rows): Show all removes the cap and the page scrolls, Show fewer restores
   it, the collapse chevron is unaffected, de/it labels render, Compare shows
   no control."
DIFF: test-data/EXPECTED.md, 5 insertions, 0 deletions. Nothing else staged.
full suite: 61/61 green — unchanged.
survival: 104/26 dereferences/files; 27/27 checks.
```

## The append

`test-data/EXPECTED.md`, at the end of the D5-08 entry recorded at `c9e3da4`,
placed immediately before the `#### D5-07 DECIDED` heading so it closes the
entry it belongs to rather than opening the next one:

```diff
 exact but untestable one; the imprecision is that a very narrow viewport could
 wrap cells and overflow below the threshold.

+**CLOSED `8c0cb71`; walked by Jon 2026-09-07 on the 07 Sep 09:13 save (ten
+rows): Show all removes the cap and the page scrolls, Show fewer restores it,
+the collapse chevron is unaffected, de/it labels render, Compare shows no
+control.**
+
 #### D5-07 DECIDED (Jon, 2026-09-07) — event month markers render on all three…
```

Wording is the brief's sentence exactly, wrapped to the file's column and
bolded to match how D5-07 carries its closure two entries below. Five
insertions, zero deletions; `git diff --stat` confirms `EXPECTED.md` is the
only file touched.

**What the walk closes that the build could not.** The D5-08 report recorded
three limits, and the walk answers all three:

- **"Nothing was measured in a browser."** The build asserted the cap's removal
  by the container's *class*, never by observing that the page scrolls instead
  of the panel. Jon's walk observes exactly that.
- **"Compare is unchanged and untested for it."** The opt-in prop defaults
  false, so `ScenarioCompareTab` cannot render the control — but no spec
  asserts that absence. The walk confirms it by eye.
- **The de/it labels.** `i18n-parity` proves the keys exist in all six locales
  and `i18n-scan` proves no literal was left hardcoded; neither proves the
  strings *render*, on the control, in those languages. The walk does.

The collapse-chevron half was covered by the mounted spec (it asserts the
collapse control is still open after the height toggle), so the walk confirms
rather than closes that one.

## Gate

`npm run suite` once, serial. No `guard-traps` — nothing in this session
touches source, scripts or `package.json`.

```
full suite   61/61 green   (unchanged from 8c0cb71)
survival     104/26 dereferences/files; 27/27 checks
```

Both survival forms are reported because they are different measurements and
have been conflated before: **104/26** is first-row dereferences across files,
**27/27** is the spec's own check count.

The suite figure not moving is the evidence this was docs-only.

## Limits

- **Nothing was re-measured about the walk.** Jon's observation is recorded,
  not reproduced — this session mounted nothing and did not open the 07 Sep
  09:13 save.
- **`guard-traps` was not run**, so this report certifies no trap count. The
  last gated figure remains 172/172 at `8c0cb71`.
- **"Compare shows no control" still has no automated cover.** The walk closed
  it by eye; the gap named in the D5-08 report is unchanged, and a spec
  asserting that absence would be the durable fix.
