# Three designs out of the transcript — and the budget clause applied to itself

## FOR ADVISOR

```
Generated: 2026-08-12 11:56 +0100 (UTC 2026-08-12 10:56)
Certifies: records only — ZERO source files changed. HEAD at start dae586d.
Repo: committed on main and pushed (origin in sync) — hash in section 0.
THE BUILD IS HELD, and that is the budget clause applied on the first session
  after it was written: a new YieldEvent carrier, its round trip, a four-state
  per-tier column, a mounted spec, a counted writer pin and a three-stage gate
  would have left nothing for the close. Shed SCOPE, not the record. The records
  task the brief put FIRST is done in full.
THE BRIEF TRUNCATES mid-item-2, at "extended never duplicated; round trip".
  Items 3 onward — specs, traps, gate — never arrived. Reissue it complete.
RECORDED 1: Request 2 = reading (b), Jon 2026-08-11. Per-tier Base ARPU
  editable; the blend stays DERIVED. Provenance: the request was "the Base ARPU
  used", i.e. the inputs. (a) rejected — two-boxes-one-relationship, and a
  rebuild of the on-hold mix-target extension. DESIGNED, NOT BUILT.
RECORDED 2: comparator = option (c), the honest label stays. (a) rejected. (b)
  QUEUED as its own gated session, explicitly MOVES FIGURES. Its scope is FIVE
  equal-weight sites, not four — the fifth is computeTierData's scaling branch —
  plus engine-side ?? 0 sites, a persisted baseline-shares carrier on
  YieldEvent, and a migration story.
RECORDED 3: Request 3's four decisions (Jon 2026-08-12) are out of the chat and
  into the file. Decision 3 is NEW GROUND and flagged: an override that RESOLVES
  absence lifts a save refusal — the first time supplying a number changes what
  the app permits, not merely what it shows.
ENUMERATION RE-VERIFIED at dae586d, not quoted: 1 construction site, 1 caller,
  1 import route. PREMISE CORRECTED in the file: there is no Request 2 merge.
```

---

## 0. What this session did, and what it deliberately did not

The brief asked for a records task **first** and then the Request 2 build. **The
records task is done in full. The build is held.**

That is the close-ritual budget clause being applied on the first session after
it was written:

> If the close ritual cannot be afforded, the last increment taken on could not
> be afforded either. A session running short sheds SCOPE, never the record.

Taking the build would have meant a new `YieldEvent` carrier, its round trip, a
per-tier column with four display states, a mounted spec with transitions, a
counted writer pin with a trap, and a three-stage gate — with no budget left for
the close. That is the shape of every gap the clause was written about. Holding
it was not caution; it was the rule.

**The brief also truncates mid-item-2** — at *"carriers presence-based via
readOptionalNumber, extended never duplicated; round trip"*. Items 3 onward
(specs, traps, gate) never arrived. The build session should reissue it complete.

## 1. Records written — all three

### Request 2: reading (b), settled, not built

**Jon, 2026-08-11.** The per-tier **Base ARPU** column becomes editable; the
blended ARPU stays derived and read-only. Provenance recorded: the original
request was *"adjusting the Base ARPU used"* — the **inputs**, not the blend.
Reading (a) is rejected on the two grounds already in the file — the
two-boxes-one-relationship rule, and that it is a rebuild of the on-hold
mix-target extension.

### The comparator: option (c), with (b) queued and scoped

The equal-weight **"Approximate" label stays and stays honest**. Option (a)
rejected. **Option (b) queued as its own gated session**, recorded explicitly as
a forecast-behaviour change that **moves figures**, so it cannot ride along with
a UI feature.

Its inherited scope is recorded as **five** equal-weight sites, not the four the
comparator question named — the fifth being `computeTierData`'s forecast-scaling
branch, which derives its scale factor the same way and sets the tier ARPUs the
card displays. Plus the engine-side `?? 0` sites, a persisted baseline-shares
carrier on `YieldEvent`, and a migration story for events saved without one.

### Request 3: four decisions, queued as design

All four of Jon's 2026-08-12 decisions are now in EXPECTED.md rather than a chat
window: pre-population from each band's trailing average with the source named;
overrides **persist** and round-trip; an override **resolves absence** — lifting
the absent-blend save refusal; and provenance visible **at a glance**.

**Decision 3 is new ground and is flagged as such.** Requests 1 and 2 make a
value editable. Request 3 makes an override *unlock a refusal* — the first time
in this arc that supplying a number changes what the app will let you do.

Two things the build session should not rediscover are recorded with it: the
engine needs no change (`perMemberArpus` is already an input), and the check
that matters is that a band edit moves the **Reachable range** live, not just
the blend — two surfaces disagreeing about one number being the defect shape
this arc keeps turning up.

## 2. The enumeration, re-verified rather than quoted

The diagnosis report's counts were re-measured at `dae586d` and **still hold**:

| thing | count |
|---|---|
| construction site building `tariffBaseArpu` from the draft | 1 |
| `addYieldEvent` callers | 1 |
| yield import routes | 1 |

The `Tariff_Base_ARPU_JSON` grep returns 2 in `App.tsx`; one is the export
writer, one the import reader. **One import route, not two** — unlike market
events, where the second route is exactly what the promo fields fell through.

## 3. A premise correction, recorded not just said

The brief carrying Request 3's decisions described it as closing the arc and
assumed a Request 2 merge. **There is no such merge.** Request 3 is the third of
four; Request 2 is now designed and still unbuilt. That correction is in
EXPECTED.md beside the decisions, so the next brief inherits the right state
rather than the transcript's.

## Scope and verification

**Zero source files changed** — `git diff --name-only | grep '^src/'` returns
**0**. This session touched `test-data/EXPECTED.md` and this report.

No figure can have moved and none was re-measured; that is the basis stated
rather than a re-run claimed. No gate was run, because there is no behaviour to
gate.

## Where things stand

Request 2: **designed, not built** — and now buildable from the file alone.
Request 3: **designed, not built**, premise corrected.
Option (b): **queued and scoped**, its own gate, moves figures.
Request 1 and the negative-ARPU rider: shipped and reported.
