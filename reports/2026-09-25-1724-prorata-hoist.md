# REQ-D7-04 session 1 — the pro-rata leaf scans hoisted

## FOR ADVISOR

```
Generated: 2026-09-25 19:17 +0100 (UTC 2026-09-25 18:17)
Certifies: 3d29479758df7e4d442fe54226d97529475929a6
Repo: committed 3d29479, pushed (origin in sync)
guard-traps targeted 127/127 CAUGHT, rotation 20, NOT RUN 164 (line s.7)
full suite:  81/81 green (first run; spec:prorata-hoist is the 81st)
Decisions: D7-04 cl.5-6 0b7f0d8 alone; D7-03 cl.6 9e6c989 alone. Skel 2cbe5b1
CONFLICT (reported): the mid-session ITEM 0 asked ONE commit for both; D7-04
  5-6 was already committed+pushed - not rewritten, not duplicated (s.1)
NEVER-SHED MET: weights and outputs IDENTICAL; 60 runs 0.015 s (was 3.26 s)
ONE builder buildProRataLeaves (forecasting.ts); override per metric, keyed
  Inflow/Outflow/Retention; engine falls back to the SAME builder if absent
Seam REQUIRES the lists; WhatIfTab + FVA memo them once on (data, columns)
Identity: 3 lists deep-equal 379c48b's transcribed buildLeaves; 4 runs'
  series/rawArpu/adjustedMonths/arpuIds hash-equal 379c48b's (s.3)
Discriminator: with lists passed the seam reads the rows ZERO times
One build of the three lists: 57.8 ms, once per dataset
Fallback callers unchanged: Step 2's main run, churnScopeSeries,
  changeSummary before/after (WhatIfTab 3692, 4285, 5680, 5683)
Traps 297-298 new, seen RED by hand; re-aims seen red: 4 specs that passed
  ONE list (aggregate-reconciliation, applied-count, arpu-companion, scen-arpu)
No new i18n keys (946 per locale). Counts s.4. Shed: nothing.
Hold: none. Next: session 2 (per-leaf scoring, built as-is).
```

Written after the build was code-complete and BEFORE the suite and
guard-traps, per the skeleton rule; the gate figures were filled after build
commit 3d29479 was pushed. Nothing else changed between the two.

## 1. Base, ITEM 0, and the conflict

- Skeleton `2cbe5b1` was the first repo action; then `git status --short`:
  empty; `git diff 379c48b HEAD --stat -- src`: empty; `-- scripts`:
  `scripts/guard-traps-ledger.json` only.
- ITEM 0 as briefed: REQ-D7-04 clauses 5-6 appended and committed ALONE as
  `0b7f0d8`, pushed.
- **Mid-session, a second ITEM 0 arrived**: "two headings, ONE commit" — REQ-D7-03
  clause 6 (0746 finding 1, option a: the out-of-view row click moves the view
  sideways, Back returning it) plus REQ-D7-04 clauses 5 and 6 "as written
  below". **Conflict, reported rather than resolved:** D7-04 5-6 were already
  committed and pushed, verbatim from the brief, before it arrived; the message
  carried no clause text of its own below. Folding both into ONE commit would
  mean rewriting a pushed commit, which is not mine to do unasked. So REQ-D7-03
  clause 6 was committed ALONE as `9e6c989` (EXPECTED.md only, pushed), and
  D7-04 5-6 were not appended a second time. If a single commit matters, that is
  your call.

## 2. What was built

- **ONE builder**, `buildProRataLeaves(data, cols)` in `src/utils/forecasting.ts`
  beside `ProRataLeaf`: computeAdjustedForecast's `buildLeaves` closure moved
  **verbatim**, returning `{ Inflow, Outflow, Retention }` — **the engine's own
  metric keys** (`LeafMetric`, which already indexes `leavesByMetric`). Type
  `ProRataLeavesByMetric`; columns `ProRataLeafColumns`.
- **The override is per metric.** `AdjustedForecastInput.proRataLeavesOverride`
  becomes `ProRataLeavesByMetric` (it was ONE list spread across all three). The
  engine uses it when given, else calls **the same builder** on `data` — so the
  fallback is today's behaviour by construction. All-or-nothing: no per-metric
  fallback.
- **The callers outside the seam are unchanged** and keep the fallback: Step 2's
  main adjusted run (WhatIfTab 3692), `churnScopeSeries` (4285), and
  `changeSummary`'s before/after pair (5680, 5683).
- **The seam takes the lists** — `proRataLeaves` is a REQUIRED input of
  `eventScopeSeries` (a caller that forgot would silently pay three scans a
  run), passed to the engine as `proRataLeavesOverride` on every run.
- **ONE memo each**: WhatIfTab (`const proRataLeaves = useMemo(() =>
  buildProRataLeaves(data, {…}), [data, …columns])`, handed to the wrapper, which
  now lists it as a dependency) and FVA (the same, handed to `viewRun`).

## 3. Mounted — `spec:prorata-hoist` (NEW) 34/34, fill-in order

- **(a) IDENTITY — the lists.** The builder's three lists deep-equal those of
  379c48b's `buildLeaves`, **transcribed verbatim** into the spec from
  379c48b's WhatIfTab.tsx:1460-1483 (the extract md5 `fe5fd75f…`) and run over the
  same rows: Inflow, Outflow, Retention — 540 leaves each — and exactly those three
  keys. **How it reads them:** the lists the engine built inside itself at 379c48b
  were never exposed, so the spec reads the same code rather than the engine's
  internals; it exposes nothing new.
- **(a) IDENTITY — the outputs.** `series`, `rawArpuByMonth`, `adjustedMonths`
  and `arpuIdsByMonth` for four runs — All/All, Corporate/Direct, one 7-part leaf
  (`Corporate|Fixed Connectivity|High Value|Direct|Call Centre /
  Tele-sales|RED ULTD|SIM-only`), and that leaf with an Outflow event too — equal
  379c48b's: SHA-256 of each field's JSON, **measured on the untouched 379c48b
  source** (`git diff --stat 379c48b -- src` empty) before any code, and recorded
  in the spec. The before is therefore 379c48b's own path, not a transcription of
  the engine. (All/All and Corporate/Direct hash alike on this fixture — they
  resolve the same 60 leaves; the leaf runs are where the weights bite: Inflow
  at the first month 152.87; the Outflow event moves the leaf's outflow
  139.20 → 136.71, so the Outflow list is exercised.)
- **(a) THE DISCRIMINATOR.** The rows handed to the seam count their own
  iterations. The engine's only read of `data` is the pro-rata fallback, so with
  the lists passed the four runs read the rows **0 times**. Its control: with no
  lists, the fallback reads them **3 times** (one per metric).
- **(b) TIMING.** 60 per-leaf runs at Corporate/Direct with the hoisted lists:
  **0.015 s** (per run median 0.22 ms, min 0.14, max 1.15) — against 3.26 s in
  the 1339 measure. PASS (< 2 s). None read the rows. One build of the three
  lists: 57.8 ms, once per dataset.
- **(c) THE FIVE WHATIFTAB CALLERS' SPECS**, green with figures unchanged:
  pricing-roundtrip 150/150; yield-roundtrip 81/81; view-apply 208/208 (leaf
  retention 1.36, All retention 1.11, mix% leaf inflow 0.82, promo dilution leaf
  inflow −0.56, yield T+1 leaf base −0.09 / T+2 −0.21); mix-card 230/230
  (pricedVol 26208.05, totalVol 10029051.17, baseline ARPU 24.00 at either
  loaded scope); promo-cohort-target 39/39 (delivers 15.03, lead 16.91, blend
  33.13, 14.23 at 300 subscribers, 15.86 at −20%). Each of these is pinned in a
  passing check, so a weight change would have moved it red.
- **(X)** one definition; no `const buildLeaves` left in the engine; the fallback
  calls the builder; WhatIfTab 2 calls (memo + fallback), FVA 1, the seam 0; the
  seam passes the lists and requires them; both callers hand them;
  computeAdjustedForecast 6.

## 4. Exact counts

| count | measured |
|---|---|
| buildProRataLeaves definitions | **1** — forecasting.ts:3537 |
| buildProRataLeaves calls | 3 — WhatIfTab 1467 (the engine's fallback), WhatIfTab 3815 (the wrapper's memo), FVA 2081 (Step 3's memo) |
| computeAdjustedForecast | **6** — WhatIfTab 1407 (definition), 3692, 4285, 5680, 5683; the seam 110 |
| eventScopeSeriesFor callers | **6** — 5 `eventScopeSeriesFor(` in WhatIfTab through the wrapper (3875), plus FVA 2088 |
| the rest as 0746 | unchanged from 379c48b, raw token counts across src: handleViewFilterChange 4 (def + bar + FVA prop + a comment; callers 3), `onViewChange(next)` 1, `onViewChange?.(from)` 1, `resolveForecast(` 7, coveredLeafKeys( 5, deriveAggregate( 3, accuracy-month-select 1, solveForCohortTarget 4, buildPromoEvents 12, `resetYieldDraft()` 2, handleDeleteCampaign 5, setPendingChange 10, campaignToggleState 4, carryInitiative 5, `{ initiative }` 3, initiativeKey 8, handleSetEventEnabled 11, initiativeGroups 4, handleSetInitiative 4, handleDeleteInitiative 2, `isPlaceholderSheet(` 11, `adjustedForecastSheetRows(` 2, viewMetaRow( 2, viewFromMeta( 2 |

## 5. Re-aims — each seen RED first

The four specs that inject their own leaves passed ONE list (`as any`, so tsc
did not see them). All four crashed:
`TypeError: Cannot read properties of undefined (reading 'forEach')`.
Re-aimed to pass the same list for each metric — the old engine spread one list
across all three, so the weights are unchanged:

| spec | after |
|---|---|
| aggregate-reconciliation (`DATA_LEAVES`) | 34/34 |
| applied-count (`LEAVES`) | 19/19 |
| arpu-companion (`LEAVES`) | 19/19 |
| scenario-arpu (`[]`, three sites — the pre-pro-rata wildcard) | 84/84 |

## 6. Traps

Planted by hand, backed up to the scratchpad, md5'd before / planted / after,
restored FROM THE BACKUP, all identical; `git status` identical before and after.

| trap | file | before -> planted -> after | red |
|---|---|---|---|
| 297 the builder drops one metric's list | forecasting.ts | 5f8687b6 -> 11a8622f -> 5f8687b6 | prorata-hoist 31/34: `(a) the builder's Retention list deep-equals 379c48b's (540 leaves) [absent vs 540]`, `(a) exactly the three metric lists, nothing else [["Inflow","Outflow"]]` |
| 298 the seam rebuilds the lists per run | eventScopeSeries.ts | acb7748f -> 577e7aed -> acb7748f | prorata-hoist 30/34: `(b) 60 per-leaf runs under 2 s [3.559 s]`, `(a) DISCRIMINATOR: … ZERO times (no fallback ran) [12 iterations]` — identity GREEN, as the brief expected |

**297's discriminator.** The engine does not fall back per metric (the override
is all-or-nothing), so a dropped list would not be silently rebuilt — it would
surface only when an event of that metric runs, and an Inflow-only event set
would pass output identity. The discriminator is the identity of the three
LISTS against 379c48b's transcription, plus the row-read count.

`PRORATAHOIST` registered in CONTROL_SPEC_MAP with its first trap.
spec:trap-anchors **311/311** (291 traps, 306 anchors; next free id 299). No
existing anchor aged out. TARGETS unchanged (24).

## 7. Gate (serial)

| check | result |
|---|---|
| `npm run suite` | **81/81 green**, first run |
| guard-traps `-- --targeted` (to a file) | **127/127 CAUGHT**, verbatim below |
| spec:trap-anchors | 311/311 |
| spec:i18n-parity | 203/203; **no new keys** — 946 in each of en/de/es/fr/it/pt, locales untouched |
| spec:i18n-scan | PASS |
| spec:survival | 27/27; **103** first-row dereferences across **26** files |
| tsc | exit 0 |
| lint | exit 0 |
| build | exit 0 |

All 24 harness TARGETS were backed up to `scratchpad/pre-gt65/` and md5'd
before the run; all 24 are identical after it, and `git status` differs only
by the ledger. The ledger is committed WITH this report; the build commit
`3d29479` excludes it.

```
guard-traps targeted 127/127 CAUGHT (ids 10 11 12 15 21 22 36 37 48 49 50 51 56 57 58 63 64 65 67 68 69 70 71 74 75 76 77 78 79 80 81 87 88 89 90 94 102 103 104 105 106 107 108 109 110 111 112 113 114 115 116 117 118 119 120 124 125 126 127 132 133 192 193 194 197 245 252 253 254 255 261 262 263 264 265 266 267 268 269 270 271 272 273 274 275 276 277 278 279 280 281 282 283 284 286 287 288 289 290 291 292 293 294 295 296 297 298), rotation 20 (ids 143 144 145 146 147 148 149 150 151 152 153 154 155 156 157 158 159 160 161 162), NOT RUN 164, last FULL run 8079186 2026-09-17T10:58:18.253Z
```
