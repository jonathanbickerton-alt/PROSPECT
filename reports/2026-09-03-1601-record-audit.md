# Working agreement v3.3.10; settled-decisions record audit

## FOR ADVISOR

```
Generated: 2026-09-03 16:01 +0100 (UTC 2026-09-03 15:01)
Verified against: HEAD 0b72574, branch main, tree CLEAN — NO source changed.
Repo: committed 0b72574, pushed (origin in sync)
Last gated commit: 2ecdefb. No gate — docs and EXPECTED.md only.
ITEM 0: v3.3.10 at 1d723ff, byte-identical (22,470 B, sha1 8dac6ea3…), v3.3.9
  removed in the same commit. ALL FOUR CLAIMS MATCH: 11/4 predicate callers;
  2 callers of pricingBaselineArpu; 3 readers of appliedEventIds; and ZERO
  readers of the blended column — a naive grep gives 6, of which 3 are the
  per-scenario columns sharing the suffix, 1 a comment, 1 a per-scenario read.
AUDIT: 28 items checked, NONE count 9 — eight recovered and written, one
  UNRECOVERABLE. THE NEVER-RE-PROPOSE LIST WAS NOT IN THE REPO AT ALL: the
  document stopped enumerating it at v3.3.7 and has cited it by bare label for
  FOUR versions since; CLAUDE.md and the agents have no copy. Recovered from
  v3.3.1 — NOT v3.3.8, which defers to v3.3.6, which abbreviates v3.3.5.
  Five of its eight items have no entry beyond the list line itself.
ALSO WRITTEN: the Δ-column direction-of-effect decision (option a, 2026-08-21)
  from v3.3.6; Q1 and Q2 of 2026-09-02 from v3.3.8. Q5 maps to the R5
  decisions, Q6 to the locale entry, so neither needed an entry.
UNRECOVERABLE (1): the nearest-populated-ancestor fallback. "ancestor" appears
  in NO working-agreement version; EXPECTED.md's two hits describe aggregate
  enumeration. Recorded with the versions searched and an explicit warning not
  to read the hasL2 entry's "filter-scoped fallback" as the missing text.
  If it is real, Jon holds the only copy, outside the repo.
LIST COPIES IN REPO: 1, and this session created it. v3.3.4 is absent from git
  history entirely (3.3.3 -> 3.3.5), so anything only there was never in repo.
```

## Base check

HEAD `638bfbb` (this session's skeleton) on `main`, tree clean at entry.
`git diff --stat 2ecdefb..HEAD -- src/ scripts/ test-data/ package.json` EMPTY.
The STOP did not fire. **No source changed in this session**; last gated commit
`2ecdefb`, no gate run.

## Item 0 — v3.3.10 committed, v3.3.9 removed; four claims checked

Committed at `1d723ff`, both operations in the one commit. Byte-identical to the
supplied file — 22,470 bytes, sha1 `8dac6ea3411ebc9b8a982dd5d1222f2338dc9f45` —
content unedited.

**All four claims MATCH. No mismatch to report.**

| claim | measured | verdict |
|---|---|---|
| 11 pinned callers of `eventScopeMatchesView` in `WhatIfTab`, 4 in `scenarioHelper` | 11 / 4 (comments stripped) | match |
| exactly two callers of `pricingBaselineArpu` | 2 | match |
| zero readers of the blended `ARPU (Adjusted)` other than its writer | 1 site, `WhatIfTab.tsx:1377`, and it is the write | match |
| three readers of `appliedEventIds` | `App.tsx:558`, `WhatIfTab.tsx:1652`, `:3980` | match |

**The third needed a precise test rather than a grep.** A naive search for
`ARPU (Adjusted)` returns six hits, of which three are the PER-SCENARIO columns
that share the suffix (`${label} ARPU (Adjusted)`), one is a comment, and one is
the per-scenario reader inside `pricingBaselineArpu`. Matching only the bare
column — as a bracketed key or an object key — leaves exactly one site, the
writer. Counting the six would have made the document look wrong when it is
right.

## Item 1 — the audit table

| item | EXPECTED.md heading | date | line |
|---|---|---|---|
| bottom-up derivation via `resolveForecast` at read time | `SETTLED: aggregates are derived at READ time` | 2026-08-04 | 1629 |
| three event carriers | **NONE** → written (Item 2) | — | — |
| pipeline order (market → yield → pricing) | `R4 DECISIONS (Jon, 2026-08-14) — the combined events summary table` | 2026-08-14 | 7597 |
| R4 | `R4 DECISIONS (Jon, 2026-08-14)` | 2026-08-14 | 7597 |
| R5 | `R5 DECISIONS (Jon, 2026-08-14) — retention dilution on the Pricing card` | 2026-08-14 | 6692 |
| Scenario Compare arc | `ORDER OF WORK: THE SCENARIO COMPARE ARC, THEN DQ (Jon, 2026-08-19)` | 2026-08-19 | 7354 |
| R7 churn mode | `R7 — CHURN-TARGETED OUTFLOW: SIX DECISIONS (Jon, 2026-08-20)` | 2026-08-20 | 6849 |
| D5-revised | `D5-REVISED — CHURN CAMPAIGNS GAIN GROUP EDIT (Jon, 2026-08-21)` | 2026-08-21 | 6891 |
| Δ-column direction-of-effect option (a) | **NONE** → written (Item 2) | — | — |
| pricing baseline scope (`resolveEventScopeForecast`) | `PRICING BASELINE SCOPE (Jon, 2026-08-21) — the fix` | 2026-08-21 | 7496 |
| chart-grid decision set | `THE CHART SCENARIO x MEASURE GRID (Jon, 2026-09-01) — UAT D1-01 / D1-02` | 2026-09-01 | 6927 |
| Q1 — held tail, hold/revert | **NONE** → written (Item 2) | — | — |
| Q2 — points, confirmed as built | **NONE** → written (Item 2) | — | — |
| Q3 — pricing baseline option (b) | `Q3 / Q4 — THE PRICING BASELINE AND THE BLEND CONSUMERS GO PER-SCENARIO` | 2026-09-02 (recorded 09-03) | 7096 |
| Q4 — blend consumers per-scenario | same entry | 2026-09-02 | 7096 |
| Q5 — pricing compounding multiplicative | `R5 DECISIONS (Jon, 2026-08-14)` — the compounding option is decision 2 there | 2026-08-14 | 6692 |
| Q6 — German and Italian are core | `LOCALE PARITY — de and it are CORE UAT LANGUAGES (Jon, 2026-09-02)` | 2026-09-02 | 7175 |
| locale decision set | same entry | 2026-09-02 | 7175 |
| **never re-propose:** top-down disaggregation | **NONE** → written (Item 2) | — | — |
| **never re-propose:** independent fitting per level | **NONE** → written (Item 2) | — | — |
| **never re-propose:** short-leaf-threshold hybrids | **NONE** → written (Item 2) | — | — |
| **never re-propose:** hasL2+tariff extension | `Extending \`hasL2\` to tariff is still the wrong fix` | undated | 8182 |
| **never re-propose:** percentage events on Custom Promotion | `Percentage on the Promotion card — declined, and the reason is the resolution model, not the interaction count` | 2026-08-02 | 4609 |
| **never re-propose:** a judge agent above the gate | **NONE** → written (Item 2) | — | — |
| **never re-propose:** goal-seek-as-recommendation | `SETTLED: constrained mix mode, not input-vs-target` | undated | 7702 |
| **never re-propose:** write-time aggregation | `Why write-time was rejected — REJECTED, do not re-propose` | 2026-08-04 | 1637 |
| **never re-propose:** nearest-populated-ancestor fallback | **NONE — UNRECOVERABLE** (Item 2) | — | — |

**NONE count: 9.** Eight were recovered and written; one is unrecoverable.

Two entries carry no date in their heading (`hasL2`, `goal-seek`). They are
recorded as found rather than dated by inference.

## Item 2 — entries written for every NONE

Four new entries in `test-data/EXPECTED.md`, each naming its source version:

| heading | line | source |
|---|---|---|
| `RECORDED FROM WORKING AGREEMENT v3.3.1 (audit 2026-09-03) — the never-re-propose list` | 7068 | v3.3.1, verbatim |
| `RECORDED FROM WORKING AGREEMENT v3.3.6 (audit 2026-09-03) — the Δ-column direction-of-effect decision` | 7106 | v3.3.6, verbatim |
| `RECORDED FROM WORKING AGREEMENT v3.3.8 (audit 2026-09-03) — Q1, Q2, and the three carriers` | 7125 | v3.3.8, verbatim |
| `UNRECOVERABLE — the nearest-populated-ancestor fallback (audit 2026-09-03)` | 7148 | — |

**The list was recovered from v3.3.1, not v3.3.8.** v3.3.8 does not carry it.
The brief's default source had to be walked back through the version chain:
v3.3.8 §3 says "Everything in v3.3.6 §3 stands", v3.3.6 restates it shorter than
v3.3.5, and v3.3.1 holds the fullest form — including the parenthetical
"(the most re-proposed)" against top-down disaggregation and the reason attached
to "summing leaf Base means". The fullest form is the one recorded.

**One truncation is reported rather than reconstructed.** The v3.3.8 carriers
line is cut off mid-sentence in the recovered source. What survives is quoted;
the remainder is not guessed at.

### UNRECOVERABLE — nearest-populated-ancestor fallback

Searched: working agreement v3.3.1, v3.3.2, v3.3.3, v3.3.5, v3.3.6, v3.3.7,
v3.3.8, v3.3.9, v3.3.10, and `EXPECTED.md`. **The string "ancestor" appears in
no working-agreement version at all.** Its two occurrences in `EXPECTED.md`
(roll-up index construction; bulk-run classification) describe how aggregates
are enumerated and are not a rejected proposal.

Recorded as UNRECOVERABLE rather than paraphrased from memory. The nearest
related text — *"the defect is the existence of a filter-scoped fallback, not
which cohorts reach it"*, under the `hasL2` entry — concerns a different
mechanism, and the entry says so explicitly so a later reader cannot mistake one
for the other.

**If the item is real, its wording exists only in the advisor's project
instructions, outside the repo.**

## Item 3 — where the never-re-propose list lives

**In the repo: ONE copy, and this session created it.**

| location | before this session | after |
|---|---|---|
| `test-data/EXPECTED.md` | not enumerated | **enumerated**, with the heading map |
| `CLAUDE.md` | 0 matches | unchanged |
| `.claude/agents/` (six definitions) | 0 matches | unchanged |
| `docs/…v3-3-10.md` | references it by name 3×, enumerates nothing | unchanged |

**The document stopped enumerating it at v3.3.7.** Measured across every version
in git history:

```
v3.3.1  ENUMERATES        v3.3.6  ENUMERATES
v3.3.2  ENUMERATES        v3.3.7  does NOT
v3.3.3  ENUMERATES        v3.3.8  does NOT
v3.3.5  ENUMERATES        v3.3.9  does NOT
                          v3.3.10 does NOT
```

So for **four consecutive versions** the working agreement has cited a list that
no file in the repo contained. v3.3.10's own §4 states the rule — *"a rejection
carries its date and reason, and the never-re-propose list cites the entry; a
bare label sends everyone looking under the wrong word"* — while §3 and §6 cite
the list by bare label. That is not a criticism of the document; it is the
condition the document was written to end, and this entry is what ends it.

**Nothing lives only outside the repo except the unrecoverable item.** The list
itself is now in `EXPECTED.md`; Jon holds the only copy of the
nearest-populated-ancestor wording, if it exists.

## Limits of this check

- **The audit matched headings, not content.** An item with a heading is
  recorded as present; whether that entry still describes the build was not
  re-verified. Two entries (`hasL2`, `goal-seek`) carry no date.
- **Q5 is mapped to the R5 decisions**, where compounding is decision 2 of
  2026-08-14. Jon's 2026-09-02 "Q5 confirmed" adds no new wording, so no entry
  was written; if the confirmation was meant to change something, that is not
  visible here.
- **Recovery is limited to versions committed to `docs/`.** v3.3.4 does not
  appear in git history at all — the sequence runs 3.3.3 → 3.3.5 — so if the
  missing wording lived only there, it was never in the repo.
- **Item 0 checked four claims**, not the document as a whole.
- No gate: no source changed.
