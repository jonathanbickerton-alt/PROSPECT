# v3.3.15 — the two loose ends closed

```
FOR ADVISOR
Generated: 2026-09-08 14:17 +0100 (UTC 2026-09-08 13:17)
Certifies: 836fb60 (the tree the suite was measured on)
Repo: committed 836fb60, pushed (origin in sync)

BASE d7e863c + 03a405f (reports/ ONLY; ZERO drift in gated paths). Proceeded.
EDIT 1 — annotation added ONCE at each citation: line 237 (§5, after the
  bold "full record in v3.3.4 §5:" lead-in) and line 309 (§7, after the
  italic block opening "Unchanged from v3.3.4"). Their own words unchanged.
EDIT 2 — bullet's "— as at v3.3.11." -> ":"; THREE paragraphs inlined from
  v3.3.11 §1 (L20 chart + four-delta card, L21 pricing baseline, L22 R7),
  indented two spaces. ALL THREE DIFF "identical" vs the git slice.
STOPPED ON ONE TOPIC, AS INSTRUCTED: **the ARPU companion has NO paragraph in
  v3.3.11 §1**, and v3.3.11 §1 holds no pointer to follow. Its name stays in
  the bullet as text; no text was written for it. FOUND, NOT USED: v3.3.10 §1
  has it ("a rate in both amount modes; own mode queued"). v3.3.11 DROPPED it
  from §1 and keeps the topic in §6's queue — already in this document. The
  gap looks deliberate. ONE LINE FROM JON: inline v3.3.10's paragraph, or
  strike the name from the bullet?
WHOLE-FILE DIFF vs d7e863c: 6 insertions, 3 deletions — the two citation
  lines, the bullet, its three new paragraph lines. Nothing else moved.
"as at v3.3.11" REMAINING 2: L70 (UAT log) is a true back-reference — L63
  carries D3-01 to D4-03. L23 (mix controls) is NOT: it enumerates its own
  content and its subject is also at L274. Left, per brief.
FINAL FILE: 339 lines, 82,663 bytes. Suite 61/61; spec:ai-hold 13/13.
```

## Base

`git log --oneline -1` read `03a405f` — `d7e863c` plus one reports-only fill
commit (the pointer-chain report). `git diff d7e863c --stat` was that file
alone.

## Edit 1 — the two v3.3.4 annotations

The sentence was appended once at each site, in italics, immediately after the
unit carrying the citation, with the citation's own words untouched:

- **§5, line 237.** After the bold lead-in `**The R7 build (2026-08-20,
  condensed; full record in v3.3.4 §5):**`, before the condensed record begins.
- **§7, line 309.** After the italic block that opens `*(Unchanged from v3.3.4
  except as resolved above: …)*`, as a second italic parenthetical.

The placement differs in form because the two citations are shaped differently
— one terminates a bold lead-in, the other opens an italic block that runs the
whole line — and nesting an italic inside either would have broken the markup.
Both readings of "immediately after the citation" land on the same sentence.

The third occurrence of `v3.3.4` in the file, on the `Last updated` line, is
this session's predecessor's own statement about the lineage and was left
alone: annotating it would be the note annotating itself.

## Edit 2 — the deferred bullet

**Before**

```
- **The Step-2 chart, the four-delta KPI card, the per-scenario pricing
  baseline, R7, the ARPU companion** — as at v3.3.11.
```

**After** — `— as at v3.3.11.` replaced by `:`, with the paragraphs beneath it
indented two spaces:

| topic | v3.3.11 §1 | verdict |
|---|---|---|
| The Step-2 chart | **L20** (first half) | identical |
| The four-delta KPI card | **L20** (second half) | identical |
| The per-scenario pricing baseline | **L21** | identical |
| R7 | **L22** | identical |
| The ARPU companion | **absent** | STOP — see below |

Two of the five topics share one paragraph (L20 states the chart grid and the
four-delta card in one bullet), so three paragraphs cover four topics. Each was
re-read out of the committed file, its two-space indent stripped, and diffed
against the `git show 1c1f224` slice: **three of three identical**.

The indent is the only departure from verbatim and it is the mechanism the
brief specified ("as an indented block"); the diffs above are run against the
de-indented text, which is what makes "identical" mean what it says.

## The topic that stopped

**The ARPU companion has no paragraph in v3.3.11 §1.** The section runs 17
bullets and none of them states it; the only occurrence of "companion" anywhere
in v3.3.11 is in §6's queue — *"ARPU-companion own mode"* — which is a backlog
item, not a statement of the capability.

There is also **no pointer to follow**. v3.3.11 §1 carries no "as at v3.3.N"
reference at all; its two `as at` phrases (`2ecdefb`, `670c7c3`) name commits,
not versions. So the brief's one-hop provision had nothing to act on, and the
STOP applied: the topic's name stays in the bullet as text and **no text was
written for it**.

**What a search does find, reported rather than used.** v3.3.10 §1 line 23 has
the paragraph:

```
- **The volume card's ARPU companion** is a rate in both amount modes;
  **own mode queued.**
```

Reaching it would have meant substituting my search for the document's own
chain, which is the thing this whole arc exists to remove. It also looks
deliberate rather than lost: v3.3.11 did not merely drop the bullet, it kept
the topic in §6's queue, and **that queue item is already present in this
document**. Inlining a v3.3.10 capability statement whose own text says "own
mode queued" would reintroduce a dated claim beside a live queue entry saying
the same thing.

One line settles it: inline v3.3.10 §1's paragraph, or strike "the ARPU
companion" from the bullet's list.

## Proof

```
git diff d7e863c -- docs/…v3-3-15.md --stat
  1 file changed, 6 insertions(+), 3 deletions(-)
```

Three lines changed, three added. The three changed are the §5 citation line,
the §7 citation line, and the bullet; the three added are the inlined
paragraphs. **No other line in the file moved.**

`grep "as at v3.3.11"` returns **two**, and they are not the same kind of thing:

- **L70** — `**UAT observation log:** through D4-03 as at v3.3.11; …` — a true
  back-reference. v3.3.11's own log line is inlined at **L63** and runs from
  D3-01 through D4-01, D4-02 and D4-03, so everything L70 defers is present
  above it.
- **L23** — `**The mix controls** as at v3.3.11 §1 (…)` — **not** a back-
  reference, and reported as such rather than framed as one. v3.3.11 §1 is not
  inlined. The citation carries its own enumeration in the parenthesis
  (`MixSliderRow`, `MixTargetPanel`, `MixPctBox`, locks persist,
  `rebalanceToTarget`, the wall, minimum change, exactly-determined), and the
  same subject is independently recorded at **L274** in §5's padlock/target arc
  entry. That is why the brief left it, and it remains the honest description.

## Gate

```
full suite    61/61 green   (serial, one run, exit 0)
spec:ai-hold  13/13
guard-traps   NOT RUN — excluded by the brief
final file    339 lines, 82,663 bytes (was 336 / 81,806)
```

## Limits

- **One topic is unresolved by instruction**, not by failure: the ARPU
  companion, above. The bullet now covers four of the five names it lists.
- **guard-traps was not run.** The 61 specs are green on `836fb60`; the 177
  traps were last proven at `d63e67a`, and no file under `src/` or `scripts/`
  has changed since `78e06de`.
- **The rendered markup was not viewed.** The nested list and the two italic
  annotations are correct by construction and by diff; nothing rendered them.
