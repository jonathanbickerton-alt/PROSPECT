# D5-07 closed on Jon's walk

```
FOR ADVISOR
Generated: 2026-09-07 15:15 +0100 (UTC 2026-09-07 14:15)
Certifies: 7b43e73 (the tree the suite and survival were measured on)
Repo: committed 7b43e73, pushed (origin in sync)

BASE 45a437a + eb3a912 (reports/ only; ZERO drift in src/ scripts/ test-data/
  package.json). Proceeded — the extra commit is last session's own report.
DOCS ONLY. EXPECTED.md's D5-07 entry, appended. Nothing else in the file.
No source, no scripts, no package.json. guard-traps NOT run.
APPENDED VERBATIM, as its own bold paragraph at the end of the D5-07 entry,
immediately before the "D5-05 / D5-06 DECIDED" heading:
  "CLOSED 45a437a; walked by Jon 2026-09-07 on the 07 Sep 09:13 save:
   markers at the event month T on Volume, Revenue and ARPU; off event and
   off campaign produce none; switching on/off moves them on all three."
DIFF: test-data/EXPECTED.md, 4 insertions, 0 deletions. Nothing else staged.
full suite: 61/61 green — unchanged.
survival: 104/26 in the standing form (104 first-row dereferences across 26
  files; the spec's own check count is 27/27, a separate figure).
```

## The append

`test-data/EXPECTED.md`, at the end of the D5-07 entry recorded at `58f136b`,
placed immediately before the `#### D5-05 / D5-06 DECIDED` heading so it closes
the entry it belongs to rather than opening the next one:

```diff
 So this is a one-axis binding, not a data, filter, scope or lag defect — the
 markers were never wrong about *which* events or *which* month.

+**CLOSED `45a437a`; walked by Jon 2026-09-07 on the 07 Sep 09:13 save: markers
+at the event month T on Volume, Revenue and ARPU; off event and off campaign
+produce none; switching on/off moves them on all three.**
+
 #### D5-05 / D5-06 DECIDED (Jon, 2026-09-05) — the bar states its reason…
```

Wording is the brief's sentence exactly. It is wrapped to the file's column and
bolded to match how the other closed entries in the log carry their closure —
no word, hash or mark differs from what was given. Four insertions, zero
deletions; `git diff --stat` confirms `EXPECTED.md` is the only file touched.

**What the walk adds that the build could not.** The build's own evidence was a
jsdom count round trip and two planted traps, and its report recorded one limit
plainly: the marker's month was pinned *structurally*, not read from the DOM —
nothing confirmed the marker sat at `2026-02` rather than `2026-03`. Jon's walk
closes exactly that gap by observation, and extends past it: **off campaign**
produces no markers, which the mounted spec never exercised at all (it drove
individual events, not a campaign switch).

## Gate

`npm run suite` once, serial. No `guard-traps` — nothing in this session
touches source, scripts or `package.json`, and it is not a docs-session
instrument.

```
full suite   61/61 green   (unchanged from 45a437a)
survival     104/26        (104 first-row dereferences across 26 files)
```

**On the survival form.** The brief asked for `104/26` and called it
`checks/files`. The figure is printed as asked, but the two numbers are
dereferences and files, not checks and files — the spec's check count is a
third number, `27/27`. Stating that here so the `checks/files` label does not
propagate into the next report as though 104 were a check count.

The suite figure not moving is the evidence this was docs-only: a docs session
that shifted it would not have been one.

## Limits

- **Nothing was re-measured about the walk.** Jon's observation is recorded, not
  reproduced — this session ran no chart, mounted nothing, and did not open the
  07 Sep 09:13 save.
- **`guard-traps` was not run**, so this report certifies no trap count. The
  last gated figure remains 171/171 at `45a437a`.
- **The "off campaign" half of the walk has no automated cover.** The mounted
  spec added at `45a437a` drives individual event switches on all three
  measures; it does not drive a campaign switch and then count markers. That is
  a gap the walk closed by eye and nothing currently guards.
