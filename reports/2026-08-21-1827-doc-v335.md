# Working agreement v3.3.5 — supersedes v3.3.3, in one commit

## FOR ADVISOR

```
Generated: 2026-08-21 18:27 +0100 (UTC 2026-08-21 17:27)
Certifies: __HASH__
Repo: __REPO__
SKELETON CREATED AS THIS SESSION'S FIRST ACTION, before the base check.
BASE: HEAD 3e5ebc8 vs 1302's 4a1d110 — one commit, REPORT-ONLY.
DONE: v3-3-5 added and the superseded copy removed in ONE commit, per the
  maintenance ritual. No source file was touched.
THE BRIEF NAMES v3-3-4; THE SUPERSEDED FILE IS v3-3-3. Corrected, not guessed:
  `git log --diff-filter=A -- 'docs/*v3-3-4*'` returns NOTHING, and the tracked
  tree at HEAD held exactly one working agreement — v3-3-3. The committed
  lineage is 3.3.1 -> 3.3.2 -> 3.3.3 -> 3.3.5.
SO v3.3.4 EXISTED AS A DOCUMENT BUT WAS NEVER COMMITTED — the new file cites it
  four times ("full record in v3.3.4 §5", "carried verbatim from v3.3.4").
  It lived in advisor project context only.
THAT IS THE ONE VERSION THAT MATTERED: v3.3.5's own purpose note says a
  condensation dropped "barred class" from D5 IN v3.3.4 and inverted the
  decision — the error that produced the wrong Fix 1 brief on 2026-08-21.
  The single version git cannot show is the single version that caused a
  defect. Recorded, not acted on.
NO GATE RUN — no source file changed. tsc run as optional sanity: clean.
  guard-traps, the specs and the build were NOT re-run and this report
  certifies none of them; the last gated state is 4a1d110.
```

---

## Base check

`HEAD` **`3e5ebc8`**; the 1302 report's Repo line names **`4a1d110`**. One commit
apart, `--stat` confirms **report-only** (its own Repo-line fill). Within the
brief's stated expectation, flagged and proceeded.

## The two-file commit

Staged together, as the ritual requires:

```
D  docs/PROSPECT-development-history-and-working-agreement-v3-3-3.md
A  docs/PROSPECT-development-history-and-working-agreement-v3-3-5.md
```

**One commit, so the tree never holds two working agreements** — a reader who
lands between an add and a later delete cannot tell which is current, and the
document's own purpose note says a stale copy silently applied is worse than no
copy.

### The brief named v3-3-4; the superseded file is v3-3-3

Checked rather than assumed:

```
git log --all --oneline --diff-filter=A -- 'docs/*v3-3-4*'   ->  (nothing)
git ls-tree --name-only HEAD docs/
    docs/PROSPECT-development-history-and-working-agreement-v3-3-3.md
```

The committed lineage is **3.3.1 → 3.3.2 → 3.3.3 → 3.3.5**. There is exactly one
superseded file and no ambiguity about which to remove, so the naming slip
changed nothing about the action — but it is recorded because the next reader
of `docs/` will see a version skip and deserve the reason.

### v3.3.4 was real, and is the one version git cannot show

The new document cites it four times — *"full record in v3.3.4 §5"*, *"carried
verbatim from v3.3.4"* — so it existed as a document. It lived in the advisor's
project context and was never committed here.

**And it is the version that cost something.** v3.3.5's own purpose note records
that a condensation dropped the words *"barred class"* from D5 **in v3.3.4**,
inverting the decision's meaning — which is exactly the error that produced the
Fix 1 premise refuted in the 1200 session. The one version the repo cannot show
is the one version that caused a defect.

That is an argument for the ritual rather than a problem to fix now: a document
that only exists in chat context leaves no lineage to check a claim against, and
checking a claim against the lineage is precisely what refuted that brief.

## Gate

**No gate was run, and no source file changed** — the commit is two documents
and this report.

```
tsc --noEmit:            clean (optional sanity, per the brief)
guard-traps:             NOT RUN
specs:                   NOT RUN
build:                   NOT RUN
```

**This report certifies no test state.** The last gated state remains
**`4a1d110`** — guard-traps 110/110, fourteen specs, mounted mix-card 195/195,
lint and build clean — and nothing since has touched `src/` or `scripts/`.

## Limits of this check

**I did not verify the document's factual claims against the repo.** It asserts
hashes, counts and decisions; I checked only the two that were free and
load-bearing for this session — that main's last gated close was `4a1d110`, and
that `docs/` held exactly one prior working agreement. The rest is Jon's
compilation and is committed as supplied.

**The version skip is now permanent in the lineage.** `docs/` goes 3.3.3 → 3.3.5
with nothing between, and only this report explains why.
