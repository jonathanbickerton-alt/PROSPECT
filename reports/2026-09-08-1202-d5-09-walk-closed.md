# D5-09 closed on Jon's walk

```
FOR ADVISOR
Generated: 2026-09-08 12:02 +0100 (UTC 2026-09-08 11:02)
Certifies: 9b58b48 (docs commit; suite measured on the identical tree)
Repo: committed 9b58b48, pushed (origin in sync)

BASE d63e67a + b4a6abb (reports/ ONLY; ZERO drift in gated paths). Proceeded.
DOCS ONLY: 1 file, +6 lines, 0 deletions. No src/, scripts/, package.json.
guard-traps NOT run (excluded by the brief). Suite run once, serial.
APPENDED VERBATIM, as D5-09's bold closing paragraph:
  CLOSED `d63e67a` (sessions A `3d3ef3d`, B `ae58aca`, C `d63e67a`); walked by
  Jon 2026-09-08 on the 07 Sep 09:13 save: card 4 · moving volume · 5
  switched on; EFFECT reads Volume ×4, Off ×5, ARPU ×1; switching moves
  label, number and caption; de/it render; Compare shows the same column on
  the 09:13 and 04 Sep 21:37 files.
Sits after the RECORDED WATCH block, before `#### D5-08 DECIDED`, matching
  D5-07's and D5-08's form. The D5-09 arc is closed; nothing held.
full suite: 61/61 green
survival:   104 dereferences across 26 files; 27/27 checks
```

## Base

`git log --oneline -1` read `b4a6abb Report D5-09 session C: Compare joins, one
id derivation` — that is `d63e67a` plus **one reports-only fill commit**
(`reports/2026-09-08-1116-effect-column-c.md`, the session-C report being
filled at its close). `git diff d63e67a --stat -- src/ scripts/ test-data/
package.json` was empty. The permitted case, stated here as required rather
than passed over. Working tree clean at start.

## The append

One paragraph, six lines including the blank line before it, at the end of the
D5-09 entry. Line-wrapped to the file's width; the hashes are backticked, as in
D5-07's and D5-08's closing paragraphs, and no other character differs from the
text supplied in the brief.

`git diff --stat` before the commit: `test-data/EXPECTED.md | 6 ++++++`,
**1 file changed, 6 insertions(+), 0 deletions(-)**. That figure is the
evidence for "changed nothing else" — a docs session cannot be certified by
reading the diff it just wrote, but it can be certified by the diff's shape.

The paragraph records what Jon walked, not what the specs assert: the KPI card
reading 4 against 5 rows switched on, the EFFECT column reading Volume ×4,
Off ×5 and ARPU ×1 on that save, the label/number/caption moving together on a
switch, two non-English locales rendering, and Compare showing the column on
**both** files. The last of those is the one the specs cannot reach — the
mounted Compare spec drives a stubbed Worker, so a real two-file session in the
browser is the only evidence that the memo derives per file correctly.

## Gate

```
full suite   61/61 green   (serial, one run, exit 0)
survival     104 first-row dereferences across 26 files; 27/27 checks
guard-traps  NOT RUN — excluded by the brief
```

Both figures match the session-C report's, which is what a docs-only change
should produce: the append moved no code, so the gate's job here is to show
that it did not.

## Limits

- **guard-traps was not run.** The brief excluded it. So this report certifies
  that the 61 specs are green on `9b58b48`; it does not re-certify that the 177
  traps still bite. The last run that did is `d63e67a` (177/177), and no source
  file has changed since.
- **The walk itself is Jon's observation, not a measurement made here.** The
  numbers in the appended paragraph — 4, 5, ×4/×5/×1 — were read from the app
  by Jon on the 07 Sep 09:13 save and are recorded verbatim. Nothing in this
  session re-derived them.
- **No source was read.** This session opened `test-data/EXPECTED.md` and the
  session-C report only.
