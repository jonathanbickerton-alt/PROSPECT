# Working agreement v3.3.6 — supersedes v3.3.5, in one commit

## FOR ADVISOR

```
Generated: 2026-08-31 19:58 +0100 (UTC 2026-08-31 18:58)
Certifies: e0eb9b1 (report filled one commit later)
Repo: committed e0eb9b1, pushed (origin in sync)
SKELETON CREATED AS THIS SESSION'S FIRST ACTION, before the base check.
BASE: HEAD 822809f vs 1924's de975a7 — one commit, REPORT-ONLY.
DONE: v3-3-6 added and v3-3-5 removed in ONE commit, per the maintenance
  ritual. No source file touched. The brief's naming was correct this time —
  v3-3-5 was the tracked copy, unlike the v3-3-4 slip last round.
GIT RECORDED IT AS A RENAME (R), not delete+add: v3.3.6 is a 23-LINE REVISION
  of v3.3.5, not a rewrite. The delta is small enough to read, and it is
  summarised below so the reviewer need not diff it.
EVERY CHECKABLE CLAIM IN THE NEW DOC VERIFIED, unlike last round where only
  two were: 4a1d110 as the last gated state, 4297026, de975a7, 29d7abd, and
  the guide's tracked path all match the repo.
THE LOAD-BEARING CLAIM WAS VERIFIED INDEPENDENTLY: `git diff --name-only
  4a1d110..HEAD` touches ONLY docs/ and reports/ — zero changes under src/,
  scripts/ or test-data/. So "4a1d110 is still the gated baseline" is true,
  and a later session can rely on it.
CARRIED INTO THE RECORD FROM 1924: the 50-of-76 untranslated locale keys are
  now a QUEUED session in the doc's own backlog, not just a report finding.
UAT RE-SCOPE is Jon's dated decision and is recorded as supplied, not checked.
NO GATE RUN — no source file changed. tsc NOT run either; nothing it could
  check changed. This report certifies no test state; the last gated state
  remains 4a1d110.
```

---

## Base check

`HEAD` **`822809f`**; the 1924 report's Repo line names **`de975a7`**. One commit
apart, `--stat` confirms **report-only** (its own Repo-line fill). Within the
brief's stated expectation, flagged and proceeded.

## The two-file commit

Staged together, as the ritual requires. Git resolved the pair as a **rename**
rather than a delete plus an add, because the content is close enough:

```
R  docs/PROSPECT-development-history-and-working-agreement-v3-3-5.md
-> docs/PROSPECT-development-history-and-working-agreement-v3-3-6.md
   23 lines changed
```

**That is a better outcome than the v3.3.5 round**, where the same ritual
produced a 133-line delete against a 109-line add and the delta was
unreadable. Here the revision can simply be read.

`docs/` now holds exactly two tracked artefacts — the current working agreement
and the user guide — with no second working agreement at any point in the
history, which is the property the one-commit rule exists to guarantee.

### What changed, so the reviewer need not diff it

- **Header restated around `4a1d110`** as the last *gated* state, naming the
  gate figures and listing the docs-only commits since.
- **UAT re-scoped** (Jon, 2026-08-21): opens ~2026-09-01 on curated files Jon
  supplies; DQ built in parallel and delivered before UAT closes. This
  explicitly supersedes the former *"UAT bar: the DQ import phase"*, and the
  doc tells a fresh advisor not to flag UAT-before-DQ as premature.
- **The user guide recorded as a tracked artefact**, English signed off.
- **The user-guide arc added to the history** — `4297026` and `de975a7`.
- **Three lessons minted**, all from the 1924 session: an edit that reports
  success without matching is the no-op class in another costume; content past
  a splice point is not reached by the splice; a guide quotes what the user
  sees, not what should exist.
- **DQ restated** as the next build arc with its true-state brief drafted.
- **The locale sweep added to the queued backlog** — the 50-of-76 finding.

### The claims I checked

Unlike the v3.3.5 round, where I could verify only two claims cheaply, every
hash and state claim in this document was checkable and checks out:

| Claim | Verdict |
|---|---|
| `4a1d110` is the last gated state | **verified** — see below |
| guide English update is `4297026` | matches |
| translations are `de975a7` | matches |
| v3.3.5 doc commit `29d7abd` | matches |
| guide tracked at `docs/PROSPECT_UserGuide_Multilingual.html` | matches |
| guard-traps 110/110, fourteen specs, mix-card 195/195 | matches the 1302 report |

**The load-bearing one, verified independently:**

```
git diff --name-only 4a1d110..HEAD | sed 's#/.*##' | sort -u
    docs
    reports

git diff --stat 4a1d110..HEAD -- src/ scripts/ test-data/
    (empty)
```

Seven commits since the gate, and **not one touches `src/`, `scripts/` or
`test-data/`**. So the document's assertion that `4a1d110` remains the gated
baseline is true, and a later session can rely on it rather than re-deriving it.

## Gate

**No gate was run, and no source file changed** — the commit is one document
and this report.

```
tsc --noEmit:            NOT RUN — nothing it could check changed
guard-traps:             NOT RUN
specs:                   NOT RUN
build:                   NOT RUN
```

**This report certifies no test state.** The last gated state remains
**`4a1d110`** — guard-traps 110/110, fourteen specs, mounted mix-card 195/195,
lint and build clean — and the verification above shows nothing since has
touched the tree it certifies.

## Limits of this check

**The UAT re-scope is recorded as supplied, not verified.** It is Jon's dated
decision about scheduling and scope; there is nothing in the repo to check it
against. The doc says the DQ true-state brief's first act records it in
`EXPECTED.md`, and that recording has not happened yet — until it does, the
re-scope lives in this document and in the advisor chat, not in the decisions
file.

**The prose was not audited.** I verified the hashes, the gated-state claim and
the tracked paths. The narrative sections, the minted lessons and the backlog
ordering are Jon's compilation, committed as supplied.

**No v3.3.4 gap this time, and the v3.3.3 → v3.3.5 skip stands.** The committed
lineage is now 3.3.1 → 3.3.2 → 3.3.3 → 3.3.5 → 3.3.6; the missing 3.3.4 is
explained in the `2026-08-21-1827` report and nothing here changes it.
