# v3.3.15 — final

```
FOR ADVISOR
Generated: 2026-09-08 14:29 +0100 (UTC 2026-09-08 13:29)
Certifies: de66add (the tree the suite was measured on)
Repo: committed de66add, pushed (origin in sync)

BASE 836fb60 + 62fad90 (reports/ ONLY; ZERO drift in gated paths). Proceeded.
DIFF vs 836fb60: 1 file changed, 1 insertion(+), 1 deletion(-) — line 24.
FINAL FILE: 339 lines, 82,643 bytes. Suite 61/61; spec:ai-hold 13/13.
CORRECTION to report 1417 below: the ARPU companion is recorded at §3 L89,
  not in §6's queue as that report said. Jon's decision is unaffected.
```

## Base

`git log --oneline -1` read `62fad90` — `836fb60` plus one reports-only fill
commit (the loose-ends report). `git diff 836fb60 --stat` was that file alone.

## The edit

```
- - **The Step-2 chart, the four-delta KPI card, the per-scenario pricing
      baseline, R7, the ARPU companion**:
+ - **The Step-2 chart, the four-delta KPI card, the per-scenario pricing
      baseline, R7**:
```

`git diff 836fb60 -- docs/…v3-3-15.md` reports **1 file changed, 1 insertion,
1 deletion** — line 24 and nothing else. The bold list now names exactly the
four topics whose paragraphs sit indented beneath it, so the bullet no longer
promises a fifth.

## The correction

Report 1417 argued the topic looked deliberately dropped because "v3.3.11 keeps
the topic in §6's queue — already in this document". **The second half of that
was wrong.** §6 is rewritten per version and was never part of the pointer
chain, so v3.3.11's queue item did not come across.

The topic does survive, in a different place: **§3, line 89**, inside the
inlined "Jon, 2026-09-02" entry — *"…the ARPU companion own mode queued…"*,
cited under its EXPECTED.md heading. It is the only occurrence of "companion"
left in the file, and it is a recorded decision rather than a capability
statement, which is the shape §1 was never the right home for.

So the striking is sound on the evidence that actually holds: the name is gone
from a bullet that could not support it, and the decision it stood for is still
in the document under §3.

## Gate

```
full suite    61/61 green   (serial, one run, exit 0)
spec:ai-hold  13/13
guard-traps   NOT RUN — excluded by the brief
final file    339 lines, 82,643 bytes (was 339 / 82,663)
```

## Limits

- **guard-traps was not run.** The 61 specs are green on `de66add`; the 177
  traps were last proven at `d63e67a`, and nothing under `src/` or `scripts/`
  has changed since `78e06de`.
- **The rendered markup was not viewed.** The change is one clause inside an
  existing bold run; verified by diff, not by rendering.
