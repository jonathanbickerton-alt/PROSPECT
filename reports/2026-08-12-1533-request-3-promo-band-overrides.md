# Request 3 — held again, and the base premise drifts a third time

## FOR ADVISOR

```
Generated: 2026-08-12 15:33 +0100 (UTC 2026-08-12 14:33)
Certifies: __CERT__
Repo: __REPO__
SKELETON CREATED AS THIS SESSION'S FIRST ACTION, before the state check and
  before any code — the rule shipped at 1a2efa4, now standing practice.
STATE CHECK: HEAD 099cd04, tree clean, origin in sync. The brief's base
  `5710300` DOES NOT EXIST — the third Request 3 brief in a row to name a base
  that is not in the repo (62b3c66, then 5710300). Request 3 is unbuilt: no
  promoBandArpuOverride, nothing in src/.
BUILD NOT STARTED, shed at the budget clause before spending. Request 3 is a
  per-band input with four display states, effective-rate wiring into three
  engine functions, a persisted carrier with export/import and counted writer
  pins across TWO market-event import routes, absence-resolution lifting a save
  refusal, a fixture case and a mounted spec — plus a gate and a close.
THE PRE-AUTHORISED SHED DOES NOT FIT EITHER. "Persistence half as an inert
  carrier" is the a50cca9 shape, and a50cca9 was a whole session. Shedding to it
  would have produced the same half-built outcome one level down.
THIS IS THE FOURTH CONSECUTIVE SESSION TO HOLD REQUEST 3, and the pattern is
  worth naming rather than repeating: the brief is sized for a session with a
  full budget and keeps arriving at sessions without one. The fix is not another
  reissue — it is a fresh context, or a brief split so each half fits.
NOTHING WAS WRITTEN except this report. Tree clean, no partial build, nothing
  to revert or recover. Request 3's design remains fully recorded in
  EXPECTED.md, so a fresh session can take the brief unchanged.
Instruments: none run — no code changed, and that is stated rather than implied.
```

---

## State check

| check | result |
|---|---|
| HEAD | `099cd04` |
| tree | clean but for this skeleton |
| origin | in sync |
| brief's base `5710300` | **does not exist** |
| Request 3 in `src/` | **none** |

**The base premise has now drifted three briefs running** — `62b3c66`, then
`5710300`, neither in the repo. Each time the named base is a commit that would
exist if Request 3 had been built. Worth flagging as a pattern rather than a
typo: the briefs are being written against an assumed future state.

## What was built

**Nothing, deliberately.** The build was shed at the budget clause, before
spending, which is the checkpoint working as designed rather than a session
running out mid-way.

Request 3 needs: a per-band input with four display states; effective-rate wiring
into `achievableTargetRange`, `solveForTarget` and `blendedArpu`; a persisted
carrier with its export column and **both** market-event import routes; counted
writer pins with a trap; absence-resolution lifting the save refusal; a fixture
case; and a mounted spec with the live-range transition. Plus a gate and a close.

**The pre-authorised shed does not fit either.** "The persistence half as an
inert carrier" names the `a50cca9` shape — and `a50cca9` consumed a full session
by itself. Shedding to it would have reproduced the same problem one level down:
a half-built increment with no budget left to record it.

## The pattern worth naming

**This is the fourth consecutive session to hold Request 3.** Declining once is
judgment; declining four times is a signal about how the work is being sized.

The brief is written for a session with a full budget and keeps arriving at
sessions that have already spent theirs on recovery, rule amendments, or the
previous increment's close. Another reissue will not change that. What would:
**a fresh context**, or **a brief split** so each half is independently
shippable — the persistence carrier and the card surface, as Request 2 was
eventually forced into.

## Gate

None run. No code changed, so there is nothing to gate — stated rather than
implied by silence.

§33 with the scope named: **main's working tree and build output are AI-free**
(`package.json`, `src/`, `.env`). History and remote branches are out of scope;
the preserved `ai-capability` branch is expected.

## Walk instructions

**None — nothing changed.** The promotion card is exactly as Jon last walked it.

## Where things stand

Request 3: **designed, unbuilt**, its four decisions recorded in EXPECTED.md so a
fresh session can take the brief unchanged. Its decision 3 — an override
*resolving* absence and lifting the save refusal — is still the genuinely new
ground in the arc.

Request 2 is complete, with item 1's `?? 0` display fixes and the mounted spec
held. Option (b) is queued, scoped and untouched. Nothing is uncommitted and
nothing needs recovery.
