node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Archived versions — never needed in the repo
*.zip
/_archive/

# Synthetic test data — keep local, not in the shared repo
/test-data/*.xlsx

# Local forecast data, imports and exports — keep local, never in the shared repo
/PROSPECT_data/

# Build output and dependencies
/node_modules/
/dist/
