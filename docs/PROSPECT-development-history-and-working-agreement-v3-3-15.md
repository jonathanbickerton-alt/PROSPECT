# PROSPECT — Development History & Working Agreement

**Purpose of this document:** persistent context for the PROSPECT Claude project. Every advisor chat reads this before its first turn. Replace this file in project context whenever it is updated — a stale copy silently applied is worse than no copy. The document is ALSO committed to the repo (`docs/`); project context carries only the latest, git holds the lineage. **This document is a MAP. For settled decisions, EXPECTED.md verbatim is the terrain.** **A prior report's claim is a premise for the next brief, not a fact; a guide's claim is the same class; a report's "not reproduced" is not evidence of absence; an observation from two different saves is two observations; a line number in a report is a locator that drifts — re-check it before relying on it.** **Every decision this document relies on is written here in full or cited by its EXPECTED.md HEADING.**

**Last updated:** 2026-09-08 (v3.3.15 — a FULL document: collapses the v3.3.12 → v3.3.13 → v3.3.14 delta chain and folds 2026-09-07 (Dev 5's closing delta, the python arc, the prompt-surface audit, the AI-hold spec, D5-07, D5-08, the REQ-D6-01 chip clarification) and 2026-09-08 (the D5-09 arc, sessions A/B/C). Blocks marked `[INLINE …]` are the v3.3.13 text carried verbatim from `docs/` history at the assembling commit; nothing in them was rewritten.) **UAT IS LIVE — opened 2026-09-01, closing around 15 September; core users work in GERMAN and ITALIAN; native review inside UAT via the users' findings. NO OPEN UAT FINDINGS as at 2026-09-08 12:02.** Last gated state **`d63e67a`** — **guard-traps 177/177 (0 CRASHED / MISSED / INCONCLUSIVE; ids unique by number)**, **suite 61/61 via `npm run suite`**, spec:ai-hold 13/13, event-toggle 112/112, view-apply-mounted 168/168, trap-anchors 189/189 (177 traps, 184 anchors), survival 104/26 dereferences/files and 27/27 checks, i18n-parity 200/200 (keys 874 per locale, six locales: en de es fr it pt), tsc 0, lint and build clean. Docs-only commits since: `9b58b48` (D5-09 walk closed). Provenance: reports 0918, 1202, 1215, 1230, 1241, 1341, 1412, 1436, 1515, 1533, 1658, 2127 (2026-09-07); 0915, 0943, 1027, 1116, 1202 (2026-09-08); Jon's walks of `45a437a`, `8c0cb71`, `d63e67a` reconciled against screenshots; hashes from reports only.

---

## 1. What PROSPECT is

PROSPECT (Predictive Reporting Of Scenarios & Planned Execution for Commercial Trends) is a React/TypeScript subscriber forecasting application for Vodafone, built entirely through Claude Code — Jon directs, writes no application code himself. Rebuilt around real IBRO data (Inflow, Base, Retention, Outflow).

- Three-step workflow via ForecastContext: Baseline Forecast → Market Events → Actuals Review; plus **Scenario Compare** (up to 4 session files; own parser feeding `computeScenarioForFilter` — a SECOND ENGINE with the only blended-ARPU computation left). Top-nav: **Overall Forecast**.
- Models: SES, Holt Linear, Damped Trend, true Holt-Winters. Scoring: symmetric absolute percentage deviation + band position.
- **Base volume is never fitted or stored per month** — reconstructed at read time from the seed rolled through flows. **All four per-scenario ARPUs are fitted and banded on every forecast month.**
- Dimensions: Customer Segment + Product/Channel/Tariff L1/L2; 7-part keys. 'All' is a DATA VALUE with TWO representations (`null`, the string `'All'`); ONE predicate `eventScopeMatchesView` (11 + 4 pinned callers; structural zero-occurrence check on the hand-rolled shape).
- **Event carriers — THREE** (`MarketEvent`, `YieldEvent`, `PricingEvent`); pipeline market → yield → pricing. **A promotion is a `MarketEvent`** built by `buildPromoEvents`; its price change is **baked into its own `arpu` at build time** and it never enters the pricing pass (Jon, 2026-09-05: stays baked; duration NOT built — a Recurring control would be a stored mode nothing reads; the spread is the promotion's multi-month mechanism).
- **How a market event reaches a view (as at `2ecdefb`, unchanged):** applicability by the shared predicate; weighting by `forecastCoverage` (percentage) / `eventProRataShare` (absolute) under the null/0 contract; coverage-0 → `zeroCoverageEventIds`.
- **Pools feed the ARPU of their own scenario (`2ecdefb`):** Inflow pool → Inflow ARPU; **re-banded Retention pool → Base at T+1 ONLY** (Retention ARPU is built by `scenarioPools`, which never reads `p_eventPools` — paid for three times, now in EXPECTED.md); the synthetic yield pool is built from the PREVIOUS month's inflow and feeds Base via the lag (**verified by hand `e5f1e79`**: Base ARPU Δ = Σ delivered size × (rate − baseArpu) / stock, at T+1, T+2 and All). ONE pool arithmetic since `d92fdaa` — the promoRebanded pool sized by `resolvedEventVolume(…, 'retention')` at `idx` (this was NOT done at `2ecdefb`, contrary to v3.3.11 §1).
- **Percentage promotions (`d92fdaa`, `945d648`):** `buildPromoEvents` takes `amountType`; for a percentage, `revenue` is 0 (a magnitude cannot survive) and `arpu` is baked (a rate survives) — the same rule `draftEventRate` already encoded; `percentageBasis: 'baseline'`; Inflow basis = the view's fitted inflow, Retention basis = the view's fitted retention for the month, both through the ordinary market-event path (a +10% promotion moves a view exactly as a +10% plain event — pinned). **The mix carries NO per-tier volumes** — it is a pricing device, one blended rate; the pool is sized from the resolved delta and priced at the mix blend. Round-trips through `Amount_Type` / `Percentage_Basis`; a promo row without `Amount_Type` loads as absolute. **The unit control on the promotion arm is a third inline copy** (three amount-mode controls, none shared; trap 122 is global(2) over the shared label expression). Volume % and price % are lexically separated (keys `whatif_promo_volume_pct_label/_help`).
- **Promo Dilution (`90e2162`):** `promoPricingMode` gains `'dilution'` riding the percentage arm through `dilutionAmountPct` (ONE function, 8 call sites); equality with the Pricing card pinned to 1e-9 three ways; columns `Promo_Dilution_Current_Pct/_Target_Pct` appended; no Target / Applies-to (decisions 4/5); applied once at build — cannot compound (unstated on screen). The promotion got its own block predicate rather than a third caller of `pricingDraftBlockReason`.
- **Display:** `eventVolumeLabel` owns the percentage form of a stored volume on three surfaces (`+10%`); absolute stays per-caller; the Volume tab's `fmtDelta` (`+10.0%`) is a remaining duplication. **The KPI ARPU Delta card subtracts UNROUNDED figures and rounds once** (`e5f1e79`); the 2dp `chartData` columns are untouched.
- **The mix controls** as at v3.3.11 §1 (`MixSliderRow`, `MixTargetPanel`, `MixPctBox`, locks persist, `rebalanceToTarget`, the wall, minimum change, exactly-determined).
- **The Step-2 chart, the four-delta KPI card, the per-scenario pricing baseline, R7, the ARPU companion** — as at v3.3.11.
- **Locales:** six, parity 194/194 (`I18N_PHASE2`/`TRANS_BACKLOG` never re-matched; `.ts`/`.tsx` asymmetry — backlog). No `strictNullChecks` (backlog).
- **The gate (as at `e5f1e79`):** `guard-traps` CAUGHT / MISSED / INCONCLUSIVE / CRASHED with its own controls; global-mutation class; `spec:trap-anchors`; **`npm run suite`** — serial, each spec captured to a file, GREEN / FAILED / CRASHED by report-line sentinel, guard-traps a separate step; **`spec:survival`** baselines first-row dereferences per file (91 over 24 files), exact both ways, comments stripped.
- Business users: Alessandro Russo and Marcel Wiegand. Roadmap: Cloud Run + BigQuery, AI Booster ML, four AI use cases; AISHA event schema.

*As at v3.3.12 §1 in full. Additions (`ede9b43`, `2864d60`):*
- **A promotion is edited from its source (Jon, 2026-09-05, option 1).** The Volume table lists promotion-built rows (unchanged) but its row pencil and its campaign pill route through ONE router (`editEventFromVolumeTable` / `editCampaignFromVolumeTable`): `isPromotion` → switch to the Promotion tab first, then `handleEditPromoStart` / `handleEditPromoCampaign`; otherwise the Volume form. The Volume form is never opened for a promotion-built row (its patch-spread save preserved the arms but zeroed the baked rate, 36 → 0 — measured 1903). The Volume card's campaign pill for a promo campaign previously rendered as a disabled span with no reason; it now routes.
- **Edit-restore of a promotion restores `amountType`** in both edit paths and `resetPromoDraft` clears it (D5-03: one writer, the toggle; a reopened +10% promotion saved back as ten subscribers). All three promo builders now declare `promoAmountMode`, `promoMixLocked` and the dilution pair in their dependency arrays (D3-04's shape).
- `computeTierData` returns `[]` unless a revenue or ARPU COLUMN is named; a mix with zero tiers correctly disables Save (fixture facts, recorded at the spec).

**Per-event on/off (REQ-D6-01, Alessandro, built `fc718bf` + `bd50f27`).** Every carrier extends a shared base with `enabled?: boolean`; **absent means ON** (every pre-existing save reloads unchanged). ONE predicate `isEventOn` (`forecasting.ts`) is the only definition of "off"; it reads BOTH the typed `enabled` and the raw `Enabled` column because Compare's engine holds sheet rows and never calls `marketEventFromRow`. **Exactly 12 apply sites** (8 What-If, 4 Compare) pinned by marker text; **exactly 6 display sites** (tooltip ×3, month markers, retention warnings, churn exclusion set) pinned apart. A third pin counts reads of `.enabled` outside the predicate and its writers: **5 in `forecasting.ts` + 1 in `WhatIfTab.tsx`** as at `d63e67a`, every one of them a read of a DERIVED summary row, never of an event carrier — the honest wording is "zero reads that constitute an apply decision", not "zero reads elsewhere" (0918 caveat). The pricing self-exclusion needed no change (it routes through `computeAdjustedForecast`). `Enabled` appended LAST on all three sheets, `'Yes'/'No'`, reader `=== 'No' ? false : true`. ONE `EventOnOffSwitch` (`role="switch"`, `aria-checked`, `mixed`), rendered first in the events summary row and in all four card tables; off rows greyed by `OFF_ROW`, written once; a promotion's two rows share one field; the campaign pill carries one switch on the group's first row — on/off sets every row through `handleSetEventEnabled` (exactly 5 invocations pinned), mixed renders `aria-checked="mixed"` and a click on mixed turns the campaign ON; switching is not editing, so the percentage-campaign bar does not block it. Compare drops at its engine, so its panel lists an off event greyed via the shared table's read-only mode. The term is "on"/"off", never "scenario".

**Three counts on the Market Events step are three different populations, by design (REQ-D6-01 decision 6, clarified by Jon 2026-09-07, EXPECTED.md `e4fd63f`):** the **summary bar's** badge (`ForecastSummaryBar.tsx`, `.filter(isEventOn).length`, trap 169) counts events that are ON; the **Events summary panel's** chip ("N events", `EventsSummaryTable.tsx`, `rows.length`) counts ALL rows, on and off — it has always done so and no spec asserts it either way; the **KPI card** counts events applied via apply site 1 (volume path — `appliedEventIds` feeds it, so a Value/Pricing event never enters it); the Metadata sheet counts all rows. On the 07 Sep 09:13 save: chip 10, card 4, five on.

**D5-05 (`cb09643`):** a campaign containing a percentage row stays group-edit-barred (the ramp reverse-engineering sums volumes); its rows are editable one at a time; both campaign pills render the existing reason `whatif_campaign_decline_percentage` as TEXT with `aria-disabled`. A row edit writes exactly one row by id. The two pills remain byte-identical JSX (trap 163 on the Volume copy only). **D5-06:** the dilution effect line renders the incomplete and out-of-range reasons distinctly (asserted en and de). **Coverage (`e177d62`):** the dilution control driven end to end with a non-zero guard in front of the ratio check; the campaign route driven on an absolute campaign; the KPI precision string pinned (`"+0.01"`).

**D5-07 — event month markers on all three measures (`45a437a`).** The marker list was never wrong; the `<ReferenceLine>` was hard-wired to `yAxisId="left"` while Revenue and ARPU plot on the right axis (`MEASURE_AXIS[activeMeasure]`), so on those measures it bound to a mounted axis with no series and no domain. Fix: `yAxisId={MEASURE_AXIS[activeMeasure]}`, key per measure. Marker month is the event's own `e.date` (month T, unlagged) on every measure; off events produce no marker. Display-site pin 6 → 6. Traps 174 (Revenue) / 175 (ARPU), per measure. The marker's month is pinned structurally, not read from the DOM (jsdom paints no label); Jon's walk is the DOM evidence.

**D5-08 — Events summary "Show all / Show fewer" (`8c0cb71`).** The panel's table is capped at `max-h-[320px]` with internal scroll. A header control beside the chip, opt-in by prop (`showAllToggle`, WhatIfTab only — the component is shared with Compare, which is unchanged), rendered only while the panel is open and only above `SHOW_ALL_THRESHOLD = 9` rows (a documented row-count proxy for the pixel cap — jsdom reads every element as 0×0); `aria-expanded`, `aria-controls="events-summary-scroll"`; view state only (not exported, not persisted). Keys in all six locales (de *Alle anzeigen / Weniger anzeigen*, it *Mostra tutti / Mostra meno*). `EventsSummaryTable.tsx` added to guard-trap TARGETS. Trap 176 (control present, effect dead). No spec asserts Compare's absence of the control.

**D5-09 — the EFFECT column and the "Events in effect" card (sessions A `3d3ef3d`, B `ae58aca`, C `d63e67a`).** "Active" is the switch's word; the app now says what each event is doing to the chart in front of the user, in one word: **effect**. (i) KPI card: title *Events in effect*, number = volume-path applied count (unchanged expression), caption *moving volume · N switched on* with N from `summaryRows.filter(r => r.enabled).length` (a derived-row read, no new predicate call). (ii) Events summary table gains an EFFECT column after the switch, before CARD; ONE function `effectStatusOf(row, appliedIds, zeroCoverageIds, appliedArpuIds, arpuCandidateIds, notAppliedHereIds)` in `forecasting.ts` decides every row, rules in order: 1 `!enabled` → **off**; 2 in `appliedIds` → **volume**; 3 in `appliedArpuIds` → **arpu**; 4 in `arpuCandidateIds` → **superseded**; 4b in `notAppliedHereIds` → **not-applied-here** (Compare-only, a parameter not a wrapper); 5 in `zeroCoverageIds` → **no-coverage**; 6 otherwise → **no-coverage** (5 and 6 kept distinct on purpose). `EFFECT_LABEL_KEY` maps status → key; opt-in prop `effectOf`. **ARPU is measured, not inferred:** per month the What-If engine records `appliedArpuIds` (every yield winner at sites 2 and 5; every pricing event applied at sites 6 and 8) and `arpuCandidateIds` (every yield event that passed the scope match at sites 2 and 5). *Superseded* = candidate in some month, winner in none (an early roll-forward yield event WINS its first month and reads ARPU; two same-month yield events are split by input order — sort stability). Pricing has no winner — sites 6/8 apply every match — so Superseded is yield-only. `zeroCoverageEventIds` is COHORT-scoped (`computeAdjustedForecast` is given the loaded cohort's dimensions, not the viewing bar); the label never says "view". `impactSummary` now returns the applied SET beside its size. (iii) Compare: ONE exported `eventRowId(raw) = String(ID ?? Name ?? '')` replaces six inline derivations (three readers, the engine at `scenarioHelper:238`, and two churn-path sites at `:452/:464`) — Compare's flat row shape now carries all four arrays; `ScenarioCompareTab` derives the unions per file and passes `effectOf`; Compare's `appliedArpuIds` is a SUBSET of What-If's (no retention-yield site, no per-scenario pricing), so a retention yield event reads **Not applied here** in Compare and ARPU in What-If. "Per-scenario pricing" is NOT a kind of event (site 8 is a second consumer of the same pricing events) — the second limb of the rule was not built. Locale keys: `whatif_events_in_effect`, `whatif_effect_caption`, `whatif_summary_col_effect`, `whatif_effect_{volume,arpu,no_coverage,off,superseded,not_applied_here}`, all six locales; ARPU and Volume added to the i18n ALLOW list with justification (pins 52→54 entries, 184→192 pairs) rather than mistranslated. Traps 177 (applied union emptied), 178 (winner never recorded), 179 (candidates never recorded), 180 (arrays dropped at the flat shape), 181 (one reader minting random ids — breaks only the ID-less file). `ScenarioCompareTab` is MOUNTED for the first time (spec:event-toggle, Worker stubbed, file delivered through `onmessage`). Walk on the 09:13 save: card 4 · 5; Volume ×4, Off ×5, ARPU ×1; campaign on → 9 · 10.

**The trap registry:** not ordered by id; `spec:trap-anchors` prints `next free trap id` (max + 1) and asserts ids unique BY NUMBER. `guard-traps.ts` is excluded from `spec:survival`'s dereference count and from `spec:ai-hold` (its quoted mutation text is its job). guard-traps cannot complete inside one foreground call; it runs to a FILE as standing — the docs-session "no background tasks" rule does not apply to the gate, and a docs-only session does not run guard-traps at all.

**The AI-approval hold (`c84256f`):** stands (Jon). `ai-capability` is an ANCESTOR of main (frozen pre-removal snapshot; `git diff main...ai-capability` is EMPTY — a false all-clear); no AI call site ever existed, only declarations. `spec:ai-hold` enforces EXPECTED.md §33's three criteria plus three identifiers (`@google/genai`; `GEMINI_API_KEY`; the vite define) over TRACKED files, pinned exact both ways with the derivation hash; APP_URL OUT of scope (kept deliberately in `432837d`); gitignored hits are an ADVISORY (`_archive/v4.3-extracted` is a whole pre-removal app copy, mirrored in an agent worktree). Traps 172/173. README.md:278 documents the gitignored `_archive/` and is not stale.

**The prompt surface (audit 1202, applied 1215 `86d9a1f`):** CONTROL, not ritual — 0 dated idioms, 5 caps boosters in 2,188 lines, every emphasis traced to an incident. Applied: the three guard-traps rule copies reconciled; the compact instruction's nonexistent file removed; hand-enumerated `spec:*` replaced by `npm run suite`; merge steps retired; qa-tester renumbered; the missing bullet restored. KEPT: report-writing's description (R3 — the rule leaked four times; the enumeration is cheap insurance). §6/§7 moved to a "branch sessions only" appendix — direct-to-main is the settled mode. `.claude/settings.json` enables only `claude-api@anthropic-agent-skills`; all other plugins DISABLED (user file) — leave them off. CLAUDE.md § Efficiency rules carries the python ban (`050c285`), traceable to reports 0918 and 1241.

## 2. Working agreement

*As at v3.3.11 §2 in full. Additions:*
- **The event list in a brief is read from the FILE, not from memory of the walk** (0747's brief omitted the save's fourth event and mis-stated the third's presence in the earlier save).
- **A reproduction fixture asserts it can discriminate before a result is read from it** — percentage on a basis of 100 (2003), constant inflow for a previous-month pool (1602), a table not in the DOM until its tab is clicked (0747), a target at exactly 20 for a rounding test (1602).
- **Guard-traps output goes to a FILE and per-trap lines are quoted** — never through `tail` (2003 relied on the exit code).
- **"Full suite" is `npm run suite`'s figure** from `e5f1e79`.
- **UAT observation log:** through D4-03 as at v3.3.11; **D5-01 CLOSED `6802cba`** (unit missing on the promotion table and summary); **D5-02 WITHDRAWN** (the sign of Base ARPU Δ at All is the yield event's −0.27, verified `e5f1e79`; the promotions contribute ~0.001); **D5-03 OPEN, to confirm** (edit-restore of a percentage promotion shows the unit control on Subs — Jon's 05 Sep screenshot; unconfirmed whether the toggle was touched); **D5-04 OPEN, measure** (a promotion is editable from the Volume card's events table as a plain market event — what save from there does to its arms is unmeasured). **REQ-D3-01 built and walked. Decision 6 (percentage promotion) BUILT `945d648`, walked `945d648`. Decision 3 (Dilution) BUILT `90e2162`.** REQ-D3-02 backlog. Re-observation = new diagnosis.

*As at v3.3.12 §2 in full. Additions:*
- **A pinned "measured, not endorsed" check must go RED before it is re-aimed** — its red line is the signal the decision landed, and it is quoted in the report (1950).
- **Capture every writer** — which writer fires says which editor took the row without spying on a handler.
- **UAT observation log:** through D5-02 as at v3.3.12; **D5-03 CLOSED `ede9b43`** (walked 2026-09-05: % pressed on reopen, save keeps +10%, no leak into a new promotion); **D5-04 CLOSED `2864d60`** (walked 2026-09-05: the Volume pencil lands on the Promotion editor with the arm restored). Re-observation = new diagnosis.

- **A check reports; it does not guard** — a `check(rows.length === 2)` in front of `rows[0]` still throws on an empty array.
- **A check that has not been seen red has not been seen** — the first id-uniqueness check passed on a planted duplicate; every trap is seen red by hand against a pre-plant md5 before it is trusted.
- **Measure an event with an instrument it can move** — an ARPU-only event read by a volume delta reports "nothing happened"; a marker measured by the series it sits on reports success while absent; a row count under a height cap reports success under any cap.
- **A collapsed panel is fixture, not evidence** — open it before asserting its rows.
- **Docs-session rules are for docs sessions.** No inline `python -c`, no background tasks, 30-second abandon: minted for docs sessions on Windows. They do not apply to the gate; a build session runs guard-traps to a FILE as standing; a docs-only session runs `npm run suite` only.
- **A brief pasted with an unfilled placeholder is recorded, not filled.** The session names it and proceeds where no edit depends on it (1341).
- **UAT observation log:** through D5-06 and REQ-D6-01 as at v3.3.14 (REQ-D6-01 CLOSED `bd50f27`, walked 2026-09-07). **REQ-D6-01 CLARIFIED `e4fd63f`** (chip counts all rows). **D5-07 CLOSED `45a437a`** (walked 2026-09-07; logged `7b43e73`). **D5-08 CLOSED `8c0cb71`** (walked 2026-09-07; logged `7118e5b`). **D5-09 CLOSED `d63e67a`** (A/B/C 2026-09-08; walked; logged `9b58b48`). Re-observation = new diagnosis. Alessandro's own re-observation of D5-07 in de/it is a watch.

## 3. Settled decisions — do not reopen

*As at v3.3.11 §3 in full (cite by heading; the record audit of 2026-09-03; the never-re-propose list under its heading; "percentage on Custom Promotion" is DECLINED-UNTIL-THE-ORDER-IS-DONE and the order is now done — status recorded beside the 2026-08-02 entry as REOPENED 2026-09-03, BUILT 2026-09-04).*

**Jon, 2026-09-05:**
- **A promotion's price change stays baked into its ARPU (option a).** Duration on the promotion pricing arm is not built; revisit with REQ-D3-02 after UAT. Recorded beside decision 3.
- **The ARPU Delta card subtracts then rounds** — built `e5f1e79`.

*As at v3.3.12 §3 in full.* **Jon, 2026-09-05:** **D5-04 option 1** — recorded in EXPECTED.md beside the D5-04 measurements: a promotion is edited from its source; the Volume table's pencil routes to the Promotion card; the row stays listed. Built `2864d60`.

**Jon, 2026-09-05:** D5-05 and D5-06 — recorded `8e79708`. **Jon, 2026-09-06:** **REQ-D6-01 — PER-EVENT ON/OFF**, eight decisions as in §1 — recorded `a23d8bf`. **Jon, 2026-09-07:** the AI hold stands; APP_URL excluded from spec:ai-hold; direct-to-main is the settled mode; plugins that don't serve the project stay disabled; **model sizing → option (b) shaped as the 1202 trial**: three archived build briefs re-run on Fable 5.1 AND Opus 5 from the same BASE, graded by the project's instruments (gate; traps red by hand; findings-reported-not-built; FOR ADVISOR conformance; vacuous-check rate), 1857's mounted-padlock brief as calibration, ~6 gated sessions, AFTER UAT; **the python ban lives in CLAUDE.md** as one traceable line (`050c285`); **the Events summary chip counts all rows** — recorded `e4fd63f`; **D5-07** markers on all three measures, month T unlagged, off events excluded — recorded `58f136b`; **D5-08** Show all / Show fewer, header, only when scrolling would occur, default capped, view state only, summary panel only — recorded `c9e3da4`. **Jon, 2026-09-08:** **D5-09** — the pivot word is *effect*; the four-then-six states as in §1; card *Events in effect / moving volume · N switched on*; card tables OUT; Compare widened to show the same statuses; (iii) resolved by **option 1** (one shared `ID ?? Name` derivation; the ID-less same-name collapse recorded as a watch, not fixed); Compare-only *Not applied here* for kinds Compare's engine never applies — recorded `ff38d5a`, `58a3cf6`, `d260fbd`; **the Fable trial's second arm** (Fable 5.1 as orchestrator over Opus/Sonnet subagents) is considered only if the first arm shows a measurable difference on the project's instruments; **each trial arm records its weekly-usage delta** (read from the usage panel before and after) so the quota question is measured, not estimated.

## 4. Standing rules

*As at v3.3.11 §4 in full. Minted 2026-09-04/05:*
- **A magnitude cannot survive a percentage; a rate can** — revenue goes to 0, ARPU is baked (`draftEventRate` already said so).
- **A pool is observable only where it feeds** — assert a pool's size where it reaches the screen (the re-banded pool: Base at T+1), never where a sibling construction happens to agree.
- **A mechanism that exonerates the suspect is a finding, not a licence to build** (0747 built nothing under an item that said "fix").
- **A claim about "the same events" is checked against the file's event list** before it enters a brief.
- **A spec that prints no report line has said nothing** — the suite runner's CRASHED state.
- **Recount, don't carry** — a baseline count is measured at the commit it pins (91, not the earlier 87).

*As at v3.3.12 §4 in full. Minted 2026-09-05:*
- **An editor that cannot show what it is editing drops it silently** — one editor per event shape.
- **A disabled control with no reason is a defect of its own** (the campaign pill).
- **A dependency array declares every read, even the ones that happen to work** (three promo builders).

*Minted 2026-09-05/07 (v3.3.14):* the four in §2 above; **an id is unique by the part that keys other checks**; **a title attribute is not a rendered reason**.

*Minted 2026-09-07:* a hung Claude Code turn with the network reachable is checked for expired auth FIRST (`claude auth status`); plugins that don't serve the project stay disabled; no inline `python -c` and no background tasks on Windows in a docs session; any tool call not back in 30 seconds is abandoned and named (docs sessions; the gate is exempt); a wedged runtime is restarted from a fresh shell after killing leftovers, never waited out; a check that quotes its own forbidden strings excludes the files whose job is to carry them; **never invoke `python` in a session — hash and verify with bash/md5sum** (the Store stub `WindowsApps\python.exe` hung at startup; aliases disabled 2026-09-07; `python --version` now answers 3.14.6 in PowerShell; a session's bash was not retested, so the ban stands); **a rule that lives only in a report's Limits is not operative** — the next session learns it by hitting it again.

*Minted 2026-09-07/08 (this document):* **survival has two figures and both are reported** — 104/26 first-row dereferences/files, 27/27 checks — never conflated; **i18n-parity counts CHECKS, not keys** — adding a key to all six locales moves the key count (report it per locale), not the parity figure, and only a new allowlist entry moves it; **an identical translation goes on the ALLOW list with a justification, never mistranslated to make a number green**; **line numbers in a report drift by the previous session's own edits — re-check before relying on them** (A→B ~5 lines, B→C again); **a status is a measurement, not an inference** — session A's carrier-inferred ARPU was removed in B; **absence from a set is not a state**; **do not build a class that does not exist** — "per-scenario pricing" is a consumer, not a kind (C); **a shared component gets a decision by opt-in prop, never by unconditional render** (`showAllToggle`, `onSetEnabled`, `effectOf`); **one function decides a row's status — a wrapper is a second place**; **a join keyed differently on two sides fails silently as a confident wrong answer** — one exported derivation, and a trap that breaks only the ID-less case; **a walk's step zero is Jon's ritual** — reload on localhost from Claude Code's browser, the named save, the expected row count — no npm commands in a walk brief; **the walk's expected outcome is written in the brief before the walk** (card 4 · 5, Volume ×4 …) so a pass is a comparison, not an impression.

## 5. Development history (condensed)

**Everything through D4-03: CLOSED at `670c7c3`** — v3.3.11 §5.

**The promotion arc (2026-09-04 → 05, CLOSED at `e5f1e79`):** *1526* (`d92fdaa`: v3.3.11 committed `1c1f224`; step 2 was NOT done at `2ecdefb`; revenue 0 / arpu baked; +10% promotion = +10% plain event to the penny; the Retention half shipped without mounted coverage, stated) → *2003* (`945d648`: retention fixture 400/100 because 100 is non-discriminating; the re-banded pool observable at Base T+1 only — third session to pay; the mix has no per-tier volumes; round-trip 106; traps 150–152) → *Jon's walk of `945d648`* (percentage promotions apply at leaf and All by the leaf's delta — 8,000 + 71.46 + 48.28 in both tooltips; D5-01 seen; D4-02/D4-03 re-checked green; W4 confirmed via the ARPU chart on 05 Sep) → *2138* (`90e2162`: Dilution rides the percentage arm through the already-extracted `dilutionAmountPct`; duration structurally unbuildable; the Pricing spec caught a reuse) → *0747* (`6802cba`: D5-02 does NOT reproduce — configuration table, rate amplification linear, the sign is the yield event's; D5-01 built; 2dp-before-subtract found) → *1602* (`e5f1e79`: yield pool verified by hand at three views/two months on a varying-inflow fixture; subtract-then-round; `npm run suite` committed, 59/59; `spec:survival` 91/24; `applied-count` re-aimed for D3-04's read-set).

**Everything through the promotion arc: CLOSED at `e5f1e79`** — v3.3.12 §5.

**D5-03 / D5-04 (2026-09-05, CLOSED at `2864d60`):** *1903* (`ede9b43`: v3.3.12 committed `510eb70`; D5-03 confirmed worse than the screenshot — silent conversion on save and a leak into the next promotion; three faults from one gap, fixed; D5-04 measured — arms preserved, rate zeroed; three options recorded) → *Jon's walk* (D5-03 green) → *1950* (`2864d60`: option 1 built through one router; the pinned check red first; the campaign pill found dead and made to route; traps 159/160) → *Jon's walk* (D5-04 green).

**Coverage, D5-05/06, REQ-D6-01 (2026-09-05 → 07, CLOSED at `bd50f27`):** *2031* (`e177d62`) → *2252* (`cb09643`) → *2005* (read-only inventory) → *2027* (`fc718bf`: predicate, pins, columns, summary switch, Compare at the engine, round trip; four traps mis-numbered) → *2128* (`bd50f27`: duplicates renumbered; the id check seen red; four card switches; campaign switch with mixed) → *Jon's walk* (REQ-D6-01 green).

**2026-09-07 — docs, audit, AI hold, python, D5-07, D5-08, chip:** *0918* (v3.3.14 committed `86a0957` by hand after the session hung — a hung `python -c` was later found still running as a background task and killed) → *1202* (prompt audit, read-only) → *1215* (`86d9a1f`, applied) → *1230/1241* (`c84256f`, spec:ai-hold; traps 172/173) → *Jon* (Store alias confirmed and disabled) → *1341* (docs: rule absent from the tracked surface; base drift routine) → *1412* (`050c285`: the python line into CLAUDE.md) → *Alessandro* (markers missing on Revenue/ARPU) → *1436* (`45a437a`, D5-07; traps 174/175) → *walk* → *1515* (`7b43e73`) → *Jon* (ten rows, no way to see them all) → *1533* (`8c0cb71`, D5-08; trap 176) → *walk* → *1658* (`7118e5b`) → *2127* (`e4fd63f`: two controls, chip vs bar).

**2026-09-08 — D5-09, the EFFECT column:** *0915* (read-only inventory: applied set discarded; no ARPU set; zero-coverage cohort-scoped; Compare drops both sets) → *A 0943* (`3d3ef3d`: card, column, four states, allowlist; STOP on the id join; trap 177) → *B 1027* (`ae58aca`: `appliedArpuIds` / `arpuCandidateIds` at sites 2/5/6/8 and Compare's 10/12; Superseded defined; traps 178/179) → *C 1116* (`d63e67a`: `eventRowId` at six sites; flat shape widened; *Not applied here*; Compare mounted; traps 180/181) → *walk* (A–F green) → *1202* (`9b58b48`).

## 6. Current state and what's next — THE RESUME POINT

**Base for the next session: the Repo line of `reports/2026-09-08-1202-d5-09-walk-closed.md` — `9b58b48`.** A reports-only fill commit ahead is routine and is named, not treated as drift.

**FIRST ACTS OF THE NEXT ADVISOR CHAT (if rotated):** (1) check the UAT log — findings go ahead of everything; (2) confirm the queue; (3) confirm the image/context state of the advisor chat itself — this chat is held open deliberately for context; it closes at the image limit or at the first sign of degraded answers.

**THE QUEUE (UAT closing ~15 Sep; findings first):**
1. **Further UAT findings** — ahead of everything below. Alessandro's re-observation of D5-07 in de/it; the users' first reactions to D5-08 and D5-09.
2. **Hold.** No pre-close build is queued; the advisor waits for findings or the close. Read-only inventories and docs sessions are permitted.
3. **After UAT, in order:** the Fable trial (calibration first; each arm records its usage delta; second arm only on a measured difference); DQ true-state (first act records the UAT re-scope and DQ-after-UAT in EXPECTED.md) then DQ build; then: guide translation pass incl. on/off, D5-05's reason, percentage promotions, Show all, the EFFECT column and card; churn hold/revert; Compare tariff fixture then the last blend; strictNullChecks count; the two byte-identical campaign pills → one component; the three inline amount-mode controls; `EventOnOffSwitch.tsx` into TARGETS with a trap of its own; the campaign switch driven on the Volume card's pill and in the marker spec; REQ-D3-02 composite design note; **new from 07/08 Sep:** a one-line check in `events-summary-spec` pinning the chip to `rows.length`; a spec asserting Compare shows no Show all control; the ID-less same-name collapse (a campaign's rows share one `eventRowId` when the sheet has no ID column); Compare's ARPU coverage narrower than What-If's (no retention-yield site) — surfaced as *Not applied here*, unfixed; same-month yield ties decided by input order (a tiebreaker would silently change which is Superseded); the Compare mount rests on a Worker stub; the `Enabled` column's effect on an OLD app version opening a new file (compatibility note in the guide).

**Product decisions surfaced, awaiting Jon:** none open. (The "Active Market Events" card's on-count question was answered by D5-09's caption.)

**Standing UAT watches:** `EventOnOffSwitch` has no trap of its own; the campaign switch is driven on one card's pill; the summary panel is collapsed by default; the de/it native read of: D5-05's reason, *Alle anzeigen / Mostra tutti*, *Wirkung / Effetto* — and in German **Menge** (EFFECT label, caption) sits beside **Volumen** (CARD chip, measure tab): two words for one concept on one row, to be settled by the native reader against TERMBASE.

**An advisor must NOT draft fix sessions for** (closed on main): everything in v3.3.13's list, plus D5-05/D5-06 (`cb09643`), REQ-D6-01 (`fc718bf`, `bd50f27`, clarified `e4fd63f`), the python rule (`050c285`), spec:ai-hold (`c84256f`), D5-07 (`45a437a`), D5-08 (`8c0cb71`), D5-09 (`3d3ef3d`, `ae58aca`, `d63e67a`).

## 7. Backlog

*(As at v3.3.11, plus:)* three inline amount-mode controls; `fmtDelta` one-decimal vs `eventVolumeLabel`; the Value card's `rollForward` boolean vs the Pricing card's duration enum; the round-trip spec uses builder-shaped literals (a `.ts` spec cannot import the card); the promo Dilution control and gating; non-compounding copy; `spec:survival`'s regex comment stripper; the suite sentinel; the anchor extractor's method list.

- **Fixtures:** the discriminating two-leaf store (per-scenario bands, retention volume, varying inflow 200/300/400 on the yield case); a second store seeded retention 400/100 for percentage retention promotions; the trimmed fixture's three Product L2 tiers for mix checks. Walk saves by sha1: 03 Sep 08:01 `56f5e7e0…` (two market events, NO yield event); 04 Sep 09:55 (two market events + the yield event; sha1 unrecorded); **04 Sep 21:37** (four market events + yield; in Jon's `Downloads/`, not the repo; sha1 unrecorded).

*As at v3.3.12 §7 in full.* Fixtures: the D5-04 mount carries Retention rows with real value tiers and an ARPU column (the shared props pass `''` for both and cannot produce tiers). Walk saves: unchanged; the 04 Sep 21:37 save (in `Downloads/`) is the D5-03/D5-04 walk subject.

*Plus:* the id/anchor registry ordering; `spec:survival` counts quoted anchor text as dereferences (4 in `guard-traps.ts`); the `Enabled` column and old app versions; the after-UAT items in §6 item 3. Walk saves: **07 Sep 09:13** (ten rows: four volume-moving events on — one a promotion — one ARPU-only Value event on, a five-row campaign off; the artefact for REQ-D6-01, D5-07, D5-08 and D5-09; in `Downloads/`; sha1 unrecorded); **04 Sep 21:37** (five rows, used in the D5-09 Compare walk). `Get-Command python` on this machine resolves to an install-manager alias (version 0.0.0.0) that dispatches to Python 3.14.6 — irrelevant to sessions while the ban stands.

---

*Maintenance: at each session close, the advisor updates this document, commits it to `docs/` (superseded copy removed in the same commit), and Jon replaces the project-context copy (latest only — git holds the lineage).*
