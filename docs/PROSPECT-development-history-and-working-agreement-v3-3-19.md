# PROSPECT — Development History & Working Agreement

**Purpose of this document:** persistent context for the PROSPECT Claude project. Every advisor chat reads this before its first turn. Replace this file in project context whenever it is updated — a stale copy silently applied is worse than no copy. The document is ALSO committed to the repo (`docs/`); project context carries only the latest, git holds the lineage. **This document is a MAP. For settled decisions, EXPECTED.md verbatim is the terrain.** **A prior report's claim is a premise for the next brief, not a fact; a guide's claim is the same class; a report's "not reproduced" is not evidence of absence; an observation from two different saves is two observations; a line number in a report is a locator that drifts — re-check it before relying on it; a report with a placeholder certifies nothing; an advisor's hand-computed expectation is checked before it disputes a report.** **Every decision this document relies on is written here in full or cited by its EXPECTED.md HEADING.**

**Last updated:** 2026-09-17 (v3.3.19 — v3.3.18 carried verbatim; additions for 2026-09-16 (after the REQ-D6-07 inventory) through 2026-09-17 under *Additions (2026-09-16 → 17)* markers: REQ-D6-07 built and walked (clauses 6–16), the rates-column defect, the stored ARPU basis, REQ-D6-08 INITIATIVES decided/built/walked (clauses 1–18), three releases queued and one made, the walk/guard-traps tree rule. **Released to UAT: `a497ff5` is the release candidate — FULL guard-traps 270/270 (2026-09-17), suite 76/76, walked F/G/H/I/J; Maddie's deploy of it not yet confirmed at close; her last confirmed deploy is `1bc094b` (`fdc5852` and `ab2c634` were sent between — which she took is unrecorded).** Last gated `src` **`a497ff5`** — **guard-traps FULL 270/270 (0 MISSED / INCONCLUSIVE / CRASHED; ledger hash `8079186`, src byte-identical; next free trap id 276)**, **suite 76/76**, trap-anchors 288/288 (270 traps, 283 anchors), i18n-parity 203/203 (947 keys per locale, six locales), survival 104/26 and 27/27, view-apply-mounted 208/208, tsc 0, lint and build clean. **Pins as at `a497ff5`:** last-column Market/Yield/Pricing **6/4/4** (`Initiative` last on all three; Market: Enabled / Tariff_Scope / Hold / Mode / Tariff_ARPU_Basis / Initiative), `computeAdjustedForecast` sites 6, `eventScopeSeriesFor` callers 5, `solveForCohortTarget` definitions 1, `buildPromoEvents` in WhatIfTab 5, `resetYieldDraft()` calls 2, `handleDeleteCampaign` callers 3, `setPendingChange` staging sites 7 (one `campaign`, one `initiative`), `campaignToggleState` 1, `handleSetEventEnabled` definitions 1 (invocations 6), `initiativeGroups` 1, `handleSetInitiative` 1, `carryInitiative` sites 3, `{ initiative }` writes 3, `handleDeleteInitiative` 1, `initiativeKey` 1 / layout reads 2 / table reads 2; runIngest 3, apply 12, display 6, ramp/hold checkboxes 4 as before. Provenance for 2026-09-16/17: the advisor handover of 2026-09-16 (reports 0647, 0706, 1008, 1019); reports 1222, 1313, 1432, 1459 (not seen by the advisor — cited from 1937), 1606 (+ addendum), 1937 (2026-09-16), 0647, 0841, 0953, 1021 (2026-09-17); Jon's Walks F, G, H, I, J reconciled against screenshots; the 09 Sep 14:25, 16 Sep 14:29, 17 Sep 07:45 and 09:37 saves read directly by the advisor; hashes from reports only. v3.3.18's own header follows.) **Previously:** 2026-09-16 (v3.3.18 — v3.3.17 carried verbatim; additions for 2026-09-11 (after `7206ff0`) through 2026-09-16 under *Additions (2026-09-11 → 16)* markers: D5-05-held, the 50MB caption, REQ-D6-05 built and released, REQ-D6-06, the guard-traps selector, REQ-D6-07 decided. v3.3.17's own header follows.) **Previously:** 2026-09-11 (v3.3.17 — a FULL document: v3.3.16 (self-contained, assembled 2026-09-09 after `f252700`) carried verbatim in blocks marked `[INLINE v3.3.16 §N]`, plus 2026-09-10 and 2026-09-11 in full — REQ-D6-03 (ramp then hold, all three carriers), REQ-D6-04 (200MB, pre-parse notice, session-file routing), the B8 stale closure, the B10 sticky banner, the promotion preview, the six checkboxes, the held-rows measurement, and the REQ-D6-05 decisions (spread vs ramp). Nothing in the inline blocks was rewritten; every 2026-09-10/11 addition sits under an *Additions* marker in its section.) **UAT IS LIVE — opened 2026-09-01; Alessandro is still filing findings past the 15 Sep target. Released to UAT `296618c` (11 Sep) and `1bc094b` (15 Sep). Open as at 2026-09-16: REQ-D6-07 (decided, inventory in session).** Last gated state **`7206ff0`** — **guard-traps 218/218 (0 MISSED / INCONCLUSIVE / CRASHED after one crashed run restored from backup; next free id 223)**, **suite 67/67**, trap-anchors 230/230 (218 traps, 225 anchors), i18n-parity 200/200 (keys 892 per locale, six locales), survival 104/26 and 27/27, view-apply-mounted 208/208, tsc 0, lint and build clean. **Pins as at `7206ff0`:** apply 12, display 6, last-column Market/Yield/Pricing 3/2/3 (`Hold` last on Market after `Tariff_Scope`), `runIngest` sites 3, `p_eventPools.push(` 4/3, `type="checkbox"` ramp/hold controls 6, `computeAdjustedForecast` sites 6, `eventScopeSeriesFor` callers 3. Provenance for 2026-09-10/11: the advisor handover of 2026-09-10 (`c11151e` as its last gated code, reports 0705–1147); reports 1208, 1218, 1556, 1651, 1907, 2014, 2059, 2146 (2026-09-10) and 0225/0839 (2026-09-11); Jon's Walks B and C reconciled against screenshots and the 10 Sep 16:50 and 20:33 saves (both read directly by the advisor); hashes from reports only. **Between v3.3.16 (`f252700`) and `c11151e` sit D5-14, D5-15 and Compare's pools (reports 0705–1106, 2026-09-10) — NOT condensed here; the handover document and those reports are the record until a docs session condenses them.**

---
## 1. What PROSPECT is

`[INLINE v3.3.16 §1]`


PROSPECT (Predictive Reporting Of Scenarios & Planned Execution for Commercial Trends) is a React/TypeScript subscriber forecasting application for Vodafone, built entirely through Claude Code — Jon directs, writes no application code himself. Rebuilt around real IBRO data (Inflow, Base, Retention, Outflow).

- Three-step workflow via ForecastContext: Baseline Forecast → Market Events → Actuals Review; plus **Scenario Compare** (up to 4 session files; own parser feeding `computeScenarioForFilter` — a SECOND ENGINE with the only blended-ARPU computation left). Top-nav: **Overall Forecast**.
- Models: SES, Holt Linear, Damped Trend, true Holt-Winters. Scoring: symmetric absolute percentage deviation + band position.
- **Base volume is never fitted or stored per month** — reconstructed at read time from the seed rolled through flows. **All four per-scenario ARPUs are fitted and banded on every forecast month.**
- Dimensions: Customer Segment + Product/Channel/Tariff L1/L2; 7-part keys. 'All' is a DATA VALUE with TWO representations (`null`, the string `'All'`); ONE predicate `eventScopeMatchesView` (11 + 4 pinned callers; structural zero-occurrence check on the hand-rolled shape).
- **Event carriers — THREE** (`MarketEvent`, `YieldEvent`, `PricingEvent`); pipeline market → yield → pricing. **A promotion is a `MarketEvent`** built by `buildPromoEvents`; its price change is **baked into its own `arpu` at build time** and it never enters the pricing pass (Jon, 2026-09-05: stays baked; duration NOT built — a Recurring control would be a stored mode nothing reads; the spread is the promotion's multi-month mechanism).
- **How a market event reaches a view (as at `2ecdefb`, unchanged):** applicability by the shared predicate; weighting by `forecastCoverage` (percentage) / `eventProRataShare` (absolute) under the null/0 contract; coverage-0 → `zeroCoverageEventIds`.
- **Pools feed the ARPU of their own scenario (`2ecdefb`):** Inflow pool → Inflow ARPU; **re-banded Retention pool → Base at T+1 ONLY** (Retention ARPU is built by `scenarioPools`, which never reads `p_eventPools` — paid for three times, now in EXPECTED.md); the synthetic yield pool is built from the PREVIOUS month's inflow and feeds Base via the lag (**verified by hand `e5f1e79`**: Base ARPU Δ = Σ delivered size × (rate − baseArpu) / stock, at T+1, T+2 and All). ONE pool arithmetic since `d92fdaa` — the promoRebanded pool sized by `resolvedEventVolume(…, 'retention')` at `idx` (this was NOT done at `2ecdefb`, contrary to v3.3.11 §1).
- **Percentage promotions (`d92fdaa`, `945d648`):** `buildPromoEvents` takes `amountType`; for a percentage, `revenue` is 0 (a magnitude cannot survive) and `arpu` is baked (a rate survives) — the same rule `draftEventRate` already encoded; `percentageBasis: 'baseline'`; Inflow basis = the view's fitted inflow, Retention basis = the view's fitted retention for the month, both through the ordinary market-event path (a +10% promotion moves a view exactly as a +10% plain event — pinned). **The mix carries NO per-tier volumes** — it is a pricing device, one blended rate; the pool is sized from the resolved delta and priced at the mix blend. Round-trips through `Amount_Type` / `Percentage_Basis`; a promo row without `Amount_Type` loads as absolute. **The unit control on the promotion arm is a third inline copy** (three amount-mode controls, none shared; trap 122 is global(2) over the shared label expression). Volume % and price % are lexically separated (keys `whatif_promo_volume_pct_label/_help`).
- **Promo Dilution (`90e2162`):** `promoPricingMode` gains `'dilution'` riding the percentage arm through `dilutionAmountPct` (ONE function, 8 call sites); equality with the Pricing card pinned to 1e-9 three ways; columns `Promo_Dilution_Current_Pct/_Target_Pct` appended; no Target / Applies-to (decisions 4/5); applied once at build — cannot compound (unstated on screen). The promotion got its own block predicate rather than a third caller of `pricingDraftBlockReason`.
- **Display:** `eventVolumeLabel` owns the percentage form of a stored volume on three surfaces (`+10%`); absolute stays per-caller; the Volume tab's `fmtDelta` (`+10.0%`) is a remaining duplication. **The KPI ARPU Delta card subtracts UNROUNDED figures and rounds once** (`e5f1e79`); the 2dp `chartData` columns are untouched.
- **The mix controls** as at v3.3.11 §1 (`MixSliderRow`, `MixTargetPanel`, `MixPctBox`, locks persist, `rebalanceToTarget`, the wall, minimum change, exactly-determined).
- **The Step-2 chart, the four-delta KPI card, the per-scenario pricing baseline, R7**:
  - **The Step-2 chart is a GRID** (MEASURE × SCENARIO pills, baseline SOLID / adjusted DASHED). **The KPI ARPU Delta card shows FOUR per-scenario deltas** (`20799e3`); absence renders an em dash; end-of-period, so a flow event in an earlier month reads 0 on its own flow and non-zero on Base (walked: Base +0.14 at All, flows +0.00).
  - **The pricing card's Baseline ARPU is per-scenario, event-scoped** (`20799e3`): `pricingBaselineArpu`, ONE definition, exactly two callers; Σrev/Σvol never averages. `ARPU (Adjusted)` column persists with zero readers in `src/`.
  - **Churn mode (R7)**: the carrier is a TARGET RATE. Q1 (hold/revert) queued.
- **Locales:** six, parity 194/194 (`I18N_PHASE2`/`TRANS_BACKLOG` never re-matched; `.ts`/`.tsx` asymmetry — backlog). No `strictNullChecks` (backlog).
- **The gate (as at `e5f1e79`):** `guard-traps` CAUGHT / MISSED / INCONCLUSIVE / CRASHED with its own controls; global-mutation class; `spec:trap-anchors`; **`npm run suite`** — serial, each spec captured to a file, GREEN / FAILED / CRASHED by report-line sentinel, guard-traps a separate step; **`spec:survival`** baselines first-row dereferences per file (91 over 24 files), exact both ways, comments stripped.
- Business users: Alessandro Russo and Marcel Wiegand. Roadmap: Cloud Run + BigQuery, AI Booster ML, four AI use cases; AISHA event schema.

*Additions (`ede9b43`, `2864d60`):*
- **A promotion is edited from its source (Jon, 2026-09-05, option 1).** The Volume table lists promotion-built rows (unchanged) but its row pencil and its campaign pill route through ONE router (`editEventFromVolumeTable` / `editCampaignFromVolumeTable`): `isPromotion` → switch to the Promotion tab first, then `handleEditPromoStart` / `handleEditPromoCampaign`; otherwise the Volume form. The Volume form is never opened for a promotion-built row (its patch-spread save preserved the arms but zeroed the baked rate, 36 → 0 — measured 1903). The Volume card's campaign pill for a promo campaign previously rendered as a disabled span with no reason; it now routes.
- **Edit-restore of a promotion restores `amountType`** in both edit paths and `resetPromoDraft` clears it (D5-03: one writer, the toggle; a reopened +10% promotion saved back as ten subscribers). All three promo builders now declare `promoAmountMode`, `promoMixLocked` and the dilution pair in their dependency arrays (D3-04's shape).
- `computeTierData` returns `[]` unless a revenue or ARPU COLUMN is named; a mix with zero tiers correctly disables Save (fixture facts, recorded at the spec).

**Per-event on/off (REQ-D6-01, Alessandro, built `fc718bf` + `bd50f27`).** Every carrier extends a shared base with `enabled?: boolean`; **absent means ON** (every pre-existing save reloads unchanged). ONE predicate `isEventOn` (`forecasting.ts`) is the only definition of "off"; it reads BOTH the typed `enabled` and the raw `Enabled` column because Compare's engine holds sheet rows and never calls `marketEventFromRow`. **Exactly 12 apply sites** (8 What-If, 4 Compare) pinned by marker text; **exactly 6 display sites** (tooltip ×3, month markers, retention warnings, churn exclusion set) pinned apart. A third pin counts reads of `.enabled` outside the predicate and its writers: **5 in `forecasting.ts` + 1 in `WhatIfTab.tsx`** as at `d63e67a`, every one of them a read of a DERIVED summary row, never of an event carrier — the honest wording is "zero reads that constitute an apply decision", not "zero reads elsewhere" (0918 caveat). The pricing self-exclusion needed no change (it routes through `computeAdjustedForecast`). `Enabled` appended LAST on all three sheets, `'Yes'/'No'`, reader `=== 'No' ? false : true`. ONE `EventOnOffSwitch` (`role="switch"`, `aria-checked`, `mixed`), rendered first in the events summary row and in all four card tables; off rows greyed by `OFF_ROW`, written once; a promotion's two rows share one field; the campaign pill carries one switch on the group's first row — on/off sets every row through `handleSetEventEnabled` (exactly 5 invocations pinned), mixed renders `aria-checked="mixed"` and a click on mixed turns the campaign ON; switching is not editing, so the percentage-campaign bar does not block it. Compare drops at its engine, so its panel lists an off event greyed via the shared table's read-only mode. The term is "on"/"off", never "scenario".

**Three counts on the Market Events step are three different populations, by design (REQ-D6-01 decision 6, clarified by Jon 2026-09-07, EXPECTED.md `e4fd63f`):** the **summary bar's** badge (`ForecastSummaryBar.tsx`, `.filter(isEventOn).length`, trap 169) counts events that are ON; the **Events summary panel's** chip ("N events", `EventsSummaryTable.tsx`, `rows.length`) counts ALL rows, on and off — it has always done so and no spec asserts it either way; the **KPI card** counts events applied via apply site 1 (volume path — `appliedEventIds` feeds it, so a Value/Pricing event never enters it); the Metadata sheet counts all rows. On the 07 Sep 09:13 save: chip 10, card 4, five on.

**D5-05 (`cb09643`):** a campaign containing a percentage row stays group-edit-barred (the ramp reverse-engineering sums volumes); its rows are editable one at a time; both campaign pills render the existing reason `whatif_campaign_decline_percentage` as TEXT with `aria-disabled`. A row edit writes exactly one row by id. The two pills remain byte-identical JSX (trap 163 on the Volume copy only). **D5-06:** the dilution effect line renders the incomplete and out-of-range reasons distinctly (asserted en and de). **Coverage (`e177d62`):** the dilution control driven end to end with a non-zero guard in front of the ratio check; the campaign route driven on an absolute campaign; the KPI precision string pinned (`"+0.01"`).

**D5-07 — event month markers on all three measures (`45a437a`).** The marker list was never wrong; the `<ReferenceLine>` was hard-wired to `yAxisId="left"` while Revenue and ARPU plot on the right axis (`MEASURE_AXIS[activeMeasure]`), so on those measures it bound to a mounted axis with no series and no domain. Fix: `yAxisId={MEASURE_AXIS[activeMeasure]}`, key per measure. Marker month is the event's own `e.date` (month T, unlagged) on every measure; off events produce no marker. Display-site pin 6 → 6. Traps 174 (Revenue) / 175 (ARPU), per measure. The marker's month is pinned structurally, not read from the DOM (jsdom paints no label); Jon's walk is the DOM evidence.

**D5-08 — Events summary "Show all / Show fewer" (`8c0cb71`).** The panel's table is capped at `max-h-[320px]` with internal scroll. A header control beside the chip, opt-in by prop (`showAllToggle`, WhatIfTab only — the component is shared with Compare, which is unchanged), rendered only while the panel is open and only above `SHOW_ALL_THRESHOLD = 9` rows (a documented row-count proxy for the pixel cap — jsdom reads every element as 0×0); `aria-expanded`, `aria-controls="events-summary-scroll"`; view state only (not exported, not persisted). Keys in all six locales (de *Alle anzeigen / Weniger anzeigen*, it *Mostra tutti / Mostra meno*). `EventsSummaryTable.tsx` added to guard-trap TARGETS. Trap 176 (control present, effect dead). No spec asserts Compare's absence of the control.

**D5-09 — the EFFECT column and the "Events in effect" card (sessions A `3d3ef3d`, B `ae58aca`, C `d63e67a`).** "Active" is the switch's word; the app now says what each event is doing to the chart in front of the user, in one word: **effect**. (i) KPI card: title *Events in effect*, number = volume-path applied count (unchanged expression), caption *moving volume · N switched on* with N from `summaryRows.filter(r => r.enabled).length` (a derived-row read, no new predicate call). (ii) Events summary table gains an EFFECT column after the switch, before CARD; ONE function `effectStatusOf(row, appliedIds, zeroCoverageIds, appliedArpuIds, arpuCandidateIds, notAppliedHereIds)` in `forecasting.ts` decides every row, rules in order: 1 `!enabled` → **off**; 2 in `appliedIds` → **volume**; 3 in `appliedArpuIds` → **arpu**; 4 in `arpuCandidateIds` → **superseded**; 4b in `notAppliedHereIds` → **not-applied-here** (Compare-only, a parameter not a wrapper); 5 in `zeroCoverageIds` → **no-coverage**; 6 otherwise → **no-coverage** (5 and 6 kept distinct on purpose). `EFFECT_LABEL_KEY` maps status → key; opt-in prop `effectOf`. **ARPU is measured, not inferred:** per month the What-If engine records `appliedArpuIds` (every yield winner at sites 2 and 5; every pricing event applied at sites 6 and 8) and `arpuCandidateIds` (every yield event that passed the scope match at sites 2 and 5). *Superseded* = candidate in some month, winner in none (an early roll-forward yield event WINS its first month and reads ARPU; two same-month yield events are split by input order — sort stability). Pricing has no winner — sites 6/8 apply every match — so Superseded is yield-only. `zeroCoverageEventIds` is COHORT-scoped (`computeAdjustedForecast` is given the loaded cohort's dimensions, not the viewing bar); the label never says "view". `impactSummary` now returns the applied SET beside its size. (iii) Compare: ONE exported `eventRowId(raw) = String(ID ?? Name ?? '')` replaces six inline derivations (three readers, the engine at `scenarioHelper:238`, and two churn-path sites at `:452/:464`) — Compare's flat row shape now carries all four arrays; `ScenarioCompareTab` derives the unions per file and passes `effectOf`; Compare's `appliedArpuIds` is a SUBSET of What-If's (no retention-yield site, no per-scenario pricing), so a retention yield event reads **Not applied here** in Compare and ARPU in What-If. "Per-scenario pricing" is NOT a kind of event (site 8 is a second consumer of the same pricing events) — the second limb of the rule was not built. Locale keys: `whatif_events_in_effect`, `whatif_effect_caption`, `whatif_summary_col_effect`, `whatif_effect_{volume,arpu,no_coverage,off,superseded,not_applied_here}`, all six locales; ARPU and Volume added to the i18n ALLOW list with justification (pins 52→54 entries, 184→192 pairs) rather than mistranslated. Traps 177 (applied union emptied), 178 (winner never recorded), 179 (candidates never recorded), 180 (arrays dropped at the flat shape), 181 (one reader minting random ids — breaks only the ID-less file). `ScenarioCompareTab` is MOUNTED for the first time (spec:event-toggle, Worker stubbed, file delivered through `onmessage`). Walk on the 09:13 save: card 4 · 5; Volume ×4, Off ×5, ARPU ×1; campaign on → 9 · 10.

**The trap registry:** not ordered by id; `spec:trap-anchors` prints `next free trap id` (max + 1) and asserts ids unique BY NUMBER. `guard-traps.ts` is excluded from `spec:survival`'s dereference count and from `spec:ai-hold` (its quoted mutation text is its job). guard-traps cannot complete inside one foreground call; it runs to a FILE as standing — the docs-session "no background tasks" rule does not apply to the gate, and a docs-only session does not run guard-traps at all.

**The AI-approval hold (`c84256f`):** stands (Jon). `ai-capability` is an ANCESTOR of main (frozen pre-removal snapshot; `git diff main...ai-capability` is EMPTY — a false all-clear); no AI call site ever existed, only declarations. `spec:ai-hold` enforces EXPECTED.md §33's three criteria plus three identifiers (`@google/genai`; `GEMINI_API_KEY`; the vite define) over TRACKED files, pinned exact both ways with the derivation hash; APP_URL OUT of scope (kept deliberately in `432837d`); gitignored hits are an ADVISORY (`_archive/v4.3-extracted` is a whole pre-removal app copy, mirrored in an agent worktree). Traps 172/173. README.md:278 documents the gitignored `_archive/` and is not stale.

**The prompt surface (audit 1202, applied 1215 `86d9a1f`):** CONTROL, not ritual — 0 dated idioms, 5 caps boosters in 2,188 lines, every emphasis traced to an incident. Applied: the three guard-traps rule copies reconciled; the compact instruction's nonexistent file removed; hand-enumerated `spec:*` replaced by `npm run suite`; merge steps retired; qa-tester renumbered; the missing bullet restored. KEPT: report-writing's description (R3 — the rule leaked four times; the enumeration is cheap insurance). §6/§7 moved to a "branch sessions only" appendix — direct-to-main is the settled mode. `.claude/settings.json` enables only `claude-api@anthropic-agent-skills`; all other plugins DISABLED (user file) — leave them off. CLAUDE.md § Efficiency rules carries the python ban (`050c285`), traceable to reports 0918 and 1241.


**REQ-D6-02 — the delta-month selector and the Revenue Delta card (Alessandro, 2026-09-09; built `9c93807`, `0e67708`, `360a591`).** ONE month selector at the top of the KPI strip, shared by the delta cards; options = every forecast month carrying no actual, **chronological, earliest at the top** (Jon's clarification — "most recent at the top" had been recorded as latest-first; he meant nearest-first); default = the last forecast month, DERIVED (`options.includes(selected) ? selected : last`), so a remount has nothing to reset; the whole horizon, never the chart window (the 6/12/18/24M buttons drive only the Brush); view state only. `computeAdjustedForecast` persists per month `baselineBaseVolume`, `adjustedBaseVolume` and `baselineRevenue.{inflow,outflow,retention,base}`, produced ONCE where `perScenarioColumns` computes them (it returns `{columns, baselineRevenue}` and the 2dp columns round THAT value); the Base Volume Delta card moved onto the unrounded pair (an e5f1e79 correction — may differ by a penny); export unmoved at 29 row columns. `ScenarioDeltaCard` — ONE component used twice (ARPU, Revenue), public testids preserved by an explicit `groupTestid` prop; Revenue reads `scenarioArpu[k].revenue − baselineRevenue[k]`, rounded once, em dash for absence. ONE predicate `monthsCarryingActuals(rows, dateCols[], valueCol) → Set` (the import modal's test extracted; the modal's inline copy retired; Step 3's cohort-scoped map is a different question and untouched). "(end of period)" had exactly two readers; both keys retired; titles carry `{{month}}` via the locale-aware formatter. Traps 188 (selector ignored), 189 (actual months offered). **Traps 190/191 (Revenue rounding) were written, planted, stayed green — every revenue on the fixture lands on a whole penny — and removed; their numbers were later reused by D5-10's yield half, so the registry comment names both.** The Revenue card's subtract-then-round rule is correct by construction and UNGUARDED until a sub-penny fixture exists; the order is pinned by the spec and by no trap. Walked on the 09 Sep 14:25 save: Jun 2028 default at +15.72K; Sep 2026 Base +0.00 then Oct 2026 +2.50K at SOHO / Mobile Voice (the lag, visible).

**D5-10 — tariff scope on the event (Jon, 2026-09-09; halves `7850298`, `fb51506`, yield `7a0bc1d`).** Finding: a Promotion saved with Tariffs in scope = RED M + RED L and its Tariff control at All applied its full uplift at the viewing bar's RED S — the event carried the literal `'All'`, which both predicates read as unnarrowed; the engine never received `selectedTariffs`. **Design (b), not a build-time split** (a split freezes at save time a share recomputed per month per view today, and multiplies a campaign's rows). The shared event base (`EventToggle`) gains `tariffScope?: string[]`; ABSENT means all tariffs, so every existing save reloads unchanged. ONE `tariffScopeFor(draftTariffL1, selectedTariffs, fullL1s)` records a scope only when the draft says All AND the selection is a non-empty STRICT subset (sorted); applied at EVERY emitter — no carrier funnels through one state writer (`setMarketEvents` has eleven callers of four kinds, two of them RESTORE which must never recompute) except Promotion, which funnels at `buildPromoEvents`, and Value (yield), which has ONE construction site — **`TARIFF_SCOPE_SITES` = 10 (WhatIfTab 9 + App 1), pinned by marker text, exact both ways**; App's `addMarketEvent` is the FIFTH WRITER, counted this time. ONE `tariffScopeAdmits(scope, viewTariff)` inside `eventScopeMatchesView` and `leafWithinScope` (so `eventProRataShare` and `forecastCoverage` weight over the in-scope leaves only); a view of All still sees a scoped event. `Tariff_Scope` JSON column appended LAST on all three sheets (empty, never `[]`, when absent); ONE reader `tariffScopeFromRow`; the `Enabled`-is-last pin re-aimed to name both positions. Compare: `scopeOf` and three match sites parse the column through the one reader; site 10 (yield) gained the field in the yield half; its `ProRataLeaf` carries `tariffL1`. Display: ONE `tariffAllLabel` helper asking `tariffScopeFor` itself renders "All in scope (RED L, RED M)" on all three Tariff controls (new `allLabel` prop on `HierarchicalDropdown`), plain "All" at a full selection; `scopeOf(dims, t, tariffScope)` appends the list in the SCOPE cell, Compare's panel included (shared builder). ONE hook `useClearTariffOnDeselect` replaced two structural copies and covers the Promotion draft (which had none). The two `EventScopeDims` declarations merged. **Yield reading (1)** (Jon): a Value-card event saved under a strict subset applies only to the in-scope tariffs whatever its mix axis; the card has no Tariff control, so the draft is treated as All. Re-saving recomputes the scope from the current selection — by design. Traps 182 (predicate), 183 (weighting — green until a weighting fixture existed), 184 (reader), 185 (Compare `scopeOf` — green until a RED M share case), 186 (an emitter skips the call → pin red), 187 (label helper — green until a DOM read), 190 (Value emitter), 191 (Compare yield site — green twice). Walked: RED S nothing, RED M +4.63K and RED L +2.56K summing to the ramp at All; labels; auto-clear; export/import; Compare. **Not done:** the real Value add path is not driven (the Add button has no testid and is gated on tier data). Watches: markers still draw at an out-of-scope view (keyed to on events, not to the view); the collapsed control truncates "All in scope (RED L, R…"; `EventToggle` now carries two cross-carrier facts under a name that describes one; one-helper-per-card deferred.

**D5-11 — the Value card reconciled to the chart (Alessandro + Jon, 2026-09-09; `6538350`).** The card's Baseline / New Blended ARPU are the yield RATIO's denominator and numerator (equal-weight comparator, "SETTLED 2026-08-12 — the comparator is option (c)", unchanged), cancelled before the forecast: `yieldArpu = fittedArpu × (share-weighted blend ÷ equal-weight blend)` at sites 2/5 and Compare 10. Measured: the same +10% target on either axis gives ratio 1.100000 and the same chart delta; an ABSOLUTE target on Historical basis gave 1.10 vs 1.816 by axis. Decided: the basis toggle defaults to **Forecast ARPU** (an initialiser, not a decision; Historical still selectable) — on Forecast basis the equal-weight blend of the scaled tiers IS the fitted mean, so an absolute target means the same thing on both axes; a **Preview Impact** box in the Pricing card's shape reads the chart's own columns (Inflow reads the month AFTER the draft's; Retention the draft month) through the existing seam — `eventScopeSeriesFor` gained `yieldDraft` / `excludeYieldId`, the draft SPLICED into the list, NO second engine (pinned: `computeAdjustedForecast` sites still 6; `eventScopeSeriesFor` callers 2 → 3); a caption states the ratio mechanism (six locales). **The comparator pair with a stated rate present is by construction:** an override REPLACES the derived rate (R3), breaking the scaling identity — Jon's 12.94 reproduced as 12.9653 from the save's override — recorded, not fixed; the preview beneath is unaffected. Traps 192 (default reverted), 193 (preview reads the blend), 194 (draft never spliced — green until the spec required the splice term). **Incident:** the session restored a plant with `git checkout --` and destroyed its uncommitted build; rebuilt verbatim from the transcript, tsc clean, 69/69; the next session diffed the five edits against the report — no divergence. Walked.

**D5-12 — "Events in effect" counts the union (Jon, 2026-09-09; `9118678`; supersedes D5-09 (i)).** The card read 0 with "Add events below" while a yield event moved Inflow ARPU +6.70. Number = |volume-applied ∪ `appliedArpuIds`| across `adjustedMonths` (`unionHere`, no third pass); caption `{{v}} moving volume · {{a}} moving ARPU · {{on}} switched on`, ALWAYS rendered; the invitation only at on = 0; `switchedOnCount` derived ONCE (a second `summaryRows.filter` copy was caught by the `.enabled` pin). `whatif_effect_caption` retired ×6, replaced by `_full`. Traps 195 (union drops ARPU), 196 (zero branch hides the caption). Walked: 4 · 3 · 1 · 8; 2 · 0 · 2 · 6.

**D5-13 — the preview names the yield winner (Jon, 2026-09-09; `f252700`; month format 2113 CERTIFIED `1254aff`).** Two Inflow yield events, both 2026-09, on one cohort ("test save W3" All/All first-saved non-roll-forward; "inflow draft" SOHO / Mobile Voice later, roll-forward). **The tie rule, measured through both engines on the 19:30 save:** candidate filter `roll ? month <= prev : month === prev` (a non-roll-forward event is a candidate in ONE month); sort by month desc, `[0]`, NO tiebreak, scope a filter not a rank — so W3 wins Oct (−5.26%) and the draft wins from Nov (+93%); both engines agree every month; EFFECT reads ARPU for both (each wins somewhere; Superseded never fires); the preview read Oct, the one month the draft loses, and said nothing. Decided: **the tie rule is UNCHANGED**; options (a) most-specific scope wins, ties by latest saved, and (b) latest saved wins (needs a sequence column on the yield sheet) are an OPEN design question for after UAT — both change existing saves' forecasts; (c) blocking DECLINED (it refuses a working file). Built option (d): the seam returns `arpuIdsByMonth` beside `series` (ONE run into a local; Pricing callers read `.series` only — argued by unchanged assertions, not a differential run); `yieldPreview` filters to this run's yield ids and FINDS the draft's first win in the series; the rival line `{{month}}: '{{name}}' applies instead · this event applies from {{first}}` and a superseded line, six locales, testids `yield-preview-rival` / `-superseded`; months through `fmtMonth` (2113). Traps 197 (seam drops the ids), 198 (winner looked up at the wrong month). Walked on the 19:30 save: the line names W3 for Oct and Nov as the draft's first win over 10.65 → 10.09; W3 off → no line, 10.65 → 20.58 matching the chart's Oct tooltip. **Nothing pins the tie rule itself.**

**The trap registry as at `f252700`:** 194 traps, next free id 199; ids 190/191 carry a burnt-then-reused note (tidy when the registry is next touched for its own sake). `spec:trap-anchors` caught a session's own aging of an anchor SIX times across 09 Sep (118, 131, 181, 186, 108, 126, 157, 134) — it is the instrument that makes same-session edits safe.

*Additions (2026-09-10/11):*

**REQ-D6-03 — RAMP THEN HOLD (Alessandro; Jon's decisions 2026-09-10; EXPECTED.md heading "REQ-D6-03 — RAMP THEN HOLD (Jon, 2026-09-10)", clauses 1–10). Built on all three carriers: Volume spread `8728893` (session 1, report 1218), churn `ac9b633` (session 2, 1556), Promotion spread `9bbb389` (session 3, 1651).** Hold is MATERIALISED AS ROWS at build, one row per month, to the LAST FORECAST MONTH AT BUILD TIME (clause 8: regenerating a longer horizon does not lengthen a held campaign; re-save to extend); no carrier field; apply sites, Compare and pools unchanged. ONE shape function `spreadShape` (Volume, Promotion via `buildPromoEvents` — its own `pct/total` retired in session 3 — and churn through `statedReductions`); ONE `holdPlateauStart` for restore on every carrier; ONE `horizonMonthsFrom` (`adjustedMonths`-derived; `horizonMonths` REQUIRED on the shape params since `477b171` — a `?? 0` default silently disabled the tail); ONE `Hold` column (`'Yes'/'No'`) appended LAST on Market_Events after `Tariff_Scope` — the last-column pin now names THREE positions (Enabled / Tariff_Scope / Hold), seen red before the re-aim; reader absent → off, so every earlier save reloads unchanged; ONE key pair `whatif_hold_after_ramp_label/_help` on all three cards. **With Hold ON the entered figure is the TARGET and the ramp is linear-cumulative to it** (clause 2: 10% over 3 = 3.33 / 6.67 / 10 then 10; 2,500 over 3 = 833 / 1,667 / 2,500 then 2,500; custom shares cumulated); **with Hold OFF the existing split is byte-identical** (clause 3: a total distributed by share over N months, then nothing — a hand-written six-literal differential pins it). **Clause 7 (ratified 2026-09-10):** a percentage draft may be spread ONLY under Hold ON; percentage amounts are not rounded. **Churn holds POINTS (clause 9):** every held month states the same reduction against its own unadjusted rate; measured adjusted÷baseline outflow 0.9089 → 0.9177 over months 3–20 on the two-leaf fixture (0.97% spread, RISING — a compounding tail FALLS, trap 207 measured 0.909 → 0.000), pinned at 1.0% with the direction asserted separately. Edit-restore (clause 5): toggle from the column, target from the last row, ramp length = the first row whose figure equals the last row's; a held campaign is ONE campaign (pill, switch, D5-05's bar, Show all). The Promotion card emits ONE row per month (the brief's "two rows" was wrong; REQ-D6-01's "a promotion's two rows share one field" needs re-measuring — a retention-linked promo is the likely referent). Traps 202–211 (202 tail not emitted; 203 Hold ON emits the split; 204 reader treats absent as Yes; 205 the Hold-OFF arm altered; 206 churn tail; 207 churn compounds; 208 churn restore; 209 promo tail — re-anchored twice in two sessions as the seam moved; 210 promo row loses hold; 211 promo restore). **The Add button under Hold reads its count** ("Add 22 Events"; Promotion "Add Promotion · N events"); the unheld churn label ("Add Event" for a 3-month ramp, pre-existing) is FROZEN because `spec:mix-card` finds that button by its text — backlog: testid, then the label.

**Held rows reach Base as ordinary rows (measured on the rendered card, report 0225/0839, `7206ff0`):** on the 20:33 save at Corporate / Mobile Voice, a held Retention 300, a held Inflow 3000 and a held Promotion 3000 (ramp 3 Even) each emit 22 rows (Sep 2026 → Jun 2028; the campaign starts at index 2 of the 24-month horizon); the KPI Base Volume Delta at month k = the sum of rows 1..k−1 (each campaign's own first month reads +0.00; Jun 2028 = 6,000 on 6,300 and 60,000 on 63,000); Volume-inflow and Promotion-inflow identical at all 22 months — the promo re-banded pool and `Contract_Length` exonerated. The advisor's 5,700 / 57,000 expectations were a hand-arithmetic slip (the ramp rows sum to 6,000, not 3,000); Jon's +54,000 reading was a subtraction against a stale "before" (the deleted B8 rows). No fix.

**B8 (Walk C, 2026-09-10) — `handleAddPromotionEvent` read `promoHold` without listing it (`477b171`, report 2014):** the third stale closure in `WhatIfTab` (churn fold; `handleEditStart`; this). The mounted spec was green because it pressed Hold BEFORE the ramp (which rebuilt the callback); Jon pressed it after. Reproduced on unchanged code by reordering the spec's clicks; both campaign paths gained the two deps; all hold specs now click Hold LAST with the reason at the line; trap 217. **The third-instance rule fired → the read-set rule is to be enforced by the linter (queued): there is NO ESLINT in the repo** — `npm run lint` is `tsc --noEmit`; ten `eslint-disable-line react-hooks/exhaustive-deps` comments suppress a rule that has never run; run once at error over `src/`: 34 violations (WhatIfTab 17, App 12, ForecastVsActualsTab 4, ScenarioCompareTab 1); all three historical sites red under it, `promoHold` named by name (report 2059, `a60e3a1`).

**The Promotion card's preview (`b2b1a9c`, report 2146):** the grid computed `pcts[i]/pctTotal` inline and never read `promoHold` — it showed the hold-OFF split while Add emitted the held shape (the chart was right; the card was not). Now ONE `promoShape` useMemo read by the grid, the button and `buildPromoEvents` (which takes `shape` REQUIRED and computes none; the row-edit path passes a named `SINGLE_ROW_SHAPE`); tail summarised on one line at `promo-hold-tail` ("then +3,000 held through Jun 2028 · 21 months"); grid and emitted rows each asserted against the SAME hand-written literal, never one against the other; traps 219–221. `spec:view-apply-mounted` CRASHED on the refactor because its eight seam calls were cast `as any` — a hole in exactly the guarantee a required parameter makes; all eight now state a shape.

**The ramp/hold controls are real checkboxes (Jon, 2026-09-11; clause 10; `7206ff0`):** the Volume and Promotion cards had rendered both as round-dot `<button>`s side by side — not exclusive (measured mounted 45/45) but the visual vocabulary of a radio group; churn used real checkboxes. ONE component, six `<input type="checkbox">`, existing testids kept; trap 222 (planted green until two discriminators were added; re-planted red). **No amount label changes with Hold on any card** ("Subscriber Volume (+/−)", "Acquisition Volume", "Reduce by (points)" all state the hold-OFF reading; only the help line says TARGET) — resolved by REQ-D6-05's mode control (§3), not by a caption.

**B10 (Walk B, 2026-09-10) — the restore banner was STICKY (`477b171`):** "Restored the first available cohort — this save did not record which one was active" had one writer to true (the fallback) and one to false (the dismiss button); nothing cleared it at the start of an import, so a banner raised by an earlier load survived every later, correct one. Both 10 Sep saves record All/All and RESOLVE (72 leaves) — neither import raises it. `applyImportSaveWorkbook` clears the flag as its first act; `spec:restore-banner` 11; trap 218. The 9c31227 active-cohort mechanism is unchanged and the file-size extraction (below) was exonerated by a byte-identical block diff.

**REQ-D6-04 — FILE SIZE 200MB, PRE-PARSE NOTICE, SESSION-FILE ROUTING (Jon, 2026-09-10; EXPECTED.md heading of that name, `3acb088`; built `fd1ad78`, report 1907).** ONE constant `MAX_UPLOAD_BYTES = 200 * 1024 * 1024` in `src/utils/ingest.ts`, read by all three ingest paths through ONE `runIngest` (pinned: exactly 3 sites; App spells no byte-size literal of its own — structural zero-occurrence). **Import Save and Import Actuals had NO size check at all before this.** Refusal names both figures ("big.xlsx is 250.0 MB. The limit is 200 MB"); a refused file never reaches `read` (stub call count 0). The notice is PAINTED before the synchronous parse — `nextPaint()` nests TWO rAFs (one fires before its own paint) — and the ORDER is the feature: trap 213 (yield removed) leaves the callback order and the structural pin green and reddens only "the notice ELEMENT was in the DOM when the parse began" (the stub reader queries `document` when it runs). The success line carries the measured parse time ("Loaded 145,344 rows in 6.4 s") so the notice's "up to a minute" can be re-worded from a figure someone READ — Jon's Chrome drops of the three scratchpad fixtures (106.7 MB / 145,344 rows; 197.3 MB / 266,464; 215.4 MB / 290,688 = REFUSED) are pending. Session-file routing on the input drop: `isSessionWorkbook` requires BOTH `Metadata` AND `Market_Events` (Metadata alone is a plausible analyst tab name), exact match; the sniff reads `wb.SheetNames` from the workbook this path must parse anyway ("reads names only" and "costs nothing extra" are two claims; only the first is true here, said at the line); routing hands the parsed workbook to `applyImportSaveWorkbook` (extracted from the FileReader callback) — never re-read. Traps 212–216. `spec:import-seam` enumerates `setBaseForecast` sites BY ENCLOSING FUNCTION and went red on the extraction — re-aimed at an exact 2, not loosened. `spec:ingest` 42 registered in the guard-traps CONTROL list with its first trap.

**The guard-traps CONTROL list** must name every spec a trap targets — `HOLDSHAPE` was absent for a session (four traps could catch vacuously); the positive control ("the spec is RED on the unmutated tree — every trap would catch vacuously") refused three first runs in five sessions (mix-card on a renamed label; import-seam on an extraction; view-apply-mounted CRASHED on `as any`). Registration of a new spec in the CONTROL list happens with its first trap.

**Walk saves (2026-09-10):** 08:43 (the Walk B step-zero subject; 13 events); **16:50** (59 Market rows: the seven earlier campaigns + `ramp and hold test` 22 rows percentage held + `B2 test` 22 rows absolute held; Metadata Active_Cohort All, Forecast 2026-07 → 2028-06, 24 months; read by the advisor); **20:33** (16:50 + `B3 churn hold` 22 rows + the three-row B8 promotion; the held-rows-base and lag measurements' subject; read by the advisor). All in `Downloads/`. Walk B: B1 (percentage ramp-then-hold: Aug 2027 Adjusted = Baseline × 1.10), B2 (absolute: 100/200/300 then 300; card +6,000 at Jun 2028), B4 (hold off: 3 rows 100/100/100), B5 (round trip), B7 (Compare) green; B6 by design (the summary panel's leading switch is per-row; the CAMPAIGN switch is the small toggle beside the pill and on the card tables); B3 (churn 0.5/1/2 then 2 held: outflow ratios 0.9465 → 0.9481 rising; this cohort's annualised churn reads 117% — 7,960 outflow on an 81,619 base, a data property) green. Walk C: C1 (both controls at once) pass; C2/C4 the preview defect above (chart correct: Mar 2027 Inflow 13.22K = 9.29K × 1.10 + 3,000; Mar 2028 +10% held); C3 pass; B9 (Volume-table pencil → Promotion editor) pass.

*Additions (2026-09-11 after `7206ff0`, through 2026-09-16):*

**RELEASED TO UAT: `296618c` (2026-09-11, via Maddie) and `1bc094b` (2026-09-15).** The previous deploy was Monday 07 Sep 11:06, at or just before `bd50f27`. Release = push `main` in sync with origin and tell Maddie the hash — after a walk on that exact tree.

**D5-05 lifted for HELD percentage campaigns (Jon, 2026-09-11; `6fd13f8`, report 0742).** The bar is `anyPercentage && !allHeld` (`every()`, so a mixed campaign stays barred); no new function — both pills and D5-04's router read `group.editable`. Two defects sat unobservable behind the bar and are fixed: the campaign hold restore and `handleSaveCampaign`'s rebuild both dropped `amountType` / `percentageBasis` / `retentionLinked` (a +10% campaign would have reopened as 10 subscribers). `spec:d5-05-held` 39, four routes in fill-in order; trap 223 plants the previous release verbatim. The 1651 harness's "no pill on the Volume-table route" was a harness fault (`btnByText('Volume')` matched the measure control, not the tab); D5-04's router is covered.

**REQ-D6-04 measured on the laptop (Jon, Chrome, 2026-09-11):** 106.7 MB in 4.7 s, 197.3 MB in 9.6 s, notice painted before the freeze both times, no "page unresponsive" prompt; 215.4 MB refused with the exact message; a session file on the input drop routed to Import Save. **The drop-zone caption still read "Up to 50MB"** — a locale STRING (French "50 Mo"), invisible to the byte-literal pin. Fixed `17b52d5` (report 0831): every user-facing megabyte figure is a placeholder from `MAX_UPLOAD_MB`; a zero-occurrence check over six locales covers MB/Mo/MiB; the notice reads "usually under 15 seconds"; the routing line had been wiped by `runIngest`'s `finally` (shared setter) — own state now, both lines walked on screen; `spec:d5-05-held` had never been in the guard-traps CONTROL list (223's catch was unguarded; registered); the guard-traps harness restores on a throw and reports CRASHED per trap. **"Last-column 3/2/3" (now 4/2/3) counts the trailing positions each sheet's last-column pin names** — Market: Enabled / Tariff_Scope / Hold / Mode; Yield: Enabled / Tariff_Scope; Pricing: Enabled / Tariff_Scope / Contract_Length_Months — in `hold-shape-spec`, `event-toggle-spec` and `pricing-roundtrip-spec`.

**REQ-D6-05 — SPREAD vs RAMP (Jon, 2026-09-11; EXPECTED.md heading of that name, clauses 1–18; inventory `f664299`/`14d9442`, report 0928; build `9036d33`, report 1006; % labels `fb412a0`, report 1206; 2dp + duration-1 `296618c`, report 1337).** Mode is explicit and PERSISTED: a `Mode` column (`Spread`/`Ramp`) LAST on Market_Events after `Hold`; reader absent → `Ramp` if `Hold` is Yes else `Spread` (rows alone cannot decide: 1,000/2,000/3,000 is a ramp to 3,000 and a spread of 6,000); the last-column pin re-aimed to FOUR positions, seen red first. **Spread:** the figure is a TOTAL; Even / Custom % / **Custom values** (typed absolutes, the total box the derived read-only sum); rows end after N; byte-identical to REQ-D6-03's Hold-OFF arm (the six-literal differential is the Spread pin). **Ramp:** the figure is the TARGET reached at month N; typed per-month values, Even prefill, re-prefilled on target/duration change; Hold ONLY in Ramp; validation blocks Add with a rendered reason when a month exceeds the next or the last ≠ target. **A percentage is always Ramp** (the mode control locks; the Promotion card's ungated share-split of a percentage retired). **The spread on/off switch is RETIRED** on Volume and Promotion — Mode (Spread | Ramp) + Duration (min 1) always open, the preview and held-tail line always rendered; churn keeps its ramp checkbox (clause 16: the checkbox pin is FOUR — three Hold + churn's ramp). Labels by mode: "Total volume — split across {n} months" / "Target volume — reached at month {n}" (+ ", then held"); percentages "Change to {scenario} — reached at month {n}" (clause 15); duration 1 reads "— one month" on both (clause 18). **A stored percentage is exact; its DISPLAY is 2dp everywhere** (clause 17) — `eventVolumeLabel` is the one rule; `fmtDelta` (the duplication recorded since v3.3.11) RETIRED; four surfaces had printed a stored percentage four different ways. Default mode: Spread for Subs, Ramp for %. Restore: mode from the column; Spread reopens with the sum and its distribution (Custom values when the rows fit no share pattern); Ramp with target = last row and the typed values. Old saves (16:50, 20:33) reload as what they were — the 13:00 save read by the advisor: every held row and every churn row `Ramp` (the writer guarantees churn), every unheld absolute campaign `Spread`. Built in one session, twelve specs re-aimed each seen red first; three defects found by building (a single-row % edit showing the order reason; Save Campaign swallowing a broken ramp; an unheld churn row re-exporting as Spread). Traps 225–236. **Watches:** the dilution arm has NO spread (a dilution ramp is NOT built — after UAT if at all); negative percentage campaigns reopen as positive (`Math.abs` in both restores); the decimal separator is "." in every locale; pricing percentages still print as typed or at 1dp (clause 17 read as the volume %); the ramp value INPUTS show 2dp without a unit.

**REQ-D6-06 — CAMPAIGN-LEVEL DELETE (Alessandro, 2026-09-15; EXPECTED.md heading of that name, clauses 1–8; `88e0adb` report 1143; follow-up `1bc094b` report 1258).** ONE `handleDeleteCampaign(name, rows)` (name alone would merge a Volume and a Promotion campaign sharing a name), exactly 3 callers pinned: the Volume table pill's bin, the Promotion table pill's bin, and the Events summary panel (opt-in prop `onDeleteCampaign`; Compare passes nothing and renders no bin). ONE dialog — `EventChangeConfirmModal` gained a `campaign` kind naming the campaign and its row count with the Now/After/Change flows — committing the previewed array in one setter call. **Ramp, held and churn campaign MEMBERS are neither row-deleted nor row-edited** (`isCampaignStepMember`, ONE predicate extracted from the churn edit bar; 7 read sites pinned): the bin reads "Use the campaign bin", the pencil "Use the campaign pill", both `aria-disabled` NOT natively disabled (a dead click would redden trap 107's "a refusal is a stated reason") — the edit bar sits at the row entry `editEventFromVolumeTable`, not in `handleEditStart`, because the campaign editors seed their drafts through that handler. Spread members and single events keep both controls. Every per-row delete goes through the dialog (the Promotion table's didn't); `removeMarketEvent(` occurs 0 times in `WhatIfTab` (structural zero). **Corrected fact, wrong twice in this chat: the Events summary panel has NO campaign switch** — REQ-D6-01 put campaign switches on the two card tables; the summary has per-row switches; the bin sits on the campaign's first row beside the row switch. Traps 237–244; 160 re-anchored. Walk E (Jon, 2026-09-15) E1–E7 green.

**Guard-traps: timed, targeted, ledgered (reports 1919, 2109, 2229; harness `ed4dd8e` → `e7c88aa` → `0af35c6`; `src` untouched at `1bc094b`).** A FULL run is 42 min: 86% spec runs, plants and restores ~0.5 s in total, the positive control 14% (364 s); 13 of 55 specs are 80% of the time; every mounted spec pays an ~8 s module-loading floor per trap (tsx 1.3 s, jsdom 1.9 s, `forecasting.ts` 1.4 s, `WhatIfTab` graph 2.5 s); slowest spec run `campaign-delete-mounted` 20.3 s. **`--changed` by target file would have saved only 20–30%** (`WhatIfTab.tsx` and `en/translation.json` change every session). Built instead: **anchor-in-hunk selection** — each trap's anchor located by EXECUTING its `mutate()` on a copy and diffing (all 240 located; the unparseable-anchor class gone) and selected when the span intersects a `-U2` hunk since BASE, or its target spec changed, or it was added/edited (text hash), or its anchor text moved; plus a **rotation of 20 oldest-run** from the tracked ledger `scripts/guard-traps-ledger.json` (advanced only for traps that ran); the positive control cached by sha256 of control specs + TARGETS + harness + registry (pays only within a session); NOT RUN a state, never in the ratio; the certification line "guard-traps targeted N/N CAUGHT (ids …), rotation 20 (ids …), NOT RUN M, last FULL run <hash> <date>". Measured: a targeted run 11.0 min (32/32, NOT RUN 208) against 42; the five prior sessions would have selected 78 / 10 / 17 / 9 / 12 traps. Batching per spec NOT built — masking (the 1206 crash pair). **FULL is the default (`npm run guard-traps`); `-- --targeted` / `-- --base <rev>` opt in; `--full` a no-op alias; a build session's gate is targeted; a release, a docs "last gated state" and the session-close skill run FULL; the session commits the ledger with its report.** Wide-span traps 122 (6,888–9,267) and 227 are selected in almost every WhatIfTab session — conservative, ~17 s. The ledger's lastFullRun: `e7c88aa` 2026-09-15 240/240.

**Walk saves:** 11 Sep 13:00 (the REQ-D6-05 round trip; read by the advisor — `Mode` column verified), 11 Sep 14:32 (release walk R1–R6), 15 Sep 13:55 (Walk E round trip). All in `Downloads/`.

*Additions (2026-09-16 → 17):*

**REQ-D6-07 — THE VALUE CARD LEADS WITH THE COHORT ARPU (Alessandro, 2026-09-15; EXPECTED.md heading of that name, clauses 1–16; built `3510cc9`, `fe0cb7e`, `fdc5852`, `ab2c634`; walked F1–F6, G1–G6, W1–W3, the reset step):** the lead pair is the cohort's fitted → delivered ARPU at the read month; the equal-weight blends demoted to a collapsed "how this is computed" line (the comparator option (c) NOT reopened); the Target box means the cohort ARPU, solved by `solveForCohortTarget` (ONE definition, `mixConstraint.ts`) — the UNROUNDED series, tolerance 0.005, cap 30, closed-form first guess then bisection; `yieldRatioFrom` ONE helper for the summary's ratio cell ("Inflow ARPU +52.1%"), the pair in its title (opt-in prop; Compare none). **The read month is a property of the CARRIER:** a yield event reads the month AFTER the draft's for Inflow (its pool enters Base at T+1) and the draft month for Retention; **an Inflow PROMOTION reads its OWN month** (clause 13, option A) — a multi-month campaign reads the draft's first month with that month's volume; the caption "with this promotion's {n} subscribers at this mix". The seam `eventScopeSeriesFor` gained `marketDraft` / `excludeMarketIds` (callers 5); the promotion `deliver` builds rows through `buildPromoEvents` (ONE row builder). Measured (1008): delivered ARPU is monotone and near-linear in the promo blend; the effect shrinks with volume (3,000 subs +2.68; 300 subs +0.31). **Defects found by building, fixed `fe0cb7e`:** Forecast-basis tier rates were scaled to the LOADED cohort's forecast, not the draft's (a SOHO draft with Corporate loaded read 31.08 where SOHO's gives 34.65) — both cards' tier derivation resolves the draft's own slice via `draftScopeForecast` → `resolveEventScopeForecast`; `MixTargetPanel` fell back to the BLEND band when the cohort band was null; `spec:ai-hold`'s exclusion named `v3-3-16` exactly (by pattern now). **Clause 14 (option A, `fdc5852`): the ARPU basis is STORED on the event** — `Tariff_ARPU_Basis` ('Historical'/'Forecast') appended LAST on Yield_Events and after `Mode` on Market_Events (mix rows only, `''` otherwise); ONE writer and ONE reader per sheet; absent → Historical, so every pre-clause-14 save reopens as saved and D5-04's no-change save holds ("changed fields: NONE" — the 1019 red was 36 → 35.6); reopen restores the stored basis on row edit, campaign edit and yield edit; a NEW draft opens on Forecast on both cards. **Clause 16 (`ab2c634`):** after a Value-card Add the next draft opens on Forecast — ONE reset `resetYieldDraft` (the twin of `resetPromoDraft`), called by Cancel and both Save dispositions; a chosen basis never carries from one added event to the next. Facts corrected: the mounted card's cohort figures are 16.91 / 33.13 / 14.23 / 15.86 (the 1008 harness literals used all-row rates); the padlock mount's band is 3.83–27.49.

**REQ-D6-08 — INITIATIVES ON THE EVENTS SUMMARY (Alessandro, 2026-09-16; EXPECTED.md heading of that name, clauses 1–18; built `6c12fab` (session 1), `c9ff73b` (session 2), `8422374` (clause 17), `a497ff5` (clause 18); walked H0–H6, I0–I8, J0–J3):** a grouping ABOVE campaigns, across all three carriers, on the Events summary only. **Storage:** `initiative?: string` declared once on `EventToggle` (every carrier extends it); an `Initiative` column (a name; `''` = none) appended LAST on Market_Events, Yield_Events and Pricing_Events by the three existing writers, read by the three existing readers through ONE trimmed parse `initiativeFromCell`; the initiative IS its rows (no members, no initiative; old saves load with none; no Initiatives sheet). **Carry:** `carryInitiative(rebuilt, replaced)` at ALL THREE campaign saves — Volume, Promotion AND the churn save (a third row-rebuild the inventory missed) — so rebuilt rows and added months keep the initiative; row edits keep it by merge (mounted (h)). **Layout:** `EventSummaryRow` gained `initiative` and `campaignName` (builder order UNCHANGED — Compare pins it); ONE pure `initiativeGroups(rows)` WhatIfTab-side returns `header | row` entries, a group placed at its EARLIEST member's position in today's order, members in today's order inside it (the Value/Pricing members pulled up from their passes), ungrouped rows unmoved; `EventsSummaryTable` renders entries and builds nothing. **The header row:** name, "{n} events", the tri-state switch (`EventOnOffSwitch` mixed; `campaignToggleState(members)`; click → `handleSetEventEnabled` per member by its OWN pass — no new predicate, no new writer), the Effect cell as a per-state count in the column's order with empty states omitted ("Volume ×3 · ARPU ×1"), Rename (pencil, inline), Dissolve (no dialog; clears the field, rows untouched), the bin. Badge counts events; the Show-all threshold (9) counts every visible entry, headers included (it stands in for height). **Controls (opt-in props; Compare passes none):** **Select** enters selection mode — a tick per event, ONE tick per campaign (`campaignUnit`, defined once, feeds tick and Ungroup: same pass, card and campaignName), headers have no tick; the bar "{n} selected" with a name input, a pick-list of existing names, **Group as…** and Cancel; **Ungroup** per member row (a campaign member ungroups the whole campaign). **ONE setter `handleSetInitiative(members, name | '')`** — per member `update{Market|Yield|Pricing}Event` by its own pass, the sibling of `handleSetEventEnabled`; Group as, Ungroup, Dissolve and Rename are that function applied to a member set; the table writes no events. **Names:** trimmed on entry; compared by `initiativeKey` = trim + lowercase (ONE helper, `forecasting.ts`), read by the layout's grouping key and by the table's one resolver `existingInitiative`; a match adopts the EXISTING casing — Group as an existing name ADDS with no prompt; **Rename onto a DIFFERENT initiative prompts "Merge into '{name}'? {n} events will join it"** (an inline panel, testids `events-summary-merge-dialog/-title/-confirm/-cancel`; nothing written until Merge) and merges into the existing casing; **Rename to the initiative's OWN name in another casing RE-CASES every member, no prompt** (clause 18); identical name = nothing; a save holding two casings renders ONE header under the first-seen casing and its switch moves both. **The bin:** dialog kind `'initiative'` (title/blurb/confirm keyed, six locales; `common_cancel` reused); `pendingChange` carries `nextYield` / `nextPricing`; the preview varies all three arrays (the same two engine calls; `computeAdjustedForecast` 6); confirm writes Market rows as the campaign bin does and calls `removeYieldEvent` / `removePricingEvent` ONCE PER Value/Pricing member — no whole-array setters, no new App props (clause 8); `handleDeleteInitiative` stages (the 7th staging site); a Market campaign editor open on a binned member is closed. **Compare:** an Initiative column after Name, pipeline order, no header, no switch, no bin (clause 15). **i18n:** 17 new keys (`whatif_summary_col_initiative`; `whatif_summary_select/_selected/_tick`; `whatif_initiative_group_as/_name_placeholder/_pick_existing/_ungroup/_dissolve/_rename/_rename_save/_merge_title/_merge_confirm`; `whatif_delete_initiative_title/_blurb/_confirm/_bin`) — 947 per locale; the parity allowlist 55 / 194 ("Initiative" is the word in de/fr). Specs: `spec:initiatives` 140 checks (a)–(t) + structure pins on the promo-cohort-target/spread-ramp-volume harness with all three carriers in real state; traps 261–275. **Limits (recorded):** the merge prompt's "{n} events" is not plural-aware ("1 events"); two SINGLE Market events with the same name tick as one unit (`campaignName` falls back to the name on read — how the pills already group them); Value/Pricing editors open on a binned member are not closed (their own row bins behave the same); the bin's preview cannot show what Value/Pricing removal changes (the dialog shows flows) — asserted on the engine call; `toLowerCase()` is not locale-aware; Compare shows each row's stored casing; a rename differing only by surrounding spaces is nothing; the ID-less same-name collapse (D5-09's watch) affects switch/ungroup by id exactly as today's per-row switch does — exports always write `ID`.

**Walk saves (2026-09-16/17):** 09 Sep 14:25 (re-read: the three-row `test tariff mix retention promo` campaign, ARPU 13.43 = 33,575 ÷ 2,500, RED L override 14 — the clause-14 pre-column reopen subject); 16 Sep 14:29 (W3's export: `Tariff_ARPU_Basis` last on both sheets; `W3 test mix promo` Forecast 7.4265); **16 Sep 14:29 INITIATIVES** (the advisor's hand-made fixture — the 14:29 save with an `Initiative` column: "Launch test" = the campaign + `test save W3`, "Ramp check" = the five `Big Volume Test` rows; Walk H's subject); 17 Sep 07:45 (H6's export: 15 events, `Launch test` on five rows incl. the Dec row added by the duration-4 edit, the yield row's Comment `carry`; Walks I and J's subject); 17 Sep 09:37 (I8's export: 7 rows, every `Initiative` blank, no yield rows). All in `Downloads/`; the INITIATIVES fixture was delivered in chat.

## 2. Working agreement

`[INLINE v3.3.16 §2]`


- **Two-tier structure:** advisor drafts prompts and pushes back on wrong premises; Claude Code writes the code. Nearly every turn ends in a pastable prompt. **Sessions are SEQUENTIAL; a walk runs in parallel with a session.** **Mid-session injections** are Jon's dated decision plus a measure-first gate and/or NOT-THIS-SESSION.
- **Outstanding questions and actions for Jon are stated in full** — the exact question, the exact thing to decide — never by label (Jon, 2026-09-03).
- **Briefs:** BASE is the previous report's Repo line; **the STOP diffs against BASE, not the last gated commit** (1618 tripped on the audit's own EXPECTED.md entries). **The never-shed item is Item 1 and is built before anything new is instrumented** (1821 spent its budget on a new spec and failed the never-shed item). **The first brief after a decision instructs the session to record it in EXPECTED.md before code** — the seven card-parity decisions sat only in this document for a day. A new source file is registered in the harness `TARGETS` when created.
- **Reports:** `reports/<yyyy-mm-dd-HHMM>-<topic>.md`, ≤25-line FOR ADVISOR block; **`Certifies:` names a HASH** (1821 wrote a sentence); a report with a placeholder certifies nothing; Limits rewritten at close. Docs-only sessions state the last gated commit.
- **Jon's walks are screenshot-gated at step zero; an observation is exported before it is reported; an observation is cleared by a walk or a mounted reproduction through the app path.** The advisor reads saves directly.
- **Agentic QA:** three-stage gate; **the gate runs SERIALLY** (guard-traps mutates real files; a concurrent run produced a spurious 57/58). **A gate is green when the suite reports.** **A plant is verified landed before its result is read — by `git diff` on a clean tree, by a pre-plant backup once the tree differs from HEAD** (a "Permission denied" plant read green; a timeout mid-plant left source mutated). **An instrument ships with a positive and a negative control, run before any measurement it makes is read.** Exact counts; pins raised with reasons; testids on new controls; selectors never by text; anchors verified unique before trusted; INCONCLUSIVE and CRASHED are read as states, not folded into a ratio.
- **UAT observation log:** D1–D2 as at v3.3.10; **D3-01/D3-02 `f55ffc8`; D3-03 `2ecdefb`; D3-04 `d586ffa`** (`handleAddYieldEvent`'s dependency array omitted three reads — a padlock, a typed rate, an override could be dropped at save; found by the first mounted Value-card spec; walked by hand on 03 Sep); **D4-01 CLOSED `efd2bd9`** (drag under a target did not hold the blend on EITHER card — a spec-vs-implementation gap on the settled entry, not parity; walked green 2026-09-04); **D4-02 `670c7c3`** (typed mix % under a target jumped on the first keystroke); **D4-03 `670c7c3`** (exactly-determined state showed the wall message). **REQ-D3-01 BUILT** (padlock `6235e47`, target `3773ce1`, walked). **REQ-D2-01**: decisions taken (§3), builds queued. **REQ-D3-02** (composite price-rise event): backlog, design note first. Re-observation = new diagnosis.

*Additions:*
- **The event list in a brief is read from the FILE, not from memory of the walk** (0747's brief omitted the save's fourth event and mis-stated the third's presence in the earlier save).
- **A reproduction fixture asserts it can discriminate before a result is read from it** — percentage on a basis of 100 (2003), constant inflow for a previous-month pool (1602), a table not in the DOM until its tab is clicked (0747), a target at exactly 20 for a rounding test (1602).
- **Guard-traps output goes to a FILE and per-trap lines are quoted** — never through `tail` (2003 relied on the exit code).
- **"Full suite" is `npm run suite`'s figure** from `e5f1e79`.
- **UAT observation log:** through D4-03 as at v3.3.11; **D5-01 CLOSED `6802cba`** (unit missing on the promotion table and summary); **D5-02 WITHDRAWN** (the sign of Base ARPU Δ at All is the yield event's −0.27, verified `e5f1e79`; the promotions contribute ~0.001); **D5-03 OPEN, to confirm** (edit-restore of a percentage promotion shows the unit control on Subs — Jon's 05 Sep screenshot; unconfirmed whether the toggle was touched); **D5-04 OPEN, measure** (a promotion is editable from the Volume card's events table as a plain market event — what save from there does to its arms is unmeasured). **REQ-D3-01 built and walked. Decision 6 (percentage promotion) BUILT `945d648`, walked `945d648`. Decision 3 (Dilution) BUILT `90e2162`.** REQ-D3-02 backlog. Re-observation = new diagnosis.

*Additions:*
- **A pinned "measured, not endorsed" check must go RED before it is re-aimed** — its red line is the signal the decision landed, and it is quoted in the report (1950).
- **Capture every writer** — which writer fires says which editor took the row without spying on a handler.
- **UAT observation log:** through D5-02 as at v3.3.12; **D5-03 CLOSED `ede9b43`** (walked 2026-09-05: % pressed on reopen, save keeps +10%, no leak into a new promotion); **D5-04 CLOSED `2864d60`** (walked 2026-09-05: the Volume pencil lands on the Promotion editor with the arm restored). Re-observation = new diagnosis.

- **A check reports; it does not guard** — a `check(rows.length === 2)` in front of `rows[0]` still throws on an empty array.
- **A check that has not been seen red has not been seen** — the first id-uniqueness check passed on a planted duplicate; every trap is seen red by hand against a pre-plant md5 before it is trusted.
- **Measure an event with an instrument it can move** — an ARPU-only event read by a volume delta reports "nothing happened"; a marker measured by the series it sits on reports success while absent; a row count under a height cap reports success under any cap.
- **A collapsed panel is fixture, not evidence** — open it before asserting its rows.
- **Docs-session rules are for docs sessions.** No inline `python -c`, no background tasks, 30-second abandon: minted for docs sessions on Windows. They do not apply to the gate; a build session runs guard-traps to a FILE as standing; a docs-only session runs `npm run suite` only.
- **A brief pasted with an unfilled placeholder is recorded, not filled.** The session names it and proceeds where no edit depends on it (1341).
- **UAT observation log:** through D5-06 and REQ-D6-01 as at v3.3.14 (REQ-D6-01 CLOSED `bd50f27`, walked 2026-09-07). **REQ-D6-01 CLARIFIED `e4fd63f`** (chip counts all rows). **D5-07 CLOSED `45a437a`** (walked 2026-09-07; logged `7b43e73`). **D5-08 CLOSED `8c0cb71`** (walked 2026-09-07; logged `7118e5b`). **D5-09 CLOSED `d63e67a`** (A/B/C 2026-09-08; walked; logged `9b58b48`). Re-observation = new diagnosis. Alessandro's own re-observation of D5-07 in de/it is a watch.


*Additions (2026-09-09):*
- **A brief's base names a report that EXISTS.** Two sessions stopped correctly on "the Repo line of a report that was never written" (1225, 1229) because the UI session had been held at its own shed point. When the intended base session has not run, the brief says so and names the last gated hash; a base clause that forbids the only real tree is a brief that cannot run.
- **A brief ends with an END-OF-BRIEF marker and Jon checks it before sending** — 1217 arrived truncated at its shed clause.
- **A planted file is restored ONLY from the pre-plant backup in the scratchpad — never `git checkout --`, never `git restore`** (1711: the tree held uncommitted work and it was destroyed).
- **Ordering is written as earliest/latest, never "most recent"** — the phrase was read two ways in one day.
- **A docs line already applied is not re-applied** — the 1936 session checked EXPECTED.md before appending and found the closing paragraphs already there; a second copy is a change to the record.
- **A report's timestamp is command-sourced** (`date`), never composed from the brief's guess at the date (1936 was named 09-09, not 09-10).
- **Scratch scripts go to the scratchpad, never into `scripts/.tmp`** (1652).
- **A "by construction" reading is recorded, not fixed unbriefed** — 12.94 (stated rate) and the Sep-vs-Oct comparator month were measured, named and left for a decision.
- **UAT observation log:** through D5-09 as at v3.3.15; **REQ-D6-02 CLOSED `360a591`** (parts `9c93807`, `0e67708`, `360a591`; walked 2026-09-09; logged `e9628b0`); **D5-10 CLOSED `fb51506`** (halves `7850298`, `fb51506`; yield half `7a0bc1d`; walked 2026-09-09; logged `41c8be7`); **D5-11 CLOSED `6538350`**, **D5-12 CLOSED `9118678`** (both logged `e5e1656`); **D5-13 CLOSED `f252700`** (logged `6eee37d`; month format 2113 pending its fill). Re-observation = new diagnosis.

*Additions (2026-09-10/11):*
- **The advisor asks for a missing hash, path or name BEFORE drafting a brief that depends on it; a brief never carries a placeholder for Jon to fill** (Jon, 2026-09-10). A brief's base names a report that EXISTS; when the base session has not reported, the brief waits.
- **A hand-computed expectation in a brief is checked before it is used to dispute a report** — the advisor's 5,700 / 57,000 (the ramp rows summed as 3,000, not 6,000) contradicted two correct sessions across three turns.
- **The advisor reads the save when the walk is about the save** (reaffirmed): B6's "missing events" were settled from the 16:50 sheet — 59 rows, both held campaigns whole, and no row named B3 or B4 because neither was in the walk as written.
- **The walk's expected outcome names the exact control** — B6 named "the first row's switch" and Jon pressed the per-row switch; the campaign switch is a different control. **Walk bands are computed from the cohort's own numbers** — B3's 0.90–0.93 assumed a 21% churn rate on a 117% cohort, and the Outflow line also carried B2's −300.
- **A session that cannot measure reports NOT MEASURED and stops** — measure-first items never proceed on a guessed figure.
- **A mid-session injection to a wedged session reads the capture files BEFORE killing anything** — the 03:26 runs had printed every figure and simply never exited.
- **A stopped session with a skeleton report is resumed by a brief that fills the report FIRST, committed alone, then continues its items** (the 0225 → 0839 resume).
- **UAT observation log (2026-09-10/11):** **REQ-D6-03 CLOSED** on all three carriers (`8728893`, `ac9b633`, `9bbb389`; walked B1–B5, B7, B3, C1–C3, B9 green; the preview defect fixed `b2b1a9c`); **B8 CLOSED `477b171`** (stale closure); **B10 CLOSED `477b171`** (sticky banner); **REQ-D6-04 CLOSED `fd1ad78`** (Jon's Chrome timing of the three fixtures pending — a docs re-wording follows it); **clause 10 (checkboxes) CLOSED `7206ff0`**; **held-rows-base MEASURED, no defect, `7206ff0`**; **D5-05-held (Jon 2026-09-11, yes) IN SESSION**; **REQ-D6-05 DECIDED, not built.** Re-observation = new diagnosis.

*Additions (2026-09-11 → 16):*
- **A brief never carries a placeholder for Jon to fill; the advisor asks first** (Jon, 2026-09-10, reaffirmed 2026-09-11 when a brief hid a hash "to fill in").
- **A walk instruction ends where the action ends** — "Cancel" after a reopen was read as "don't add"; the walk names which control the word applies to.
- **A release is certified by a walk on the exact tree, on the paths users take** (fresh file → generate → both modes → promotion add → churn → round trip → Compare → German), then the hash goes to Maddie with a plain list of what changed since her last deploy.
- **A pin the sessions cannot exercise is defined by the advisor, not guessed a third time** (the 3/2/3 definition).
- **UAT observation log (2026-09-11 → 16):** D5-05-held CLOSED `6fd13f8`; the 50MB caption CLOSED `17b52d5`; REQ-D6-05 CLOSED `296618c` (walked D1–D7, R1–R6); REQ-D6-06 CLOSED `1bc094b` (walked E1–E7 + the two-step check); **REQ-D6-07 (Value card leads with the cohort ARPU) DECIDED 2026-09-15, inventory in session 2026-09-16**; **AFTER UAT:** Step 3 after a restore says "No forecast found" for every cohort while Step 2 renders (generated sessions unaffected; found 2026-09-11 on the 16:50 save; Jon parked it); the Step 2 viewing bar is not persisted in the save (the recorded Active_Cohort is the Step 1 load); negative percentage campaigns reopen as positive; pricing percentages at 2dp; the locale decimal separator; the English help line on the German card and the de/it screenshots; the yield tie rule; the user guide (Alessandro isn't using it — with the environment phase). Re-observation = new diagnosis.

*Additions (2026-09-16 → 17):*
- **A walk and a guard-traps run never share a tree** — guard-traps plants mutations into real `src` for seconds at a time; a dev server (`npm run dev`) serves the plant and restarts. Walks run on a BUILT PREVIEW, or wait for the run; **a walk that cannot say which server served it is repeated on a quiet tree** (the TARGETS md5 table proves quiet) — the 2026-09-16 clause-14 walk was re-walked on that rule.
- **The walk's expected reading is written as "reads X — don't touch it"**, never as the bare word — "toggle Historical" was read as an instruction and pressed (Walk W3, 2026-09-16); the 2026-09-11 rule ("a walk instruction ends where the action ends") covers the reverse case.
- **Jon's pass is the pass** (Jon, 2026-09-17). The advisor states, once, where a screenshot contradicts a spec, and otherwise takes what Jon reports; it does not re-litigate steps it wrote, and it does not withhold a step's pass for evidence it did not ask for at the time.
- **A walk step that ran a different action from the one written is a different test, not a failure** — J2 renamed the initiative itself (the self-case) where the step named Ramp check (the merge case); the build was doing what its clause said. The advisor re-reads the screenshot against the STEP before it reads it against the SPEC.
- **A read-only inventory that ends with questions gets Jon's answers recorded by that session, committed alone, in an addendum naming the commit** (1606 → `69624ef` / `6343265`) — the answers do not live only in chat.
- **Rebuild sites are counted by grep for the replacer, never read from an inventory's list** — the churn campaign save was a third row-rebuild the 1606 inventory did not list; session 1 found it by grep and carried the initiative there too.
- **A session's "by construction, not exercised mounted" is a queued mounted case for the next session** (the row-edit carry → (h)), and the walk covers it directly meanwhile (H5).
- **UAT observation log (2026-09-16 → 17):** REQ-D6-07 CLOSED `fdc5852` (clauses 1–15; walked F, G, W1–W3) and `ab2c634` (clause 16; walked); the rates-column defect CLOSED `fe0cb7e`; **REQ-D6-08 CLOSED `a497ff5`** (clauses 1–18; sessions `6c12fab`, `c9ff73b`, `8422374`, `a497ff5`; walked H, I, J). **Observation, unexported (after UAT):** a reopened yield event (`test reset`, 2026-09-16) showed the "Apply to this cohort month only" toggle OFF beside the caption "at Oct 2026 only" — either the caption or the toggle's restore on reopen is wrong; the export was never taken; one look with a real file. Re-observation = new diagnosis.

## 3. Settled decisions — do not reopen

`[INLINE v3.3.16 §3]`


*EXPECTED.md verbatim governs; cite by HEADING. **The record audit of 2026-09-03 (report 1601) mapped every inherited item to a heading**; missing ones were written verbatim from git history under *"RECORDED FROM WORKING AGREEMENT v3.3.x (audit 2026-09-03)"*; the never-re-propose list lives under *"RECORDED FROM WORKING AGREEMENT v3.3.1 (audit 2026-09-03) — the never-re-propose list"* (it had been in NO repo file for four versions). "Nearest-populated-ancestor fallback" is UNRECOVERABLE — advisor memory mislabelling a shelved backlog line ("§16b per-cohort ancestry denominator") as a rejection; it is NOT a rejection and is dropped.*

**Jon, 2026-09-02** — as at v3.3.10 §3 (option (i) `63cc27f`; no-populated-leaf `051a9b3`/`2b818ee`; caption `bd2cf63`; Q3/Q4 `20799e3` under *"Q3 / Q4 — THE PRICING BASELINE AND THE BLEND CONSUMERS GO PER-SCENARIO"*; ARPU companion own mode queued; per-tab defaults stand).

**Jon, 2026-09-03** — as at v3.3.10 §3 (pool feed `2ecdefb`; UAT ~two weeks, findings before DQ, DQ AFTER UAT — confirmed; 1005 SIGNED OFF and its §5 corrected `89f68a9`; native review inside UAT; pins accepted; stored aggregates keep historical weighting — after UAT; **card parity — seven decisions**, under *"CARD PARITY — SEVEN DECISIONS (Jon, 2026-09-03)"*: (1) locks persist — BUILT; (2) churn on a promotion declined, composite event → REQ-D3-02; (3) Dilution on the promo pricing arm — build; (4/5) Target and Applies-to NOT ported, option (i); (6) percentage on the Promotion volume arm — BUILD, Inflow AND Retention, retention basis = the view's forecast retention for the month; (7) one slider component first — BUILT).

**Jon, 2026-09-04:**
- **The six non-unique anchors: option (i), fix** — four fixed `7b64ddb`; 22 moved to the global class and 102's spec made to survive `e5aec46`.
- **Gate hygiene — three decisions**, under *"GATE HYGIENE — THREE DECISIONS (Jon, 2026-09-04)"*: CRASHED distinct from CAUGHT; the global-mutation class; 102's assertion reachable and `App.tsx:1653` covered. Built `e5aec46`, green `a6d35e5`.
- **Drag under a target — three decisions**, under *"SUPPLEMENT: THE DRAG ITSELF (Jon, 2026-09-04)"* inside *"SETTLED: constrained mix mode, not input-vs-target"*: (1) build sum-AND-blend on both cards; (2) the slider stops at the wall, the target is never rewritten; (3) minimum change above two free sliders. Built `efd2bd9`.
- **The typed box and exactly-determined**, under *"SUPPLEMENT: THE TYPED BOX, AND EXACTLY DETERMINED (Jon, 2026-09-04)"*: D4-02 option (a) commit on Enter/blur in ALL modes; D4-03 yes. Built `670c7c3`.

*(cite by heading; the record audit of 2026-09-03; the never-re-propose list under its heading; "percentage on Custom Promotion" is DECLINED-UNTIL-THE-ORDER-IS-DONE and the order is now done — status recorded beside the 2026-08-02 entry as REOPENED 2026-09-03, BUILT 2026-09-04).*

**Jon, 2026-09-05:**
- **A promotion's price change stays baked into its ARPU (option a).** Duration on the promotion pricing arm is not built; revisit with REQ-D3-02 after UAT. Recorded beside decision 3.
- **The ARPU Delta card subtracts then rounds** — built `e5f1e79`.

**Jon, 2026-09-05:** **D5-04 option 1** — recorded in EXPECTED.md beside the D5-04 measurements: a promotion is edited from its source; the Volume table's pencil routes to the Promotion card; the row stays listed. Built `2864d60`.

**Jon, 2026-09-05:** D5-05 and D5-06 — recorded `8e79708`. **Jon, 2026-09-06:** **REQ-D6-01 — PER-EVENT ON/OFF**, eight decisions as in §1 — recorded `a23d8bf`. **Jon, 2026-09-07:** the AI hold stands; APP_URL excluded from spec:ai-hold; direct-to-main is the settled mode; plugins that don't serve the project stay disabled; **model sizing → option (b) shaped as the 1202 trial**: three archived build briefs re-run on Fable 5.1 AND Opus 5 from the same BASE, graded by the project's instruments (gate; traps red by hand; findings-reported-not-built; FOR ADVISOR conformance; vacuous-check rate), 1857's mounted-padlock brief as calibration, ~6 gated sessions, AFTER UAT; **the python ban lives in CLAUDE.md** as one traceable line (`050c285`); **the Events summary chip counts all rows** — recorded `e4fd63f`; **D5-07** markers on all three measures, month T unlagged, off events excluded — recorded `58f136b`; **D5-08** Show all / Show fewer, header, only when scrolling would occur, default capped, view state only, summary panel only — recorded `c9e3da4`. **Jon, 2026-09-08:** **D5-09** — the pivot word is *effect*; the four-then-six states as in §1; card *Events in effect / moving volume · N switched on*; card tables OUT; Compare widened to show the same statuses; (iii) resolved by **option 1** (one shared `ID ?? Name` derivation; the ID-less same-name collapse recorded as a watch, not fixed); Compare-only *Not applied here* for kinds Compare's engine never applies — recorded `ff38d5a`, `58a3cf6`, `d260fbd`; **the Fable trial's second arm** (Fable 5.1 as orchestrator over Opus/Sonnet subagents) is considered only if the first arm shows a measurable difference on the project's instruments; **each trial arm records its weekly-usage delta** (read from the usage panel before and after) so the quota question is measured, not estimated.


**Jon, 2026-09-09:** **REQ-D6-02** — one shared month selector, forecast horizon, chronological earliest-first, forecast months carrying no actual, default = last forecast month, view state only; Base Volume and ARPU cards read the selected month; a Revenue Delta card with four per-scenario rows; every delta subtracts unrounded adjusted minus baseline and rounds once; titles carry the month; no new stock-vs-flow caption; authorisations (A) the persisted engine fields and (B) ONE `monthsCarryingActuals` predicate, file-level — recorded `c7dbf02`, clarified `7cc0e6b`. **D5-10** — design (b), nine decision clauses as in §1; applied at every emitter with an exact-count pin (one-helper-per-card deferred to after UAT); yield reading (1) — recorded `cfcdd68`, clarified through `e9628b0`. **D5-11** — Forecast basis default; Preview Impact through the existing seam, no second engine; the ratio caption; "SETTLED 2026-08-12 — the comparator is option (c)" cited as unchanged — recorded `3ac8a1c`. **D5-12** — the union count and the always-rendered caption, superseding D5-09 (i) — recorded `2373b94`. **D5-13** — the tie rule unchanged now; (a)/(b) open for after UAT with the 1936 costings; (c) declined; the preview names the winner and the draft's first winning month — recorded `99dfada`. **The de/it screenshot watch is parked to the END of UAT** (Jon).

**Jon, 2026-09-10:**
- **REQ-D6-03 — RAMP THEN HOLD**, clauses 1–6 as in §1 (materialised rows to the build-time horizon; Hold ON = target with a linear-cumulative ramp, custom shares cumulated; Hold OFF byte-identical; a "Hold after ramp" toggle on Volume spread, Promotion spread and churn ramp, independent of the spread switch; `Hold` column last; a held campaign is one campaign) — recorded before code in session 1. **Clause 7** (percentage spreads only under Hold, unrounded) and **clause 8** (tail bounded by the build-time horizon) ratified 2026-09-10, `c876e4c`. **Clause 9** (churn holds POINTS; 1.0% ratio band, direction asserted) 2026-09-10, `40b4ab6`. **Alessandro's case is volume events (inflow, outflow, retention); the churn example is the theory behind it** — the ordering (Volume never-shed, churn second, Promotion third) followed from that.
- **REQ-D6-04 — file size**: 200MB on all three paths; a pre-parse notice painted before the parse; the success line carries the measured time; a detected session file on the input drop is routed to Import Save automatically; ships before UAT closes, at the end of the then-current bug cycle — recorded `3acb088`. The notice's "up to a minute" is re-worded to the figure Jon measures in Chrome on the laptop (16GB, Core Ultra 235U) — the constraint is the frozen main thread, never memory.
- **Guard-traps runtime: measure per-trap wall time first; then targeted selection** (traps whose target file or spec changed since BASE, traps added or edited this session, traps whose anchor moved) **plus a rotation of 20 oldest-last-run traps from a tracked ledger; NOT RUN is a state, never folded into the ratio; a FULL run before any hash is called "last gated state"; parallel worktrees only after that.** Queued after the REQ-D6-05 and ESLint sessions.
- **The de/it screenshot ask and the yield tie rule stay parked to the END of UAT.**

**Jon, 2026-09-11:**
- **The ramp/hold pair on every card is a real checkbox (clause 10)** — option (a) of the 2059 report — built `7206ff0`.
- **D5-05 lifted for HELD percentage campaigns** — a held percentage campaign is group-editable (its restore never sums); an unheld one stays barred — to be recorded under D5-05's heading in the session now running.
- **REQ-D6-05 — SPREAD vs RAMP (decided, to be recorded before the inventory session's first edit, under the heading "REQ-D6-05 — SPREAD vs RAMP (Jon, 2026-09-11)"):** (1) Spreading a volume and ramping are two different things and the user chooses explicitly: **Spread** — the entered figure is a TOTAL; custom shares must sum to 100%; ends after N months (today's Hold-OFF behaviour, byte-identical); **Ramp** — the entered figure is the TARGET reached at month N; each month's figure ≤ the next (today's Hold-ON shape). (2) **Hold lives ONLY in Ramp mode** ("Ramp then hold"; a permanent monthly figure is Ramp with duration 1 + Hold); Spread has no Hold. (3) **Ramp mode's per-month entry is TYPED VALUES** (1,000 / 2,000 / 3,000, like the churn card's boxes), Even as the prefill; shares belong to Spread. (4) **Default mode: Spread for an absolute amount, Ramp for a percentage** (churn is already a ramp; Jon named "dilution too" — the dilution arm has NO spread today, decision 3 applies it once at build, so a dilution ramp is a NEW capability the inventory costs, not a default). (5) **The Promotion card's spread accepts typed absolute per-month values as well as shares, matching the Volume card** (today the promotion distributes shares only). The amount label follows the mode ("total, split across the spread" / "target reached, then held"). This supersedes clauses 2–4 and 7's coupling of shape to the Hold box; the emitted rows and the `Hold` column are unchanged in meaning.

**Jon, 2026-09-11 (continued):** D5-05 lifted for held percentage campaigns — recorded and built `6fd13f8`. **REQ-D6-04 clause 4** (the laptop timings; "usually under 15 seconds"; no typed megabyte figure) — `17b52d5`. **REQ-D6-05 clauses 7–18** — as in §1: Mode persisted (7); Custom values (8); a percentage is always Ramp (9); the switch retired, panel always open, Hold only in Ramp (10); validation (11); labels (12); no dilution ramp (13); one session, Volume first, walked by Jon (14); % labels carry the Ramp suffix (15); the checkbox pin is FOUR (16); % display 2dp (17); duration 1 reads "— one month" on both (18). **File-size (2026-09-10) built and measured** — decision 4 above.

**Jon, 2026-09-15:** **REQ-D6-06 — CAMPAIGN-LEVEL DELETE**, clauses 1–8 as in §1 (every pill surface incl. the summary; one confirm with the count; members barred from row delete AND row edit; one function; Compare unchanged; every per-row delete through the dialog; the short reasons). **REQ-D6-07 — THE VALUE CARD LEADS WITH THE COHORT ARPU** (decided, inventory running): (1) the lead pair is the cohort's fitted ARPU → delivered ARPU at the preview month, cohort and month named; the equal-weight blends DEMOTED to a collapsed line (the comparator itself unchanged — option (c) not reopened); (2) the Target box means the cohort ARPU at that month, solved by GOAL-SEEK ON THE PREVIEW (bisect the blend target through the seam; overrides honoured because the seam honours them; Reachable in cohort terms); (3) the summary ADJUSTS cell states the ratio as a percentage ("Inflow ARPU +52.1%"), the tooltip the pair; (4) the basis toggle unchanged; (5) not the never-re-propose "goal-seek-as-recommendation" — the app proposes nothing. **Guard-traps clauses 1–6** under "GUARD-TRAPS TARGETED RUNS (Jon, 2026-09-10; built 2026-09-15)" — as in §1; **full is the default, targeted opt-in (clause 6, 2026-09-15).**

**Jon, 2026-09-16 (REQ-D6-07 clauses 6–16, EXPECTED.md heading "REQ-D6-07 — THE VALUE CARD LEADS WITH THE COHORT ARPU"):** (6–11) the solve reads the UNROUNDED series, tolerance 0.005, cap 30, closed form first guess then bisection; the read month is the preview's (Value card: Inflow the month AFTER the draft's, Retention the draft month); no rival marker in the cell; band per render memoised; roll-forward suffix "applies from {month} onward" / "at {month} only"; refusals when fitted ARPU is absent or the draft has no stored rates. (12) The Promotion arm speaks in cohort units too. (13) **An Inflow promotion moves Inflow ARPU in its OWN month — the Promotion arm reads the promotion's own month (option A);** a multi-month campaign reads the draft's first month with that month's volume. (14) **Option A — the ARPU basis is STORED on the event** (`Tariff_ARPU_Basis`, absent → Historical; reopen restores; NEW drafts open on Forecast on both cards; D5-04's no-change save holds). (15) The seam's `marketDraft` slot; ONE row builder; ONE solver. **(16, Jon 2026-09-17-recorded, decided 2026-09-16)** after a Value-card Add the next draft opens on Forecast — "it should reset, not hold ghosts from previous events." Not reopened: the comparator option (c); goal-seek-as-recommendation (never-re-propose, EXPECTED.md:7702).

**Jon, 2026-09-16/17 (REQ-D6-08 — INITIATIVES ON THE EVENTS SUMMARY, clauses 1–18, EXPECTED.md heading of that name):** (1) one initiative may hold campaigns AND single events from all four cards; a single event joins on its own; a campaign joins WHOLE; one initiative per row. (2) Storage option A — an `Initiative` column, last on all three sheets, ONE writer/reader each, absent → none; the initiative IS its rows; no Initiatives sheet. (3) The switch on the Events summary ONLY (not the card tables), setting `Enabled` on every member across carriers, three states; rows stay the truth; card switches independent. (4) Controls option (ii): selection mode — tick, "Group as…" (new or existing; existing ADDS, no prompt), a campaign ticks whole, Ungroup per member, Dissolve whole. (5) Members sit TOGETHER under a header once grouped. (6) An initiative bin (Jon: "I feel it will come when he tests"); Compare shows the name as a column, no switch, no bin. (7) Ships before UAT closes. (8) The bin removes Value/Pricing members through the existing per-row removers from the one confirm — no whole-array setters. (9) Rebuilt campaign rows keep the initiative; added months join. (10) A member of A grouped as B MOVES to B. (11) Names trimmed; the merge prompt on renaming onto a name in use ("Merge into '{name}'? {n} events will join it"); an unused name renames with no prompt — the case rule of this clause superseded by 17. (12) A group sits at its EARLIEST member's position. (13) The badge counts events only; the Show-all threshold counts every visible row, headers included. (14) The header's Effect cell is a per-state count in the column's order, empty states omitted. (15) Compare: pipeline order, the name in a column only. (16) Session 1 shipped `6c12fab`; the carry at all THREE campaign saves. **(17, 2026-09-17, Walk I5)** names compared CASE-INSENSITIVELY after trimming for Rename, Group as and the layout; a match IS that initiative and the EXISTING casing wins; two initiatives can never differ only in case. **(18, 2026-09-17, Walk J)** a rename whose typed name matches the initiative's OWN name case-insensitively is a RENAME — every member re-cased to what was typed, no prompt (the 0841 build's no-op superseded); a merge prompt arises only when the typed name matches a DIFFERENT initiative; an identical name is nothing.

**Released / release path (2026-09-16/17):** `fdc5852` (walked W1–W3 on a quiet tree; FULL 254/254) and `ab2c634` (FULL 255/255 per the 1459 gate) were each sent to Maddie as the hash to deploy; `a497ff5` (FULL 270/270) is the current release candidate with the combined note (the Value-card reset; initiatives — select, group, switch, ungroup, dissolve, rename with the merge prompt, delete; names ignore case, a casing-only rename re-cases; saved with the file; a Compare column; old saves load with none). Which of the three Maddie has deployed is confirmed with her before the next note.

## 4. Standing rules

`[INLINE v3.3.16 §4]`


*(The full inherited set stands — provenance-before-reading, no figure for unopened populations, surface-not-store + mount-above-gates, spec-may-not-feed-itself with hand-written literals, drive-the-real-path, evidence-is-what-mounted, planted-violation-or-not-verified with exact-count negative controls, removal test, third-instance, NaN/absence carriers, layer-before-deletion, five 'All' marker sites, duplicate-predicates collapse, reserved-decisions hold, harness-noop count guards, convention-beats-brief, cannot-fail checks, anchor family, no unclaimed properties, structural windows; summarisers describe; measure-before-render-cost; silent render failure is a defect independent of cause; the R7-arc set: read-set rule mount edition, a-restore-is-not-a-transition, zero-re-baselines-means-nothing-exercised-it, same-named-quantity, read-the-call-site-first, derivation-beats-guarding, one-flag-two-jobs.)*

**Minted in the pricing/close arc (2026-08-21):**
- **A recorded decision is not evidence the code implements it.** EXPECTED.md is ground truth for what was decided, never for what is true of the build. The event-scoped-ARPU gap ran four days and survived two sessions that both had the decision in front of them.
- **A spec's claim is about the code as RUN, not as written.** The member-edit decline gate was correctly written, spec-pinned — and born unreachable in its own commit (`7330e8e`): memoised on `[setNewEvent]` while reading `marketEvents`, built once over an empty list. "True as written, false as run" is a distinct failure class from a regression.
- **The harness prop-stability rule** (§2; in EXPECTED.md): an inline arrow where the app passes a stable setter silently disables memoisation and makes every stale closure invisible. One file has produced two stale-closure defects — one the mount caught, one the mount hid; the difference was harness fidelity.
- **Assert the fixture discriminates before asserting anything about it.** `arpu: 0` made `near(0,0)` hold whatever the scoping did; the distinctness precondition written before the substantive checks is what caught it. Same species as a guard admitting 0 because 0 is a number.
- **The configuration where a scoping bug is invisible is the one everybody loads.** C = S makes the wrong feed return the right answer — why no walk caught the pricing scope defect, and why invariance checks structurally cannot see scoping defects. The discriminating configuration is the one users don't habitually use (wide-loaded, narrow draft).
- **The read-set rule cuts in the removal direction too:** a source that stops being read stops being a dependency.
- **The condensation-inversion rule** (preamble): this document dropped "barred class" from D5 and the advisor propagated the inversion into a walk expectation and a fix brief. For settled decisions, read EXPECTED.md verbatim before briefing against them.
- **The "what would this trap actually redden?" test** precedes adding a trap: a trap mutating the same string a source-text spec reads only proves the spec reads the file.
- **A trap that leaves one half green proves the LAYER** (trap 113's construction): distinguishing "the figure looks right" from "the fix lives on the right layer" needs a plant that reddens the invariant while the surface literal stays green.
- **Ambiguous selectors fail by switching affordance, not by erroring** — the title-matched `/edit/i` selector silently drove the campaign pencil the moment the bar came down; testids on both controls now.
- **Zero-re-baselines instances: two on record** (`C.prod`; the ARPU fixture) — third instance triggers the third-instance rule.

**Minted in the user-guide arc (2026-08-31, docs-class but transferable):**
- **An edit that reports success without matching is the no-op class in another costume** — a typed-sentence replacement silently matched nothing in French (straight vs curly apostrophes). Edit scripts assert exactly-one match and report-and-skip on failure; frozen blocks are SHA-pinned at entry AND exit of every script.
- **Content past a splice point is not reached by the splice** — the Custom Promotion tail kept "ramp and decay" in four languages after the Step-2 rewrite. A stale-string sweep on the corrected claims is the check; the 1924 sweep covered the ramp/decay family only, stated not silent.
- **A guide quotes what the user sees, not what should exist** — 50 of 76 locale keys render English in every locale; the translated guides quote them in English with one honest note per language, because inventing translations the app never shows makes the guide wrong in a way the reader cannot detect.

**Minted in the chart-grid arc (2026-09-01):**
- **A recorded decision is not evidence the code implements it** (reaffirmed); **a spec's claim is about the code as RUN**.
- **Retiring a value from a vocabulary is a sweep, not an edit** — every literal that names it (defaults, stored selections, restores, fixtures) is a site; trap 120's source check guards the class.
- **A guard must read the quantity it protects** (`selectedKpis.length` guarding a chart that drew `selectedScenarios`).
- **A floor cannot see a removal once anything has been added** — trap 77 MISSED in practice; exact counts only for source-site evidence.
- **A trap that plants an unobservable change is not a trap** ("what would this actually redden?" — trap 120's first aim).
- **Plant by hand before believing** — a green run did not find the pills/chart disagreement; the negative control did.
- **The configuration where a defect is invisible is the one everybody loads** (reaffirmed for scoping); **the step that reveals is not the step that causes**.

**Minted in the locale arc (2026-09-02):**
- **A sample of what a document quotes is not a census of the app** (50/76 → 703/822).
- **A tool that diagnoses its own defect in a comment and fixes it in one block has left it standing in the next** ("PRESENCE IS NOT TRANSLATION").
- **A strictness check needs an exemption route with reasons, or it pressures you to make the product worse to stay green** ("Volume dei dati").
- **An exemption must be wired to its premise** — the `.detail` exclusion lapses the moment one is rendered; `LOCALE_DEFERRED` sat a year because it couldn't.
- **Enforcement that only runs under a flag nobody passes is documentation** — `--check` is in the gate now.
- **Render in the language; a source check cannot see a second render path** (Step 3's hand-drawn legend) and **a formatted date is invisible to a parity check**.
- **A scanner's label is not evidence; read the module** (the seventeen diagnostic strings).
- **Exact counts pin REMOVAL where removal is the dangerous direction** (the identifier's 20/10/7).

**Minted in the D2-03 arc (2026-09-02/03):**
- **A reproduction hands the engine what the APP hands it** — an engine call with a friendlier representation of the same value (null for 'All') measures a different program.
- **Where a value has two representations, there is one predicate and every caller uses it** — a second copy is retired, never patched into agreement; the inline copy at `WhatIfTab:965` knew one of 'All's two forms and withheld every leaf-scoped event from every broader view.
- **A walk observation is cleared by a walk or a mounted reproduction through the app path, never by an engine call handed the event.**
- **A walk observation is exported before it is reported.**
- **A spec that can only be green on a fixture that hides the defect is not written — its absence is the finding.** Every synthetic fixture whose forecast is proportional to its history hides the coverage divergence; the discriminating fixture asserts its two ratios differ before anything runs.
- **A caption that cannot be wrong cannot be evidence** — `marketEvents.length` printed "1 applied" beside +0.00 and was quoted as corroboration.
- **`null` and `0` are two answers** — "cannot answer" and "measured zero" never share a number; `forecastCoverage` set the contract, `eventProRataShare` still violates it.
- **Blast radius zero across the suite means nothing catches it going in wrong** — a follow-up brings its own fixture.
- **INCONCLUSIVE is a distinct state; read the states, not the ratio; capture gate output to a file.**
- **One assertion per source check.**
- **A record beats a re-derivation** — the expander reads `zeroCoverageEventIds`, not the predicate again; the tooltip's three carriers are not repointed at a market-only record.
- **A scanner's deferral list is an exemption wired to nothing** — `I18N_PHASE2` passed a live literal as DEFERRED; the context-file block one screen away already re-matches its entries (reaffirms "an exemption must be wired to its premise").

*Minted 2026-09-03:*
- **A decision is written in full or cited by its EXPECTED.md heading — never by pointer to a superseded document.** The record is the terrain; a pointer to deleted terrain is a map of nothing.
- **A rejection carries its date and reason, and the never-re-propose list cites the entry;** a bare label sends everyone looking under the wrong word.
- **A caller count cannot see a non-caller** — a pin on callers of the one definition is paired with a structural zero-occurrence check on the hand-rolled shape. (Seven copies lived beside a green count of seven.)
- **Same lines, opposite outcomes, decided by how each caller spells the wildcard** — a copy that is right only because of its caller's representation is a defect waiting for a second caller. Retire, never repair.
- **A mutation trap is coupled to the source text of the line it guards; the traps most likely to rot are on the code most likely to change.** INCONCLUSIVE is what catches the rot (13, 115).
- **A gate is green when the suite reports.**
- **A re-point that passes with or without the defect is not made** — test the trap against the new target before moving an assertion (D3-02 stayed on Base twice, measured).
- **A fixture asserts its per-scenario bands exist before per-scenario deltas are asserted** — four em dashes pass vacuously; retention `band(0)` on every leaf reads exactly like a scoping defect at All.
- **A harness passes what the card passes** (`viewLeafForecasts` omitted → 30 not 20).
- **A guide sentence is a claim about source** and is corrected from source, not from the previous guide.
- **A report with a placeholder certifies nothing.**

*Minted 2026-09-03/04:*
- **Cite a decision by its EXPECTED.md heading; a line number is a courtesy that rots.**
- **A never-re-propose entry cites its heading; an entry with no recoverable reason binds nobody.**
- **The STOP diffs against BASE.** **The never-shed item is built first.** **The first brief after a decision records it.**
- **A spec that cannot finish cannot be read** — CRASHED is a spec-survival defect, never "a why nobody asserted" (all three had their assertions).
- **An instrument ships with its own positive and negative controls.** A classifier that silently degrades to "everything caught" restores the defect it was built to end, invisibly.
- **A plant is verified landed before its result is read** — `git diff` on a clean tree, a pre-plant backup otherwise; a green spec after a plant means nothing unless the plant is known to have landed.
- **A trap that cannot be planted is not written** — plant first; the brief asked for two traps that measurement proved could never fire (the two reads cover each other).
- **The gate runs serially.** **A gate is green when the suite reports.**
- **Uniqueness is a property of the trap's SUBJECT** — where the spec's subject is a pair of identical sites, the trap mutates both (global class) rather than pinning one.
- **A mounted check is the only position from which "the event never arrived" and "the engine mishandled it" look different** (D2-03) — and the only one that sees a stale closure at save (D3-04).
- **A recorded decision is not evidence the code implements it** — the settled "sum and blend" sentence had never been built on either card (D4-01).
- **The panel computes nothing; the engine decides** — `MixTargetPanel` draws what `mixConstraint.ts` hands it; a second implementation of the unreachable rule or the wall is how one card undoes a settled decision.
- **Same numbers on two cards to six decimals is either one function or one defect** — assert which.
- **A drag is continuous; typing is not** — a numeric box commits on Enter/blur; committing on keystroke applies numbers the user never asked for.
- **A guide sentence is a claim about source** — 1005 §5 described a delta that never existed; corrected from `pricingBaselineArpu`, not from the previous guide.

*Minted 2026-09-04/05:*
- **A magnitude cannot survive a percentage; a rate can** — revenue goes to 0, ARPU is baked (`draftEventRate` already said so).
- **A pool is observable only where it feeds** — assert a pool's size where it reaches the screen (the re-banded pool: Base at T+1), never where a sibling construction happens to agree.
- **A mechanism that exonerates the suspect is a finding, not a licence to build** (0747 built nothing under an item that said "fix").
- **A claim about "the same events" is checked against the file's event list** before it enters a brief.
- **A spec that prints no report line has said nothing** — the suite runner's CRASHED state.
- **Recount, don't carry** — a baseline count is measured at the commit it pins (91, not the earlier 87).

*Minted 2026-09-05:*
- **An editor that cannot show what it is editing drops it silently** — one editor per event shape.
- **A disabled control with no reason is a defect of its own** (the campaign pill).
- **A dependency array declares every read, even the ones that happen to work** (three promo builders).

*Minted 2026-09-05/07 (v3.3.14):* the four in §2 above; **an id is unique by the part that keys other checks**; **a title attribute is not a rendered reason**.

*Minted 2026-09-07:* a hung Claude Code turn with the network reachable is checked for expired auth FIRST (`claude auth status`); plugins that don't serve the project stay disabled; no inline `python -c` and no background tasks on Windows in a docs session; any tool call not back in 30 seconds is abandoned and named (docs sessions; the gate is exempt); a wedged runtime is restarted from a fresh shell after killing leftovers, never waited out; a check that quotes its own forbidden strings excludes the files whose job is to carry them; **never invoke `python` in a session — hash and verify with bash/md5sum** (the Store stub `WindowsApps\python.exe` hung at startup; aliases disabled 2026-09-07; `python --version` now answers 3.14.6 in PowerShell; a session's bash was not retested, so the ban stands); **a rule that lives only in a report's Limits is not operative** — the next session learns it by hitting it again.

*Minted 2026-09-07/08 (this document):* **survival has two figures and both are reported** — 104/26 first-row dereferences/files, 27/27 checks — never conflated; **i18n-parity counts CHECKS, not keys** — adding a key to all six locales moves the key count (report it per locale), not the parity figure, and only a new allowlist entry moves it; **an identical translation goes on the ALLOW list with a justification, never mistranslated to make a number green**; **line numbers in a report drift by the previous session's own edits — re-check before relying on them** (A→B ~5 lines, B→C again); **a status is a measurement, not an inference** — session A's carrier-inferred ARPU was removed in B; **absence from a set is not a state**; **do not build a class that does not exist** — "per-scenario pricing" is a consumer, not a kind (C); **a shared component gets a decision by opt-in prop, never by unconditional render** (`showAllToggle`, `onSetEnabled`, `effectOf`); **one function decides a row's status — a wrapper is a second place**; **a join keyed differently on two sides fails silently as a confident wrong answer** — one exported derivation, and a trap that breaks only the ID-less case; **a walk's step zero is Jon's ritual** — reload on localhost from Claude Code's browser, the named save, the expected row count — no npm commands in a walk brief; **the walk's expected outcome is written in the brief before the walk** (card 4 · 5, Volume ×4 …) so a pass is a comparison, not an impression.


*Minted 2026-09-09:*
- **A trap that stays green when planted is not a guard — plant it before believing it.** SIX instances in one day (183, 185, 187, 191, 194, and 190/191-Revenue, which stayed green because the fixture could not see rounding and were REMOVED). The check exists because the trap demanded it; a fixture that cannot fail the trap is the finding. The Revenue rounding rule is the recorded example of "correct by construction, unguarded until a discriminating fixture exists" — say so in the report rather than keep an id that catches nothing.
- **A pin on a field's call sites is paired with the invariant the count stood in for** — `eventScopeSeriesFor` callers 2 → 3 was the intended outcome, so the new pin says what mattered: `computeAdjustedForecast` sites still 6, "no second engine", asserted literally.
- **Derive once, read twice** — the caption and the invitation asked the same question; the `.enabled` pin caught the second copy.
- **A status is per-event; a collision is per-month** — EFFECT correctly read ARPU for both yield events while one displaced the other in October; the model had no place for a per-month fact, so the preview got it instead.
- **A preview reads the month the effect lands in, and names the winner if it is not the draft** — a box that shows a real figure attributed to nobody is the same failure as a caption that cannot be wrong.
- **An engine runs once into a local when a seam widens** — the ids and the series it returns must come from the same run or they can disagree.
- **A stated rate replaces; a scaled rate reconciles** — the Forecast-basis identity holds only over derived rates (R3's ordering); the card is un-reconciled by any override, and the caption should eventually say so.
- **A roll-forward flag makes a non-roll event a one-month candidate** — the asymmetry that made the tie bite exactly once; a fixture for a tie must carry both flags.
- **Byte-identity for untouched callers is measured by a differential run, not argued from green assertions** (1957 Limits — not done).
- **The advisor reads the save when the walk is about the save** — the 19:30 workbook settled the two-event question before any session ran.

*Minted 2026-09-10/11:*
- **A harness that passes what the card passes can still lie if it presses the controls in a different order from a person** — mounted specs drive the form in fill-in order, Hold LAST, and the order is stated at the line (B8; report 2014).
- **A 0 default that silently disables a feature is the guard-admits-0 class; the cure is the type** — `horizonMonths` REQUIRED; tsc red at the omitting site before the fix and clean after, quoted.
- **`as any` at a spec's call site is a hole in exactly the guarantee a required parameter makes** — the type caught the app's three sites and none of the spec's eight; only CRASHED-distinct-from-FAILED caught the spec.
- **A preview that recomputes what the save computes is a preview that can disagree with it** — derive once, read thrice (grid, button, builder); the Promotion card was the THIRD inline copy of `pct/total`, left by the session that retired the first.
- **Grid and emitted rows are each asserted against the SAME hand-written literal, never one against the other** — a check comparing them passes with both wrong.
- **A trap that stays green when planted earns a discriminator or is removed and SAID SO** (reaffirmed): 222 prefix-matched a renamed tag; 190/191-Revenue removed.
- **A new spec is registered in the guard-traps CONTROL list with its first trap** — `HOLDSHAPE` sat unregistered for a session.
- **A mounted measurement script ends with an explicit process exit after its report line** — JSDOM's window, React roots and stubbed timers keep the process alive indefinitely; two runs sat four hours "running" with every figure already on disk. **Never two concurrent runs of a mounted spec; a foreground run carries a hard timeout.**
- **A guard-traps CRASH can leave the tree MUTATED** (errno −4094, a Windows write lock) — restore from the pre-plant backup, md5-verify, re-run; never trust a green after a crash without the restore.
- **A stale banner is worse than none** — a flag with one writer to true and one to false (a dismiss button) is a sticky state; every import clears it first.
- **A by-enclosing-function enumeration goes red on an extraction and is RE-AIMED at its exact count, never loosened** (`spec:import-seam`, 2).
- **"Reads names only" and "costs nothing extra" are two claims** — say which is true at the line.
- **Both sheets, never either** — an inference from a name the app coined (`Market_Events`) is safe; from a plausible analyst tab name (`Metadata`) it is not.
- **A codebase that believes it has a check it never ran** (ten inert `eslint-disable-line` comments) is the enforcement-under-a-flag class again — the third-instance rule routes the read-set defect to the linter, not a fourth patch; **the two fresh-per-render values (`fullTariffTree`, `BLANK_EVENT`) are memoised first, the inert comments audited not inherited.**
- **A scripted patch that breaks parsing yields a vacuous 0** — check that the intended text reached the file before trusting a count.
- **A report's arithmetic is checked against its own row count by the reader** — and the reader's arithmetic is checked first (the advisor was the one wrong).
- **A session that corrects unbriefed scope breaks the gate** (session 2's label tidy) — the pre-existing label is frozen and pinned with the reason.
- **An edit to a trap's seam re-anchors the trap at the NEW seam with the SAME defect, not a weaker one** (209, twice).

*Minted 2026-09-11 → 16:*
- **A locale string with a figure typed in is a caption that can be wrong** — every user-facing number that mirrors a constant is a placeholder filled from it; the zero-occurrence check covers the localised units too (MB / Mo / MiB).
- **Two controls that share a setter share a fate** — the routing line was wiped by the notice's `finally`; a message with its own meaning gets its own state.
- **A native `disabled` on a refusal is a dead click; a refusal is a stated reason** — `aria-disabled` + text, and the click still reaches the rule (trap 107's assertion is the guard).
- **A bar belongs at the entry the user takes, not in the handler the editors share** — the campaign editors seed through `handleEditStart`; barring there would close the pill's route.
- **A campaign is identified by name AND rows** — name alone merges a Volume and a Promotion campaign.
- **A mutation's location is found by executing it, not by parsing it** — apply `mutate()` to a copy and diff; the unparseable-anchor class disappears.
- **Selecting by target file is selecting everything** when one file changes every session; select by the anchor's hunk.
- **A run that does less by default is the guard-admits-0 class in a new costume** — full is the default; the subset is a flag; a subset never prints the line a gate parses.
- **Batching mutations is unsound unless anchors are disjoint AND every trap has a known distinct FAIL line** — the 1206 crash pair is the measured masking case.
- **A harness's positive control cannot be skipped on a build day** — TARGET files change; the cache pays only within a session; the selection is the saving.
- **Every session commits the ledger with its report** — a tracked file every run rewrites is `+dirty` until someone does.
- **A user reads the biggest number as the answer** — when the mechanism's figure is typeset above the outcome's, the mechanism reads as a mistake (REQ-D6-07).
- **Grid and emitted rows are asserted against the same literal; two surfaces are never asserted against each other** (reaffirmed at the 2dp fix: four surfaces, four forms).

*Additions (2026-09-16 → 17):*
- **A user reads the biggest number as the answer — the outcome leads, the mechanism collapses** (REQ-D6-07, reaffirmed built).
- **A measure-first item's STOP is a result, not a failure:** 1008 stopped on "delivered = fitted at any mix" and the build brief was written against what it measured.
- **The read month is a property of the CARRIER** (yield: next month's inflow pool; promotion: its own month) — never assume one clause covers both; clause 7's "month after" was right for yield events and WRONG for Inflow promotions (measured flat at T+1).
- **A basis (Historical/Forecast) that affects a baked rate must be STORED on the event, or every reopen re-derives it** — D5-04's no-change save is the guard; a default that changes (Historical → Forecast) is what exposed it (36 → 35.6 on an untouched save).
- **A rates column follows the DRAFT's cohort, never the loaded or viewed one** (image 8: 31.08 vs 34.65).
- **A spec exclusion that names a document version exactly ages on the next version — patterns, not names** (`spec:ai-hold` and `v3-3-16`).
- **A pin that did not go red is not re-aimed; a pin is ADDED and planted red instead** — `campaign-delete :326` counted the literal `kind: 'campaign'` and stayed green beside the new `'initiative'` kind; the both-kinds pin (7) was added and seen red by plant.
- **A trap whose anchor aged out is re-aimed at the NEW line with the SAME defect and planted red by hand before it is trusted** (269 at the `if (into)` line) — reaffirms the 209 rule.
- **A mounted pair discriminates what a single reading cannot:** "restored" vs "defaulted" (a Historical and a Forecast event reopened side by side); "reset" vs "never stored" ((f): the next draft Forecast AND the added event reopening Historical); "self" vs "other" ((s)/(t): the same key typed into the same control, one re-cases, the other prompts). One reading alone is green for the wrong reason.
- **A green plant on a fixture that hides rounding earns a discriminator** — 245: rounding at 0.01 hides a 0.005 tolerance.
- **A carrier-free predicate paired with a pass-hard-coded writer is one defect away from a group control** — `campaignToggleState` read any carrier while `handleSetCampaignEnabled` wrote pass 0 only; the initiative switch calls the per-row setter with each member's OWN pass (trap 263 guards it).
- **A group's position is a property of its members, re-derived per render** — the header moved up the moment an earlier row joined (I2), which is clause 12 working, not a defect.
- **A layout is derived AFTER the builder when another surface pins the builder's order** — grouping inside `buildEventsSummaryRows` would have moved Compare's rows and reddened its order pin; `initiativeGroups` sits WhatIfTab-side.
- **Two rules, two actions, named separately** — "match ignoring case → the existing initiative" (Group as / Rename onto another) and "re-case my own initiative" (Rename onto self) are different intents behind one input; a rule stated for one was applied to the other (0841's no-op) until Jon named both.
- **A source-level trap is declared source-level** — 271 reddens the engine call, not the dialog's digits (Value/Pricing removal moves no flow the dialog shows); saying so is the honest form of a trap the UI cannot see.
- **The advisor's own wording is the first suspect when a walk step "fails"** — three of this arc's four disputed steps were the advisor's: an expected reading written as a bare word (W3), a step whose target the advisor mis-counted (14 vs 15 events), a step run on the self-case (J2).


## 5. Development history (condensed)

`[INLINE v3.3.16 §5]`


**Phases 0–3 (COMPLETE):** the original defect family dead on main; Phase 0–2 merges (9177d9b, c1ef1a0, 631729c, d1180ad, c806370, eb036c6, 6726d4c, 52843af, 2531585, 67eca3b); Phase 3 sessions G–M (4fb6b15, a39a6d0, 18f6622, a51ec8e, d4a7f8a, ec77b34, f04ec6f) minting the third-instance, mount-above-gates, and spec-may-not-feed-itself rules. Section-B arc closed the Phase 3 walk; Base arc killed the seedless-line family (0d4c40c); verified on screen 2026-08-11.

**R1/R2 arc (2026-08-11→12):** mix engine + card; R1 (6667464, fifth-writer catch); negative rider (03a08fe, rate-sign rule); R2 (a50cca9 + 4d8ae2b); one-baseline (6e9adff). Walked 2026-08-12. R3 held four times; v3.2 misrecord caught by verification; v3.2.1 cut for the Dev 3→4 handoff.

**R3 arc (2026-08-13):** true-state → carrier (81ceadb) → dead session → recovery (reverted clean, findings banked) → surface-1 (4531acf) → surface-2 (c7d4710). Walked green (W5/drop spec-covered — UAT watch). Verification 2026-08-14 (14 VERIFIED; two-field wording corrected). R4/R5 facts established.

**R4/R5 arc (2026-08-17→18):** R5 (b3795f3) and R4 (7e57a25) built; the walks then surfaced seven observations → five gated sessions: display+gating (8f3b34e), residuals (c4e1f48, corrected 1b record), preview scope (77d3de7, measured in), volume-scope measurement (5a14872, stop fired, NO CODE), stored volumes (f6bd0b2, decision-5 supersession); then the restore defect → diagnosis (f678be3: `Is_Active` marks store leaves only; aggregate-active saves marked nothing; fallback took an arbitrary leaf; the 'All'-predicate hypothesis refuted over 46,656 combinations) → active-cohort fix (9c31227). Closing walk green 2026-08-18.

**Scenario Compare arc (2026-08-18→20):**
- *scenarioHelper pricing scope* (62923a3): target/cohortScope honoured through shared functions; Product_L2 omission found and fixed; compounding measured on the real pass (104.04); new spec drives the real computeScenarioForFilter (first gate on this path ever); vacuous zero-guard confession recorded.
- *Filter populate fix + R6 true-state* (3337b46): three-carrier union collector; the silent baseline-fallback sibling found; column disagreements as declared data.
- *fromRow seams* (c426521): market/yield readers extracted with the SOURCE argument (the two inline parses never matched — collapsing them would have been a behaviour change dressed as refactor); yield writer extracted; **spec:yield-roundtrip promoted to the real writer+reader — FINDING 2 FULLY CLOSED** (open since 2026-08-13); transition equivalence spec with self-declared retirement condition.
- *R6 events panels* (ac141ca): per-file typed parse through the real seams (tab-side by measurement); R4 summarisers verbatim; the spec-mirror vacuity catch → buildPerFileEventPanels extracted.
- *Walk* → CV1/CV4 blank → *blank-series diagnosis* (dc4c9c9): data path exonerated BY MEASUREMENT (360 rows, 24 finite months); Brush window hypothesis; matcher-split divergence recorded; populate/render vocabulary contract → DQ.
- *Brush window fix* (5e91b73): clamp + reset + sharpened message; exact-count negative control; third TARGETS gap.
- *Walk* → still blank → **Jon's discriminator: the break is THIRD-FILE-borne, not file-borne** (2155+1349 pair renders). **Advisor read the 1349 workbook directly in-chat**: numerically clean everywhere; Market/Yield_Events are placeholder NOTE SHEETS → the phantom "Unnamed" panel events explained.
- *Third-file diagnosis* (0839): Node replication across all combinations — every state finite and structurally identical; the mechanism proven count-coupled and render-side; phantoms confirmed a defect but not the killer; the silent gate named.
- *Render + phantom fix* (0922): the count mechanism fixed with the 1→4 file transition pinned; the shared placeholder guard (['Note'] shape) at the seam boundary; the named undrawable state (fourth empty-state).
- **Closing walk (2026-08-20) GREEN on all four checks**: four files render at All/All/All and at Corporate/Mobile Voice/Direct; **CV4's weighted £14.34 on screen** (the diagnosis's replicated figure) beside £14.47; panels at 1 event (phantoms dead); brush healthy after churn. **Arc closed.**

**Phases 0–3, Section-B, Base arc, R1/R2, R3, R4/R5, Scenario Compare — all CLOSED**. Anchors: Phase 3 sessions G–M; R3 `81ceadb`/`4531acf`/`c7d4710` (walked 2026-08-13); R4 `7e57a25` + R5 `b3795f3` + restore/active-cohort `9c31227` (walked 2026-08-18); Compare arc closed 2026-08-20 (`62923a3`→`5e91b73` + render/phantom fix).

**The R7 build (2026-08-20, condensed; full record in v3.3.4 §5):** *(v3.3.4 was never committed — the lineage runs v3.3.3 → v3.3.5; the condensed record here is the record.)* true-state 1136 `24cbc6e` (36/36; churn rides pricingMode's WAY-OF-SAYING class; the cliff dissolves); build 1155 `da92622` (the fold; signed verbatim; thirteen specs); walk-round-1 card-state fixes 1425 `b85ae0e` (the exclusive tri-state derived); walk-round-2 wiring diagnosis 2023 `734786a` (**B is ONE LINE** — churn branch dead behind `!spreadEnabled`; one flag two jobs; nine walk defects zero spec-caught → the mount case); scope+add fix 2033 `b4e0752` (fix B, fix A, the mount commissioned 99→127, caught the stale-churnFold read-set defect); edit path 2211 `7330e8e` (edit seeds directly; re-states with excludeId; inversion closed structurally; mount 128→151 — **and the member-decline gate this session added was born unreachable, found 2026-08-21**); totalVol confirmation 2233 `293485f` (read-only; the stop fired; the coincidence of two wrong quantities named).

**The pricing-scope + close arc (2026-08-21, all sessions same day):**
- *0906 pricing-volumes-fix* (`8b7bdc1`, read-only, **the stop fired again**): the advisor-briefed ARPU/volume asymmetry REFUTED structurally — one `baseForecast` object feeds every baseline column; rider 1 correctly not recorded (would have installed an unsatisfiable pair); correction note appended to 2033 §6; the arc's argued-mechanism record reached 0-for-4.
- *1050 pricing-baseline-scope* (`7b456a1`): the fork resolved — the 2026-08-17 decision names THIS surface and was never implemented; **the first mounted measurement on the path** (wide vs narrow loaded, all four figures followed the loaded cohort to the penny; after the fix, invariant at the slice's); the vacuous-fixture catch (arpu 0→24/6, ZERO re-baselines); `resolveEventScopeForecast` with the exactly-two-callers pin; the absence path built with the seam's reason; traps 108/109; mix-card 165.
- *Combined re-walk rounds 1–2 (Jon):* Block B spine green (breakdown moves with dims, positive delta, cumulative fingerprint, seed-on-reopen, plain edit); Block A green (Preview = chart cross-check at 14.28 on the slice; row = Preview to the penny; the absence path shown working on a declined leaf, Add disabled, reason verbatim; edit/base/dilution slice-sized; round trip). Fell out: the :512 misread (advisor's, from the condensation inversion), the inert churn Add, the tooltip label, the member pencil live end-to-end, the Δ-sign question.
- *1200 walk-fixes* (`cf4a610`): **Fix 1 refused** — D5 verbatim says BARRED class; the walked brush absence was the decision working; the excluding term an explicit dated bar guarded by a passing spec and catching trap 97; building the brief would have deleted live guards. Fix 2 built (churn Add disabled-with-reason, pricing card's shape, pin stands at two — counted). Fix 3 built (tooltip prefers campaignName; the trap correctly refused as circular). Mix-card 172; traps 108.
- *Jon overrules D5's group-edit half* (product call: Alessandro flagged missing edit affordances before; remove-and-retype reads as unfinished) → **D5-revised**; picks **option (a)** on the Δ display; drives the member pencil end to end (form, confirm dialog, Save — the zero-change dialog explained: member 1's cumulative = single statement; the walk save left a mixed-semantics campaign, disqualified as a fixture).
- *1302 churn-ramp-edit* (`4a1d110`): **the decline never fired, once** — stale closure via `[setNewEvent]`, born unreachable at `7330e8e`; the whole chain un-gated (handleSaveEdit had no sibling test; single-gate design kept, made reachable); **the mount masked it** — the harness's inline-arrow prop disabled memoisation → the prop-stability rule + trap 111 (which could not have worked before); D5-revised built (group edit seeds the stored statement; `churnExcludedIds` derived; round-trip identity spec — literal equality month for month, never near-zero); Δ display at `outflowDelta` with trap 113's layer proof; rounded input; trap 97 retired; two harness defects found by running it (format-matching assertion; the ambiguous selector → testids). Mix-card 195; traps 110; churn-fold 56.
- *Re-check (Jon, R1–R4):* member pencils decline with the campaign-route reason rendered; campaign editor seeds 20/3/6.67–13.33–20 with the replace-all banner and per-month rate readout; round-trip identity on screen; Δ column negative agreeing with the chart; single churn edits fully. **ARC CLOSED.**

**The user-guide arc (2026-08-31, docs-class, no source changes, no gate):**
- *1847 user-guide-update* (`4297026`): the guide tracked at `docs/`; English brought to `4a1d110` capability — Step 2 restructured on the four tabs, churn mode in full, the Pricing and Value cards, dilution, editable ARPU, events summary, a new Scenario Compare section, expanded save section, generation-complete modal, 12 glossary entries. **Four corrections**: ramp-and-decay described a control that does not exist (the app has a SPREAD dividing a stated total); "outflow entered as negative by default" corrected to the forced-sign truth (`-Math.abs`); Decay glossary entry removed; Ramp rewritten (churn sense only). Translated tail SHA-proved untouched. Jon signed the English 2026-08-31.
- *1924 user-guide-translations* (`de975a7`): all five languages to parity, nothing shed; English SHA-pinned in all nine edit scripts. **Headline finding: 50 of 76 quoted locale keys are byte-identical to English in every locale** — the app renders them in English whatever language is selected (only the churn family, dilution notes, pricing modes, three compare strings, and import_save genuinely translate; French has two extra untranslated dilution keys). Recorded as an app i18n gap, not fixed. The splice-tail parity defect caught by a stale-string sweep and fixed in four languages; two hardcoded labels (Subs / %; Compare's window sizes) render English everywhere.

**Everything through the R7 + pricing arc and the user-guide arc: CLOSED**. Last gated state before 2026-09-01 was `4a1d110`.

**The chart-grid arc (2026-09-01, UAT Day 1–2, CLOSED at `42c55ac`):** *1219 true-state* (`8d00543`, read-only: all four per-scenario ARPUs already fitted; adjusted collapses to one blend over base stock; three denominators under two lines; Compare a second engine; the pricing coupling flagged) → *1312 engine* (`006a15f`: item 0 measured the split at +25.52%; `scenarioArpu.ts` pure, four quantities ALONGSIDE the blend, after pass F, read-only — the pin holds by construction; the 1219 yield claim corrected; **trap 77's `>=` MISSED and was re-aimed**; traps 113) → *1403 control/columns/export* (`d63175b`: sixteen columns key-order-pinned at 29; the two-row control; five keys six locales; readers structurally unaffected + old-file round trip; the `>=` sweep 13→2 re-aimed; traps 116; mount 208) → *Jon's walk* (Blocks green; W3 misread — actual vs forecast; W6a the empty chart; W8 the bulk gap) → *2343 selection fix* (`42c55ac`: the per-tab default `['ARPU']`, never the add; the guard read the wrong quantity; fix at the derivation; trap 120 re-aimed after a vacuous first aim; the negative control found the pills/chart disagreement; the "Volume" label collision → tab testids; traps 117; mount 235) → *re-check* (R1 per-tab default = design; R2 Step 2 = Step 3 at Oct 2026 to the penny; R3 new keys render). **ARC CLOSED.**

**The user-guide addendum, English (2026-09-02 ~10:05, `3c0dcb2`): RAN, REPORT NOT YET RECONCILED** — Jon to upload `reports/2026-09-02-1005-*.md` to the next chat; sign-off pending; the translation pass is briefed on it.

**The locale arc (2026-09-02):** *1028 sweep* (`59731fa`: census 703/822 per locale — the app ~14% translated; scan-i18n's presence-not-translation defect diagnosed in its own comment; all five locales +3,462; exact allowlist 52/184 with reasons and staleness checks; three invented differences caught; hardcoded labels keyed; UAT-D2-01 larger than briefed — `<Line>` without `name` plus hardcoded "Forecast"/"Actual"; the de/it walk found seven literals incl. Step 3's hand-drawn legend and a dropdown disagreeing with itself; `measureDisplay()` keeps export headings English; traps 119) → *1242 follow-up* (`a766d0b`: the seventeen mixConstraint strings are DIAGNOSTIC — premise refuted, five real tooltip reasons found and keyed; `--check` gated with a conditional exclusion; TERMBASE §11's open question answered at four stale sites, identifier pinned 20/10/7, old-save round trip; Step-2 month formatter locale-aware (Step 3/Compare already `yyyy-MM`); de layout measured, fr and Step 3 shed; traps 121; 51 specs).

**Everything through the chart-grid and locale arcs: CLOSED**. Last gated state before this arc was `a766d0b`.

**The guide addendum, English (2026-09-02 10:05, `3c0dcb2`): RECONCILED 2026-09-03** — additive (the guide had never documented the Step-2 chart); line styles verified solid/dashed in source; translated tail byte-identical; no gate (docs only).

**The D2-02/D2-03/D2-04 arc (2026-09-02 → 03, CLOSED at `051a9b3`):** *1456* (`a4550bf`: D2-02 fixed at one cell; D2-03 NOT reproduced at the engine) → *1556* (`bd2cf63`: Jon's 01 Sep save was NOT the walk's; found the coverage/basis divergence — historical rows vs fitted forecast, 17.18% apart, aggregate 14.7% low; caption fixed to count at the view; refused to write the reconciliation spec green) → *1634* (`63cc27f`: on the 02 Sep save, the event was ABSOLUTE (Jon's recreation lost the mode) and reconciled 8000/8000; the percentage variant diverged; `forecastCoverage` weighting by the fitted forecast, `resolveFromStore` returns the leaves it had been discarding; the positive control caught two seam defects — `override-arpu` crashing on a non-optional `resolveForecast` no provider supplies, and the mix-card pin; trap 13 found aged out; exact walked +0.00 still unreproduced) → *Jon's walk on `63cc27f`* (W1/W3/W5/W6 FAIL: +0.00 and "0 applied" at All in the BROWSER for the absolute event the engine applied — the defect was in front of the engine) → *2044* (`0737ebf`: ONE inline copy of `eventScopeMatchesView` at `WhatIfTab:965` tested `!vprodL1`, so only null was 'All'; deleted, shared predicate called; mounted spec reads the rendered KPI; trap 128 reproduces the walk exactly; `:5534` hardcoded English literal found = D2-04) → *Jon's walk on `0737ebf`* (W0–W6 PASS; expander reads Basis 31.84K · 14.8% · In scope 1.5% · Applied +71.46 at All; export 03 Sep 08:01 verified by the advisor from the sheet; **D2-03 CLOSED**; D3-01 logged) → *0857* (`051a9b3`: coverage-0 events leave `appliedEventIds` for `zeroCoverageEventIds`; three readers found; D2-04 keyed as two strings; the scanner had listed the literal in its own deferral list; `eventProRataShare` conflation measured and briefed, deferred on Jon's instruction).

**Everything through the D2-02/D2-03/D2-04 arc: CLOSED at `051a9b3`**

**The 2026-09-03 arc (CLOSED at `2ecdefb`):** *0937* (`2b818ee`: `eventProRataShare` null/0 split; `viewShare`; v3.3.9 committed `079ebc0`; `strictNullChecks` found OFF) → *1012* (read-only parity true-state: the "%-on-promotion" reason found at `EXPECTED.md:4609`; two raw sliders, no component; constraint engine shared; the re-banded pool's hand-rolled predicate found LIVE) → *1049* (`f55ffc8`: reproduced mounted (leaf 0.94 / All 0); SEVEN copies retired, four live in WhatIfTab, three latent in Compare; site 7 had omitted tariff dims — Compare's behaviour changes for tariff-scoped Inflow events, no fixture; structural check; trap 132 proves the pairing) → *Jon's walk on `f55ffc8`* (All reads ARPU Delta +0.16 — **D3-01 CLOSED**, its mechanism was site 2) → *1247* (`20799e3`: Q3/Q4 recovered from git history and recorded before code; stopped on the pool/blend conflict, continued on Jon's ruling with a measure-first gate; branch 1 — pools feed `scenarioArpu.base` directly; card per-scenario with em-dash absence; `impactSummary.arpuDelta` removed; zero blended readers; Q4's second consumer doesn't exist; i18n-scan caught template-literal column names → `NEVER`; the `.ts`/`.tsx` scanner asymmetry) → *1409* (`2ecdefb`: `poolsFor` sized percentage pools from the PERCENT — D3-03; deleted; feed built at three sites; `eventMonthIdx`; Base lagged; D3-02 re-point tested and declined; trap 115 re-anchored).

**Everything through the 2026-09-03 pool-feed arc: CLOSED at `2ecdefb`**

**The record and guide arc (2026-09-03):** *1601* (`0b72574`: v3.3.10 committed `1d723ff`; 28 items audited, 9 NONE, 8 recovered from v3.3.1/v3.3.6/v3.3.8, the never-re-propose list found in NO repo file for four versions and written to EXPECTED.md) → *1618* (`89f68a9`: §16b is three other things; guide §5 corrected from source, tail SHA-identical).

**The padlock/target arc (2026-09-03 → 04, CLOSED at `670c7c3`):** *1626* (`6235e47`: `MixSliderRow`, zero raw sliders; locks persist on both carriers; lock-roundtrip 25; trap (a) went nowhere against mix-card — no Value coverage existed; 1409's trap delta corrected 131→133) → *1821* (`de44ae0`: `spec:trap-anchors` — 10 first-run failures, 5 extractor, 1 relative terminator, 6 real, baselined exact both ways; **never-shed Item 1 NOT delivered**, stated plainly) → *1857* (`d586ffa`: sibling mounted spec 30/30; **D3-04 found and fixed**; trap (c) exists as 140; `TARGETS` crash fixed) → *Jon's walk* (padlock, override, export, reload — all held) → *0958* (`3773ce1`: `MixTargetPanel`, two call sites; the brief's two traps measured impossible → one; two older specs caught the move and were strengthened; trap 143 catches ~0.003 only because compared to the penny) → *Jon's walk* (**D4-01**: target not held under drag) → *1039* (`7b64ddb`: four anchors fixed by planting at each candidate; 22 and 102 stopped with findings) → *1213* (`eb2ffbb`: Branch B — both cards identical to six decimals through `rebalance`, which takes no target; the settled entry says "sum AND blend"; two missing clauses named) → *1226* (`e5aec46`: CRASHED state; three traps not one; the classifier's control caught the backspace; gate RED by design 138/140) → *1327* (`a6d35e5`: 72 and 90 guarded, zero new checks; 140/140 true; no suite runner; 87 unguarded `X[0]` counted) → *1356* (`efd2bd9`: `rebalanceToTarget`; the wall derived as the interval endpoint; minimum change vs brute sweep; trap 145 spans both cards from one mutation) → *Jon's walk* (**D4-01 CLOSED**; the three-tier freeze under target + hold is exactly determined — correct; D4-02/D4-03 observed) → *1452* (`670c7c3`: `MixPctBox`; typing = dragging to 1e-9; `whatif_mix_exactly_determined`).

**Everything through D4-03: CLOSED at `670c7c3`**

**The promotion arc (2026-09-04 → 05, CLOSED at `e5f1e79`):** *1526* (`d92fdaa`: v3.3.11 committed `1c1f224`; step 2 was NOT done at `2ecdefb`; revenue 0 / arpu baked; +10% promotion = +10% plain event to the penny; the Retention half shipped without mounted coverage, stated) → *2003* (`945d648`: retention fixture 400/100 because 100 is non-discriminating; the re-banded pool observable at Base T+1 only — third session to pay; the mix has no per-tier volumes; round-trip 106; traps 150–152) → *Jon's walk of `945d648`* (percentage promotions apply at leaf and All by the leaf's delta — 8,000 + 71.46 + 48.28 in both tooltips; D5-01 seen; D4-02/D4-03 re-checked green; W4 confirmed via the ARPU chart on 05 Sep) → *2138* (`90e2162`: Dilution rides the percentage arm through the already-extracted `dilutionAmountPct`; duration structurally unbuildable; the Pricing spec caught a reuse) → *0747* (`6802cba`: D5-02 does NOT reproduce — configuration table, rate amplification linear, the sign is the yield event's; D5-01 built; 2dp-before-subtract found) → *1602* (`e5f1e79`: yield pool verified by hand at three views/two months on a varying-inflow fixture; subtract-then-round; `npm run suite` committed, 59/59; `spec:survival` 91/24; `applied-count` re-aimed for D3-04's read-set).

**Everything through the promotion arc: CLOSED at `e5f1e79`**

**D5-03 / D5-04 (2026-09-05, CLOSED at `2864d60`):** *1903* (`ede9b43`: v3.3.12 committed `510eb70`; D5-03 confirmed worse than the screenshot — silent conversion on save and a leak into the next promotion; three faults from one gap, fixed; D5-04 measured — arms preserved, rate zeroed; three options recorded) → *Jon's walk* (D5-03 green) → *1950* (`2864d60`: option 1 built through one router; the pinned check red first; the campaign pill found dead and made to route; traps 159/160) → *Jon's walk* (D5-04 green).

**Coverage, D5-05/06, REQ-D6-01 (2026-09-05 → 07, CLOSED at `bd50f27`):** *2031* (`e177d62`) → *2252* (`cb09643`) → *2005* (read-only inventory) → *2027* (`fc718bf`: predicate, pins, columns, summary switch, Compare at the engine, round trip; four traps mis-numbered) → *2128* (`bd50f27`: duplicates renumbered; the id check seen red; four card switches; campaign switch with mixed) → *Jon's walk* (REQ-D6-01 green).

**2026-09-07 — docs, audit, AI hold, python, D5-07, D5-08, chip:** *0918* (v3.3.14 committed `86a0957` by hand after the session hung — a hung `python -c` was later found still running as a background task and killed) → *1202* (prompt audit, read-only) → *1215* (`86d9a1f`, applied) → *1230/1241* (`c84256f`, spec:ai-hold; traps 172/173) → *Jon* (Store alias confirmed and disabled) → *1341* (docs: rule absent from the tracked surface; base drift routine) → *1412* (`050c285`: the python line into CLAUDE.md) → *Alessandro* (markers missing on Revenue/ARPU) → *1436* (`45a437a`, D5-07; traps 174/175) → *walk* → *1515* (`7b43e73`) → *Jon* (ten rows, no way to see them all) → *1533* (`8c0cb71`, D5-08; trap 176) → *walk* → *1658* (`7118e5b`) → *2127* (`e4fd63f`: two controls, chip vs bar).

**2026-09-08 — D5-09, the EFFECT column:** *0915* (read-only inventory: applied set discarded; no ARPU set; zero-coverage cohort-scoped; Compare drops both sets) → *A 0943* (`3d3ef3d`: card, column, four states, allowlist; STOP on the id join; trap 177) → *B 1027* (`ae58aca`: `appliedArpuIds` / `arpuCandidateIds` at sites 2/5/6/8 and Compare's 10/12; Superseded defined; traps 178/179) → *C 1116* (`d63e67a`: `eventRowId` at six sites; flat shape widened; *Not applied here*; Compare mounted; traps 180/181) → *walk* (A–F green) → *1202* (`9b58b48`).


**2026-09-09 — REQ-D6-02, D5-10, D5-11, D5-12, D5-13 (all CLOSED at `f252700`):** *0939* (read-only, superseded by the STOP) → *0948* (`3da3da6` read-only: STOP — baseline revenue not stored, `newBBase` a loop local; a third "has actuals" predicate would be a third definition; the gate re-proved `ai-hold-spec`'s `78e06de` change at 177/177) → *Jon authorises the engine field and one file-level predicate* → *1039* (`9c93807`: the fields, `monthsCarryingActuals`, the Base card corrected, export 29 columns measured, trap 118 re-anchored; UI shed at the brief's own stop point; `python` invoked once in error and hung — the ban confirmed for a session's bash) → *1202* (read-only inventory: `selectedTariffs` never reaches the engine; `'All'` short-circuits both predicates; design (a) vs (b) costed; a saved `'All'` is ambiguous without the list on the event) → *Jon: (b), all three cards, "All in scope" label, ARPU effect scoped too* → *1217* (STOP: no card builds its event in one place — Volume 5, Promotion 3, Pricing 2; the fifth writer) → *1225/1229* (STOPs: the base report did not exist — the UI session had been held) → *Jon: pin at every emitter* → *1232* (`7850298`: carrier, `tariffScopeFor`, `tariffScopeAdmits`, `Tariff_Scope`, pin 9, traps 182–184, 131/181 re-anchored) → *1324* (`fb51506`: label helper, SCOPE cell, `useClearTariffOnDeselect`, Compare; traps 185–187, two green first; survival guarded not re-baselined; the Value card has no Tariff control — addendum STOP) → *Jon's walk A–H green* → *1430* (`0e67708`: selector, `ScenarioDeltaCard`, Revenue card, keys retired, traps 188/189, 190/191 removed as uncatchable, `Math.max(0, findIndex)` defect caught by the mount at 153/177; applied-count, 157, 134 re-aimed) → *Jon: order reversed* → *1531* (`360a591`: chronological, default by key, seen red on `[0]`) → *Jon: yield reading (1)* → *1605* (`7a0bc1d`: one yield emitter, pin 10, four yield scope objects, traps 190/191 reused, 186 re-anchored) → *Jon's walk green; observation: Value vs Tariff toggle changes the baseline blend* → *1652* (read-only: the ratio; equal-weight comparator settled; +10% identical on both axes, absolute not; verdict both) → *Jon: Forecast default, preview, caption* → *1711* (`6538350`: D5-11 built; the `git checkout --` loss and recovery; traps 192–194; 108 re-anchored; pricing-roundtrip 137) → *Jon: Events in effect 0 beside ARPU +6.70* → *1759* (`9118678`: D5-11 verified on disk; 12.94 explained as the stated rate; D5-12 built; traps 195/196; 126 re-anchored) → *Jon: the Inflow preview shows −5.26% for a +93% draft; the 19:30 save read by the advisor: two Sep Inflow yield events* → *1936* (read-only: the tie rule, per-month winners in both engines, four options costed; only (d) fits) → *Jon: (d) extended; (a)/(b) after UAT* → *1957* (`f252700`: the seam carries `arpuIdsByMonth`, the rival and superseded lines, traps 197/198, nothing re-aimed) → *Jon's walk green* → *2113* (docs `6eee37d`; two JSX lines through `fmtMonth`; spec re-pinned on the whole sentence; **suite 61/61, guard-traps 194/194, mounted 208/208, anchors 206/206, ai-hold 13/13; certified `1254aff`, fill `1f1c86e`**).

**2026-09-10 — the advisor chat rotated; REQ-D6-03 on all three carriers; REQ-D6-04; B8/B10; the preview:** *state check 1208* (`dfd767b` read-only: `c11151e` the last code commit, three reports-only commits ahead; NO Hold control in `src/`; the volume spread a share split that terminates; the linear-cumulative shape on churn only) → *1218* (`8728893` → Repo `cba369f`: Volume spread hold, `spreadShape`, `holdPlateauStart`, `Hold` last, pin 3 positions seen red, six-literal differential, traps 202–205; clause 7 flagged, clause 8 named) → *Jon's Walk B* (B1/B2/B4/B5/B7 green; B6 by design; the 16:50 save read by the advisor) → *1556* (`ac9b633` → `10040a4`: churn hold; clauses 7/8 ratified `c876e4c`; POINTS not proportion, 0.97% pinned 1.0% rising; four harness corrections before a trustworthy figure; the label tidy broke the gate and was reverted; `HOLDSHAPE` found missing from the control; traps 206–208) → *Jon's Walk B3* green (117% cohort) → *1651* (`9bbb389` → `fe8f906`: Promotion spread hold through `spreadShape`, `pct/total` retired; ONE row per month; a held % promo campaign cannot reopen (D5-05); the lag measured on the 08:43 save — T+1 exact; traps 209–211) → *Jon's decisions: 200MB, guard-traps targeting, churn points* → *1907* (`fd1ad78` → `083843c`: REQ-D6-04; `runIngest`; two-rAF `nextPaint`; trap 213 red on the DOM check only; `import-seam` re-aimed; three fixtures; traps 212–216) → *Jon's Walk C* (C1 pass; C2/C4 preview wrong, chart right; C3/B9 pass; B10 banner; the 20:33 save read by the advisor) → *2014* (`477b171` → `487e21b`: B8 stale closure reproduced by click order; `horizonMonths` required; B10 sticky banner; `spec:restore-banner` 11; traps 217/218; 209 re-anchored) → *2059* (`a60e3a1` → `7e95170`: controls not exclusive, radio glyph; no label changes with Hold; NO ESLINT, 34 violations, three positive controls red; `churnHold` listed though transitively covered; a patcher's vacuous 0 caught) → *2146* (`b2b1a9c` → `9c44942`: `promoShape` derive-once; tail line; button count; `shape` required; `view-apply-mounted` CRASHED on `as any`; the lag measured promo-vs-volume identical; traps 219–221; 209 re-anchored again).

**2026-09-11 — held rows on the card; checkboxes:** *0225/0839* (`7206ff0`: three runs through the rendered `impact-base-delta` across every selector month — one rule, all carriers, no fix; the two 03:26 runs wedged on open handles after printing, killed on Jon's injection; clause 10 checkboxes, ONE component, trap 222 red only after two discriminators; a guard-traps crash left the tree mutated, restored from backup; Item 3 shed) → *D5-05-held session* (in progress; base `7206ff0`; trap 223).

**2026-09-11 (continued) — D5-05-held, the caption, REQ-D6-05, release:** *0742* (`6fd13f8`: the bar `anyPercentage && !allHeld`; two hidden defects; the Volume-tab harness fault; trap 223 = the previous release) → *0831* (`17b52d5`: the 50MB caption as a placeholder; "usually under 15 seconds"; the routing line's own state; `d5-05-held` registered in the control; restore on throw; traps 224) → *0928* (read-only: REQ-D6-05 inventory, eight questions; EXPECTED.md `f664299`, design note `14d9442`) → *Jon's eight answers* → *1006* (`9036d33`: the build on all three cards; `Mode` column; twelve specs re-aimed; three defects found by building; traps 225–233) → *Jon's Walk D* (D1–D7 green; `D5 promo ramp` never added — a walk-wording slip; the viewing bar not persisted = pre-existing; the 13:00 save read) → *1206* (`fb412a0`: % labels; traps 229/232 CRASHED on the spec's own Hold click under their mutation — guarded; 234) → *Jon's release walk R1–R6* green → *1337* (`296618c`: 2dp display, `fmtDelta` retired, duration-1 label; traps 235/236; 125 re-aimed) → **released `296618c`**.

**2026-09-15 — REQ-D6-06, release, guard-traps:** *1143* (`88e0adb`: campaign delete; one function ×3; the dialog's campaign kind; member bar; traps 237–242; 107 re-anchored) → *Jon's Walk E* green → *1258* (`1bc094b`: the edit bar at the row entry, `aria-disabled` not disabled; the Promotion bin under the dialog; the harness remover made real; traps 243/244; 160 re-anchored) → *Jon's two-step check* green → **released `1bc094b`** → *1919* (`ed4dd8e`, scripts only: per-trap timing; 42.3 min measured; the distribution; the selector costed; batching refused) → *2109* (`e7c88aa` → `21581a2`: anchor-in-hunk by execution; ledger; rotation; control cache; controls C1–C6; targeted 11.0 min; full 42.2 min identical output) → *2229* (`0af35c6`: full the default; wording in CLAUDE.md, both skills, suite help; the ledger commit rule).

**2026-09-16 — REQ-D6-07 built, walked, the basis stored; the advisor chat rotated at the image limit; REQ-D6-08 decided and session 1:** *0647* (the inventory: the padlock mount's band 3.83–27.49) → *0706* (`3510cc9`: `solveForCohortTarget`, the demoted blends, the ratio cell, traps 245–251, the `.detail` breach) → *1008* (measure-first, STOPPED on "delivered = fitted at any mix" — the Promotion arm's mechanism measured: monotone, near-linear, shrinking with volume) → *Jon: clauses 6–13, 14 HELD* → *1019* (`fe0cb7e`: the market-draft slot, the promo lead/target/band/caption; the rates-column defect fixed via `draftScopeForecast`; the blend-band fallback; `spec:ai-hold` by pattern; traps 252–256, 249 retired; **clause 14 held: an untouched Historical promotion re-saved 36 → 35.6**) → *Jon's Walks F (Value card, Alessandro's SOHO / Mobile Voice scenario) and G (Promotion arm) green* → **handover written** → *new advisor chat* → *1222* (`fdc5852`: clause 14 option A — `Tariff_ARPU_Basis` on both sheets, one writer/reader each, absent → Historical; restore on every reopen path; D5-04 "changed fields: NONE"; pins 5/3/3 seen red; `promo-cohort-target` re-aimed to select Historical; `arpu-basis` spec (a)–(e), (c) as a pair; traps 257–259; 142/192/217 re-anchored) → *Jon's Walk W1–W3* (the 09 Sep 14:25 save read: the pre-column campaign reopens Historical, 13.43 × 3 unchanged; a new draft Forecast; the 14:29 export verified; a "FAIL" withdrawn — the advisor's wording had been pressed; re-walked on a quiet tree after the FULL run restarted the dev server) → *1313* (**FULL 254/254 on `fdc5852`**, ledger `67346b6`; 23 TARGETS md5-identical; suite 75/75) → *Jon: clause 16, reset* → *1432* (`ab2c634`: `resetYieldDraft` ONE reset; (f) as a pair; trap 260 red by hand; 142 re-anchored) → *Jon's walk green; the `test reset` toggle/caption observation, unexported* → *1459* (FULL 255/255 on `ab2c634`, ledger `6693bd6` — report cited from 1937, not seen) → *Maddie sent `fdc5852` / `ab2c634`* → *Alessandro's last request: initiatives* → *Jon's seven answers* → *1606* (`d9fe40b`, read-only; decisions 1–7 `6b7fdea`; FINDINGS: the delete dialog Market-only end to end; both campaign saves rebuild rows; the per-row setter already spans three carriers; summary rows sort pass-then-month with no adjacency; eight questions) → *Jon's eight answers via the session; clauses 8–15 `69624ef`, addendum `6343265`* → *1937* (`6c12fab`, session 1: the column on three sheets; `carryInitiative` at THREE campaign saves (the churn save found by grep); `initiativeGroups`; the header switch by own pass; Effect count; Compare column; `spec:initiatives` 45; pins 6/4/4 seen red; i18n 931; traps 261–265 red by hand).

**2026-09-17 — REQ-D6-08 session 2, the case rules, the release gate; session close:** *Jon's Walk H0–H6 green on the advisor's INITIATIVES fixture* (the 07:45 export read: the carry through a duration-4 edit and a Value comment edit) → *0647* (`c9ff73b`, session 2: selection mode, `campaignUnit`, Group as / Ungroup / Dissolve / Rename with the merge panel, the `'initiative'` dialog kind with `nextYield`/`nextPricing` and per-row removers; `campaign-delete :326` NOT red — a both-kinds pin ADDED and planted red; `spec:initiatives` 111; 16 keys, 947; traps 266–271 red by hand, 271 source-level) → *Jon's Walk I0–I8* (I1–I4, I6–I8 green; I5: `launch test` typed lower-case renamed without a prompt — clause 11's case rule, working as written and wrong in use; the 09:37 export read: seven blank cells) → *Jon: case-insensitive* → *0841* (`8422374`, clause 17: `initiativeKey` ONE helper; `existingInitiative` ONE resolver; (p)(q)(r); traps 272–274; 269's anchor aged and re-aimed, planted red; **flagged: a casing-only self-rename made a no-op**) → *Jon's Walk J* (J1 green; J2 run on the self-case; Jon: the self-case is a rename, not a no-op; **Jon's note on the advisor's tone — recorded as a §2 rule**) → *0953* (`a497ff5`, clause 18: the Rename path branches self-vs-other; (s)/(t) as a pair; trap 275) → *Jon's step green (`LAUNCH TEST`, 5 events)* → *1021* (**FULL 270/270 on `a497ff5`**, ledger `8079186`, 23 TARGETS identical, suite 76/76; report+ledger `744ef59`) → *Maddie's note for `a497ff5` drafted* → **v3.3.19 written at close.**

## 6. Current state and what's next — THE RESUME POINT

**Release candidate: `a497ff5` (2026-09-17)** — FULL guard-traps 270/270 (ledger hash `8079186`, src byte-identical), suite 76/76, walked H/I/J; Maddie's note drafted. **Maddie's last CONFIRMED deploy: `1bc094b` (2026-09-15).** `fdc5852` and `ab2c634` were sent to her on 2026-09-16; whether she deployed either is unrecorded — **the first act with Jon is to confirm what she has, then send the `a497ff5` note with the right "replacing" line** (the reset bullet is dropped if she has `ab2c634`). Last gated `src`: `a497ff5`. Report+ledger commit `744ef59`; harness unchanged since `0af35c6`. Next free trap id **276**.

**Base for the next brief: the Repo line of the last report — src `a497ff5`, HEAD the report+ledger commit `744ef59` (plus any docs commit for this document).** The STOP diffs against `a497ff5`: `-- src` EMPTY; `-- scripts` the ledger only. The advisor asks for a missing hash before drafting.

**FIRST ACTS OF THE NEXT ADVISOR CHAT (if rotated):** (1) confirm Maddie's deployed hash and send/record the `a497ff5` note; (2) check the UAT log — findings go ahead of everything; Alessandro's initiatives request was his LAST before close; (3) confirm the queue; (4) every build brief's gate line reads `npm run guard-traps -- --targeted` with the certification line quoted; a release or a "last gated state" runs FULL; (5) walks run on a built preview, never on the dev server during a gate; Jon's pass is the pass.

**THE QUEUE:**
1. **Confirm the deploy; further UAT findings** (UAT closes with Alessandro's sign-off; the date has slipped past 15 Sep).
2. **v3.3.19 committed to `docs/`** (superseded copy removed) — the next session's first act if this document was only delivered in chat.
3. **ESLint — the read-set rule by the linter** (third instance; queued since 2026-09-10; ~12-minute targeted gates make it affordable): devDependencies, `eslint.config.mjs`, `npm run lint` = tsc AND eslint; memoise `fullTariffTree` and `BLANK_EVENT` first; clear the 34 (WhatIfTab 17, App 12, FvA 4, Compare 1) by listing reads with a reason at any deliberate exclusion; audit the ten inert disable comments; a trap on the rule's severity.
4. **Docs:** the user guide (English from source, Jon signs, then translations) when Alessandro will read it — now including initiatives and the cohort-ARPU lead; v3.3.16 → `c11151e` condensed (D5-14/15, Compare pools); REQ-D6-01's "a promotion's two rows" re-measured (one row per month).
5. **After UAT, in order:** the `test reset` toggle/caption observation (export a real file; caption vs restore); the two REQ-D6-07 limits (a percentage promotion's caption prints "{n} subscribers"; an unfinished draft gets the "no fitted ARPU" refusal, naming the wrong cause; the cohort band costs two seam runs per draft change — 8.9 ms/run on the Value card, not re-measured for promotions; the padlock mount resolves every key to the seeded baseline; "a second solver" pinned structurally, not planted); the REQ-D6-08 limits (the "1 events" plural; same-name single Market events ticking together; Value/Pricing editors not closed on a bin; a locale-aware lowercase if ever needed); Step 3 after a restore; the viewing bar not persisted; pricing percentages at 2dp; the locale decimal separator; negative percentage campaigns; de/it screenshots; the yield tie rule; dilution ramp (if ever); the churn Add button testid/label; guard-traps worktrees; the FAIL-line ledger; the v3.3.16 list.

**Standing UAT watches:** as v3.3.18, plus: the Events summary now scrolls at 10+ entries with headers counted (Show all appears at the same point as before, by entries); an initiative header's Effect cell is a count, not a status — a reader may look for a status word.

**An advisor must NOT draft fix sessions for** (closed on main): everything in v3.3.18's list, plus REQ-D6-07 (`3510cc9`, `fe0cb7e`, `fdc5852`, `ab2c634`), the rates-column defect (`fe0cb7e`), REQ-D6-08 (`6c12fab`, `c9ff73b`, `8422374`, `a497ff5`).

## 7. Backlog

`[INLINE v3.3.16 §7]`


*(Unchanged from v3.3.4 except as resolved above: the long tail — leaf-grain challenger redesign; Step 1 Base-series rendering; ragged-history fixture variant; arpuOf's `?? 0`; Model Advisor copy batch + de debt + French cognate notes (correct as-is); Volume/ARPU toggle reset; panel-dead writers; pendingScopedRun; symbol-citation; buildIbroRowsForCohort; promoRebanded (decision: fix; 25.18 vs 25.00); retention-split floor; Month silently gating; phase-2 translations; cross-path comparison; arpuScore TRAP; noImplicitAny 22; dead prop plumbing; old-session challenger flags; edge-actuals fixture; §16b SHELVED; broadAggrSnapshotMap delete-in-passing. The tooltip scenario-label item is DONE (`cf4a610`); the :512 in-anger shed is SUPERSEDED by D5-revised's built-and-walked group edit.)* *(v3.3.4 was never committed — the lineage runs v3.3.3 → v3.3.5; the condensed record here is the record.)*

- **Fixtures:** edge (12,112, ARPU baseline — also carries Customer_Volume + Avg_Subscribers_per_Customer, read by nothing); trimmed (12,432); the 07 Aug save (spec:restore-base); the 17 Aug 1211 save (spec:active-cohort); the 18 Aug saves (1349/1351/2155; 1349's Note-sheet shape is the phantom regression case); PROSPECT_Save_PromoMix_Behavioural. **Jon's pre-fix pricing test events: recreated per the recorded decision. The 2026-08-21 walk save containing the mixed-semantics "test ramp edit" campaign is DISQUALIFIED as a fixture or re-check subject — one single-statement member beside two cumulative ones.**

*(Plus:)* the mislabelled `derive-aggregate` check ("not a single model" asserting `>= 1`); trap 77's anchor now unique (extended, not moved); the `<Trans>` conversion of sentence fragments (`whatif_retention_volume` family; `bulk_don_t`); the `.detail` premise-check hardening; French/Step-3 layout measurement; Step 3 mounted refusal states (engine and mapping driven, pixels not); a group-edit round trip on a CHANGED trajectory (not separately mounted); **the ARPU/summary blended columns' compatibility annotation** in the export (Q3/Q4 session item 4).

- **Fixtures:** as at v3.3.6; the 2026-08-21 mixed-semantics walk save remains DISQUALIFIED; the trimmed fixture (12,432, 74 leaves) was the locale walks' subject.

*(Plus:)* `I18N_PHASE2`/`TRANS_BACKLOG` staleness re-match in `scan-i18n` (the context-file block's check, applied to the two deferral lists); `ForecastContext.resolveForecast` typed non-optional while a mounted provider omits it (type or harness, one of them lies); the mounted harnesses that supply their own resolver take the historical fallback — the forecast-weighted path is exercised only by the new specs; a TERMBASE row for "view"; export compatibility annotation for the two changed columns; the row expander and tooltip established by source reading, not driven; Base-dominance stated qualitatively in the guide, unmeasured on Jon's data.

- **Fixtures:** as at v3.3.8; `test-data/Walks/` is gitignored at `63cc27f`; walk saves cited by sha1: 01 Sep 23:37 `f24ca5d2…` (absolute, NOT the walk's), 02 Sep 16:33 `1322c3e6…` (absolute, recreated), **03 Sep 08:01 `56f5e7e0…` (absolute + percentage — the D2-03 closing artefact)**. The edge fixture (12,112 rows) pairs with all three.

*(Plus:)* `strictNullChecks` — count first; `scan-i18n` TS-literal rule `.ts`-only asymmetry; `I18N_PHASE2`/`TRANS_BACKLOG` staleness re-match; a mutation-trap anchor-staleness sweep (two aged out in two days); the structural predicate check pins ONE spelling — `v == null`, a helper or a ternary escapes it; `ForecastContext.resolveForecast` type-vs-provider; a TERMBASE row for "view"; `scenarioHelper.ts:249`'s own copy of `appliedEventIds` read by nothing; two spread implementations of one shape (Volume and Promotion); two per-band ARPU override draft maps; the "pricing mode" naming collision; six sliders outside the four cards, uninventoried.

- **Fixtures:** as at v3.3.9; the two-leaf mounted fixture now carries per-scenario ARPU bands and retention volume on every leaf (recorded at the fixture). Walk saves by sha1: 01 Sep 23:37 `f24ca5d2…`; 02 Sep 16:33 `1322c3e6…`; **03 Sep 08:01 `56f5e7e0…`** (absolute + percentage; the D2-03 and D3-01 closing artefact).

*(Plus:)* no committed suite runner; 87 unguarded `X[0].` sites across 21 spec files (textual upper bound); the 15 specs no trap points at (`percentage-events` has 6 unguarded sites and no trap); the anchor extractor reads `.replace`, `.split` and unpositioned `.indexOf` only; `FAIL` is a text convention, not a contract; the global class has one member; `MixTargetPanel` has no trap of its own; trap 51's anchor depends on whitespace; two per-band ARPU override draft maps; the "pricing mode" naming collision; six sliders outside the four cards, uninventoried; `I18N_PHASE2`/`TRANS_BACKLOG` staleness; `.ts`/`.tsx` scanner asymmetry; `strictNullChecks` count; `resolveForecast` type-vs-provider; TERMBASE row for "view".

- **Fixtures:** the two-leaf mounted fixture carries per-scenario ARPU bands and retention volume on every leaf; the trimmed fixture's three Product L2 tiers under Corporate drive every mounted mix check (three tiers = exactly determined; no fixture seeds the tariff axis). Walk saves by sha1: 03 Sep 08:01 `56f5e7e0…` (D2-03/D3-01 closing artefact); **04 Sep 09:55** (padlock and target walks — sha1 not yet recorded).

*(Plus:)* three inline amount-mode controls; `fmtDelta` one-decimal vs `eventVolumeLabel`; the Value card's `rollForward` boolean vs the Pricing card's duration enum; the round-trip spec uses builder-shaped literals (a `.ts` spec cannot import the card); the promo Dilution control and gating; non-compounding copy; `spec:survival`'s regex comment stripper; the suite sentinel; the anchor extractor's method list.

- **Fixtures:** the discriminating two-leaf store (per-scenario bands, retention volume, varying inflow 200/300/400 on the yield case); a second store seeded retention 400/100 for percentage retention promotions; the trimmed fixture's three Product L2 tiers for mix checks. Walk saves by sha1: 03 Sep 08:01 `56f5e7e0…` (two market events, NO yield event); 04 Sep 09:55 (two market events + the yield event; sha1 unrecorded); **04 Sep 21:37** (four market events + yield; in Jon's `Downloads/`, not the repo; sha1 unrecorded).

Fixtures: the D5-04 mount carries Retention rows with real value tiers and an ARPU column (the shared props pass `''` for both and cannot produce tiers). Walk saves: unchanged; the 04 Sep 21:37 save (in `Downloads/`) is the D5-03/D5-04 walk subject.

*Plus:* the id/anchor registry ordering; `spec:survival` counts quoted anchor text as dereferences (4 in `guard-traps.ts`); the `Enabled` column and old app versions; the after-UAT items in §6 item 3. Walk saves: **07 Sep 09:13** (ten rows: four volume-moving events on — one a promotion — one ARPU-only Value event on, a five-row campaign off; the artefact for REQ-D6-01, D5-07, D5-08 and D5-09; in `Downloads/`; sha1 unrecorded); **04 Sep 21:37** (five rows, used in the D5-09 Compare walk). `Get-Command python` on this machine resolves to an install-manager alias (version 0.0.0.0) that dispatches to Python 3.14.6 — irrelevant to sessions while the ban stands.


*Plus (2026-09-09):* the after-UAT items in §6 item 3; the Revenue rounding rule unguarded (sub-penny fixture); the delta-selector order pinned by spec only; ARPU and Revenue cards' per-month values asserted by construction, Base card by number; `monthsCarryingActuals` given `[wiDateCol]` alone at the selector (the modal passes the incoming file's column too — correct at each site); the four-card KPI strip unexamined at a narrow viewport; the preview's `reason` path untested; the rival line asserted in English only; `monthLabel` unpinned per locale in that sentence. **Walk saves:** **09 Sep 14:25** (13 events: the D5-10 tariff-scoped promotion, the REQ-D6-02 and D5-11/12 walks; in `Downloads/`); **09 Sep 19:30** (14 events; two Sep 2026 Inflow yield events — "test save W3" All/All non-roll-forward with `{Low Value: 10}`, "inflow draft" SOHO / Mobile Voice roll-forward with `{RED ULTD: 38}`; the D5-13 artefact; read directly by the advisor; in `Downloads/`); **07 Sep 09:13** (ten rows; REQ-D6-01, D5-07/08/09). The 04 Sep 21:37 mixed-semantics save remains a Compare fixture only.


*Plus (2026-09-11 → 16):* the four Volume-card controls the d5-05-held spec finds structurally (amount box, ramp duration, unit arms, campaign-save button) and the campaign-save button have no testids; `removeMarketEvent` remains a prop of `WhatIfTab`, unused there; the Promotion pencil guards at the row and no trap covers its own `return`; an unnamed ramp's rows have no pill and are not barred; the step rule exists twice (`marketEventExportRow`'s `Mode` line and `isCampaignStepMember`) — the writer line is pinned; the control cache does not hash `src` modules a control spec imports without being a TARGET; the B10 spec's fixture synthetic; the percentage grid on the Promotion card not mounted-asserted; the two `pendingChange` kinds pinned by staging-call count (2). **Walk saves:** 11 Sep 13:00, 14:32, 15 Sep 13:55 — all in `Downloads/`; the 13:00 file is the `Mode`-column subject.

*Plus (2026-09-16 → 17):* the `pendingChange` staging pin is now 7 with both kinds named (supersedes the "(2)" note above); `spec:initiatives` is 140 checks in one file and will want splitting before it grows again; the Compare `showInitiativeColumn` prop is the only Compare change — its row objects gained `initiative`/`campaignName` as `undefined` on non-members (the compare-JSON pins did not see them; a member fixture in Compare would); the promo-cohort-target spec's drafts now select Historical explicitly (a pin on the OLD default became a pin on the OPPOSITE — read the intent before re-aiming); the 1459 FULL report on `ab2c634` was never uploaded to the advisor (cited from 1937's ledger line). **Walk saves:** 09 Sep 14:25 (re-read), 16 Sep 14:29, 16 Sep 14:29 INITIATIVES (advisor-made), 17 Sep 07:45, 17 Sep 09:37 — see §1's *Walk saves (2026-09-16/17)*.


---

*Maintenance: at each session close, the advisor updates this document, commits it to `docs/` (superseded copy removed in the same commit), and Jon replaces the project-context copy (latest only — git holds the lineage).*
